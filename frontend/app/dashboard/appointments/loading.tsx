import { OpdTodaySkeleton } from "@/components/skeletons/opd-today";

/** Redirect route → same skeleton as OPD Today destination. */
export default function AppointmentsLoading() {
  return <OpdTodaySkeleton />;
}

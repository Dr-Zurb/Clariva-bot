/**
 * Prefix for medicine notes on the letterhead preview.
 * Helvetica (PDF) cannot render ↳ — keep this WinAnsi-safe and in sync
 * with `backend/src/utils/rx-instruction-marker.ts`.
 */
export const RX_INSTRUCTION_MARKER = ">";

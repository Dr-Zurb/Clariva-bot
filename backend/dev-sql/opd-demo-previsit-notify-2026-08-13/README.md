# Previsit notify full-ladder seed (2026-08-13)

10 video appointments, **15-minute serial starting +60m**, all to `8264602737` / `as.sahilabhi2937@gmail.com`.

No stamps pre-set. Each patient should receive the live ladder as the worker ticks:

| When (relative to that slot) | Email |
|---|---|
| T−30 | check-in (join link) |
| T−15 | nudge (join link) |
| T−5 | nudge (join link) |
| T=0 | starting now (join link) |

First patient (Riya, +60m): first email ~30 minutes after seed. Last patient (Dev, +195m): starting-now ~3h15m after seed.

Requires migration **195**. Cleanup: `delete.sql`.

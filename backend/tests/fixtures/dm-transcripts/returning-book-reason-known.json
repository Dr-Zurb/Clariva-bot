{
  "id": "returning_book_reason_known",
  "description": "mca-15: returning consented patient with reason in state — owned-page link.",
  "expectedBranch": "booking_start_link_first",
  "when": {
    "intent": "book_appointment",
    "just_starting_collection": true,
    "returning_memory_ready": true,
    "reason_for_visit_in_state": true
  }
}

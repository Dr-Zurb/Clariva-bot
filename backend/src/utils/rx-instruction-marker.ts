/**
 * Prefix for medicine notes on the printed Rx.
 * Helvetica cannot render ↳ — keep this WinAnsi-safe and in sync
 * with `frontend/lib/cockpit/rx-instruction-marker.ts`.
 */
export const RX_INSTRUCTION_MARKER = '>';

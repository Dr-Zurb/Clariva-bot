# Halo Aid — full rename (batch spec)

> **Status:** ✅ Complete (2026-07-21). `har-01`…`har-05` shipped; `rg Clariva` clean across `frontend`/`backend` except the excluded shipped migration `022` comment. Teal theme unchanged.
> **Parent:** follows [`../halo-aid-home/`](../halo-aid-home/plan-halo-aid-home-batch.md) (HAH-D2 deferred the app-wide rename to its own batch — this is it).
> **One-line intent:** Rename **Clariva / Clariva Care → Halo Aid** everywhere it is user- or patient-facing (frontend UI + backend messages/documents), swap the teal "C" brand assets for the Halo Aid mark, and update internal strings/docs — **without** re-coloring the app (teal stays; blue re-theme is a separate batch).

---

## Decision lock (owner skipped scope Qs → executed on recommended defaults)

| ID | Decision | Implication |
|---|---|---|
| **HAR-D1** | **Name-only rename. Keep the teal UI theme.** The teal→blue app re-theme is a SEPARATE future batch. | This batch changes strings + brand assets, not `globals.css` `:root` tokens. The Halo Aid **logo** is inherently blue, so the header/favicon become blue marks on an otherwise-teal chrome (transitional, acceptable). |
| **HAR-D2** | **Reach = frontend + backend patient-facing copy.** | Patients see SMS, prescription/transcript PDFs, email sender name, and bot replies — all must read Halo Aid, not just the dashboard. |
| **HAR-D3** | **"Clariva Care" / "Clariva Clinic" / "Clariva Bot" all collapse to "Halo Aid".** | New name is just "Halo Aid" (no "Care"/"Clinic"/"Bot" suffix). |
| **HAR-D4** | **Do NOT touch shipped migrations.** `migrations/022_payments_platform_fee.sql` has "Clariva" in a SQL comment — left as-is per the migrations rule. | Cosmetic-only; changing a shipped migration violates the change rule. |
| **HAR-D5** | **External assets are out of code scope** — the Instagram/Meta **Page name** ("Clariva Care") and any **domain/email address** are renamed by the owner outside the repo. Only the *display* sender name in `config/email.ts` changes here. | Noted in Open questions. |

---

## Grounded inventory (from `rg Clariva`, excluding `* 2.*` duplicates + tests)

### Frontend — user/patient-facing (HIGH)
- `app/layout.tsx` — metadata `default` / `template` / `applicationName` / OG `title` / `siteName`; OG image `/brand/og.svg`.
- `public/manifest.json` — `name` / `short_name`.
- `components/layout/Header.tsx` — wordmark text + `aria-label` + logomark `src`.
- `app/icon.svg` (favicon), `public/brand/{logo,logomark,og}.svg` — teal "C" mark.
- `app/privacy|terms|data-deletion/page.tsx` — "Clariva Care" copy + `<title>`.
- `components/ehr/PatientRxView.tsx` ("Generated by Clariva"), `components/opd/PrimaryCta.tsx` ("Thank you for using Clariva Care").
- `components/consultation/PrescriptionPreSendCheck.tsx`, `ModalityUpgradeRequestModal.tsx` (`displayName`), `public/sw.js` (push title), `hooks/useCallMediaSession.ts` (lock-screen media), `app/c/share-target/page.tsx`.

### Backend — patient-facing (HIGH)
- `src/services/video-replay-otp-service.ts` — SMS body.
- `src/services/ai-service.ts` + `src/controllers/booking-controller.ts` — `practice_name` fallback "Clariva Care".
- `src/templates/prescription-pdf/Footer.tsx` + `src/services/transcript-pdf-composer.ts` — PDF attribution/signature/author/clinic fallback.
- `src/config/email.ts` — `DEFAULT_FROM` sender display name.

### Internal / dev / ops (LOW — comments, dev errors, docs)
- FE: `lib/api.ts`, `lib/api-base.ts`, `lib/clinic/branding.ts` (comments), `hooks/useChartPrefetch.ts` (comments), `vitest.config.ts`, `README.md`, `package.json`, `components/consultation/VideoConsultLobbyHeader.tsx` (comment).
- BE: `src/config/platform-fee.ts`, `src/types/payment.ts`, `src/controllers/health-controller.ts`, `src/templates/prescription-pdf/PrescriptionDocument.tsx` (comment), `Dockerfile`, `scripts/send-test-email.ts`, `scripts/meta-subscribe-comments.ts`, `package.json`.
- **Excluded:** `migrations/022_…sql` (HAR-D4); `* 2.*` duplicate files (working-tree junk); test files.

---

## ⚠️ Scope guard

- **No `:root` / `tailwind.config.ts` token edits** — teal theme stays (HAR-D1).
- **No shipped-migration edits** (HAR-D4).
- **No new features** — pure string/asset rename.
- Keep brand assets consistent with the Halo mark already shipped in `halo-aid-home` (`public/brand/halo-*.svg`).

---

## Task list

| Task | Title | Size | Model |
|---|---|---|---|
| `har-01` | Frontend brand assets + metadata (favicon, brand SVGs, layout, manifest, Header) | S | Sonnet |
| `har-02` | Frontend legal + patient/doctor copy | S–M | Sonnet |
| `har-03` | Frontend internal strings + docs | S | Sonnet/Composer |
| `har-04` | Backend patient-facing copy (SMS, PDFs, email, bot fallbacks) | S–M | Sonnet |
| `har-05` | Backend internal strings + docs + **close gate** (`rg Clariva` clean sweep, lint/typecheck slice) | S | Sonnet/Composer |

---

## Acceptance gate

- [x] `Clariva` in `frontend`/`backend` returns only: the excluded shipped migration `022` comment (`* 2.*` duplicate junk files ignored).
- [x] Header, favicon, PWA name, tab title, OG all read **Halo Aid** with the Halo mark (verified: `manifest.name="Halo Aid"`, `/icon.svg` Halo, assets 200).
- [x] Patient SMS, prescription/transcript PDFs, email sender, and bot fallbacks read **Halo Aid**.
- [x] Teal theme unchanged (`globals.css` `:root` untouched); dashboard still teal.
- [x] Lint clean (0 errors) + typecheck clean for the touched slice (frontend + backend); `transcript-pdf-composer` test updated + green (7/7).

**Note:** the npm package `name` fields (`clariva-bot-frontend` / `clariva-bot-backend`) were left unchanged — internal identifiers, not user-facing, and renaming risks lockfile/reference churn. Parked in `capture/inbox.md`.

---

## Open questions

- **OQ-1:** Owner renames the **Meta/Instagram Page** and any **domain/email address** externally (out of code scope). The `config/email.ts` sender still uses `onboarding@resend.dev` — only the display name changes to "Halo Aid".
- **OQ-2:** Blue app-wide re-theme (teal→primary blue in `globals.css`) — deferred to its own batch (needs visual QA across every screen).

---

**Created:** 2026-07-21. **Status:** ✅ Complete (2026-07-21) — `har-01`…`har-05`. Follow-ups (Meta Page + domain rename, blue re-theme, npm package names) parked in `docs/Work/capture/inbox.md`.

# Task sr-02: Practice info (pattern page)

> Pattern for all simple leaf migrations. Drop currency (SR-D3).

**Status:** ⏳

## Breakdown

- [ ] 2.1 Server page: `requireDashboardAuth` → pass `token` to client.
- [ ] 2.2 Client uses shell + form hook; shadcn `Input` + semantic tokens.
- [ ] 2.3 Remove `appointment_fee_currency` field (moves to Pricing in sr-05).

## Acceptance

- [ ] Name / timezone / specialty / address save still works.
- [ ] No currency field.
- [ ] Lint green.

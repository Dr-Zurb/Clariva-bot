# Tasks: Video consult — Selected features batch (2026-04-28)

**Initiative status:** **Voice Sub-batch 0 + Video Sub-batch A — implementation complete (2026-04-30). Sub-batch B (T2 real polish) — all 10 tickets complete (2026-04-30 / 2026-05-01). Sub-batch C (T3 clinical workflow) — 6 of 8 complete (C2 + C5 + C3 + C4 + C6 + C8 Phase 1; C1 deferred pending Krisp vendor decision; C7 BLOCKED on Plan 10). Sub-batch D (T4 post-call) — 3 of 4 shipped (D.2 D1 post-call summary + D.3 D2 recording / transcript playback + D.4 D3 snapshot review-and-attach all Phase 1; D.1 D4 patient rating deferred — service-reviews infra audit invalidated assumption). Sub-batch E (T5 reliability) — 6 of 7 shipped (E.1 E7 cellular-data warning + E.3 E1 adaptive bitrate + E.4 E2 auto-audio-fallback + E.5 E3 multi-tab kick + E.6 E4 crash-recovery rejoin + E.7 E6 QoS health metrics, all Phase 1, all 2026-05-02). **Sub-batch F (T6 mobile native) — COMPLETE: 5 of 5 shipped (2026-05-02 → 2026-05-03). F.1 F4 battery-saver auto-downgrade + F.2 F1 front/back camera switch + F.3 F2 orientation-lock + landscape layout Phase 1 all shipped 2026-05-02; F.4 F3 Android persistent foreground notification Phase 1 shipped 2026-05-03 — bears voice C10 foundation; F.5 F5 hardware-volume / MediaSession verification Phase 1 shipped 2026-05-03 — closes Sub-batch F with defensive null-setters for 6 unsupported MediaSession actions in `useCallMediaSession` (so a stale handler from a prior hook revision can't surface a misleading scrub bar / skip button on the OS lock-screen widget) + an OEM degradation matrix in the hook JSDoc covering Pixel/stock Android (full) + Samsung One UI ('Internet call' branding override) + Xiaomi MIUI (Battery Saver intercepts MediaSession) + iOS Safari regular tab vs PWA + Bluetooth headset; voice C6 + C10 will inherit when they mount the same hook with `modality:'voice'`.** **E.5 + E.6 each ALSO ship modality-agnostic foundation hooks (`useTabPresenceClaim` + `<MultiTabKickBanner>` for E.5; `useCallRejoinCache` for E.6) that voice C4/C5 + text D2 will reuse on pickup — ZERO backend / DB / ops handoff for either, because Supabase Realtime presence is broker-provided AND the rejoin cache is pure sessionStorage (backend audit confirmed `getConsultationToken` is idempotent + Twilio identity-replacement handles dup participants).** E.2 E5 browser-push deferred 2026-05-02 — push backend (voice C3 / text D6a) audit found neither sibling has shipped, would balloon scope from 2 days to 7+ days; promote back when a sibling lands. **E.4 unblocked a bigger insight: `system_event` is TEXT (not a Postgres ENUM) per Migration 063 — adding new system events (A1's `mute_changed`, A2's `camera_changed`, B3's `hold_changed`, etc.) is a one-line TS change with ZERO migration. The "deferred enum migration window" assumption in those task drafts was a misread of the schema.** B5 (disconnect splash) complete (2026-04-30); B2 (caller-card overlay) + B10 (recording-status pill) + B9 (volume slider) + B8 (video-quality picker) + B1 (pre-call lobby) + B3 (hold call — local UI only; counterparty signal deferred to voice B3 backend, A1 doctrine) + B4 (reconnection UX — local overlay shipped; `useTwilioReconnectState` + `<ReconnectionBanner>` authored ahead of voice B1 per the same A1 doctrine) + B6 (layout swap — Speaker / Gallery / Sidebar with per-device persistence; mobile auto-degrades Sidebar → Speaker; readonly forcing deferred until `<VideoRoom>` gains a `mode` prop) + B7 (picture-in-picture — `usePictureInPicture` hook + pure `pip-support` module; capability-gated button hidden on Safari pre-iOS 14 + in-app webviews; re-enter on reconnect deferred for v1) complete (2026-05-01). Sub-batch C: **C2 virtual background** complete (2026-05-01) — `@twilio/video-processors@^3.2.0` installed with postinstall WASM/TFLite asset copy; `frontend/lib/video/virtual-background.ts` + `<VirtualBackgroundPicker>` ship the `off` / `blur-light` / `blur-heavy` subset; `image:clinic` / `image:neutral` typed in the union but hidden behind `IMAGE_OPTIONS_ENABLED = false` until designer JPGs land in `frontend/public/video-backgrounds/`. **C5 screen share** complete (2026-05-01) — pure `screen-share-support.ts` capability module + `useScreenShare` hook + `<ScreenShareTile>` component shipped; remote-track routing split by `track.name === 'screen'` so screen + camera don't clobber each other; tile container compresses into a horizontal thumbnail strip when sharing; sticky amber privacy banner mounts above the canvas while local share is live; OS-stop event (browser's "Stop sharing" notification) fires the same cleanup as explicit `stop()`; system-message enum extension (`screen_share_started` / `screen_share_stopped`) deferred to combined enum migration window (A2 / E2 / C3 / C5). **Sub-batch D: D.2 / D1 post-call summary** Phase 1 shipped (2026-05-01) — modality-aware backend aggregator + greenfield `<CallPostCallSummary>` mounted in `<VideoRoom>` (post-call inline) + dashboard appointment detail (history). **D.3 / D2 recording playback** Phase 1 shipped (2026-05-01) — deep-link from D.2 summary expanding `<RecordingReplayPlayer>` inline + Plan 10 transcript-timeline placeholder; player extension was already done by Plan 07 + Plan 08 / Task 44. **D.4 / D3 snapshot review** Phase 1 / Phased-MIN shipped (2026-05-01) — 3 doctor-only endpoints + greenfield `<SnapshotReviewPanel>` mounted in summary + artifacts panel; section + soft-discard persisted on `consultation_messages.metadata` (no separate snapshots table; reprojection into clinical-record SOAP tables deferred to Plan 11). **Sub-batch E entered: E.1 / E7 cellular-data warning** Phase 1 shipped (2026-05-02) — pure detection + estimate helpers in `frontend/lib/video/data-estimate.ts` + self-gating `<CellularDataWarning>` modal on the patient join page; routes "Audio-only" choice through the same `localStorage["video-quality"]` key `<VideoRoom>` reads at connect time; renders nothing on Safari / desktop / Wi-Fi / second-visit (one-time per device via the `video-cellular-warning-shown` flag). **Sub-batch F is now CLOSED — all 5 tasks shipped Phase 1.** The remaining open work in the video initiative is: D.1 (D4 patient rating) deferred (service-reviews infra audit invalidated assumption), C1 (noise suppression) deferred (Krisp vendor decision), C7 (live captions) BLOCKED on Plan 10 STT, and E.2 (browser push) deferred until voice C3 / text D6a sibling lands. *38 of 42 video tasks* carry **Complete** / **Shipped (Phase 1)** in their task files; the remaining 4 are explicit deferrals or external blocks documented above. **Decision §34 honored on F.1** — battery-triggered audio fallback reuses E.2's `auto_audio_fallback` system event with new `meta.reason` discriminator (`low_bandwidth` | `battery_low` | `battery_critical`) instead of a new event name; ZERO migration needed since `system_event` is TEXT.

**Parent batch plan:** [plan-video-consult-selected-features.md](../Plans/plan-video-consult-selected-features.md)
**Sibling batches:**
- [Voice consult selected features](../Plans/plan-voice-consult-selected-features.md) — **Sub-batch 0 (hard gate) cleared 2026-04-30**; companion wiring + error surfacing for patient video join shipped.
- [Text consult selected features](../Plans/plan-text-consult-selected-features.md) — push backend share with E5.

**Source product plans (single source of truth for each item's contract):**
- [Video T1 — Quick wins](../../../../Product%20plans/video-consult/plan-t1-video-quick-wins.md)
- [Video T2 — Real polish](../../../../Product%20plans/video-consult/plan-t2-video-real-polish.md)
- [Video T3 — Clinical workflow](../../../../Product%20plans/video-consult/plan-t3-video-clinical-workflow.md)
- [Video T4 — Post-call](../../../../Product%20plans/video-consult/plan-t4-video-post-call.md)
- [Video T5 — Reliability / safety](../../../../Product%20plans/video-consult/plan-t5-video-reliability-safety.md)
- [Video T6 — Mobile native niceties](../../../../Product%20plans/video-consult/plan-t6-video-mobile-native.md)
- [Roadmap index](../../../../Product%20plans/video-consult/plan-00-video-consult-roadmap.md)

**Foundation invariants every task respects** (drawn from the foundation status pages — DO NOT subvert):
- **`safe_uuid_sub()`** in every new RLS policy (E6 migration is the only schema work in this batch)
- **Three-host parity** — every UI item works in `<TextConsultRoom layout='standalone' | 'panel' | 'canvas'>` for the companion-chat surface (Plan F06)
- **`mode='readonly'`** — every new in-call control / mutation affordance is hidden / DOM-removed in readonly views (Plan F07)
- **Recording boundary** — Plan 02 + 08 govern recording artifacts; snapshots (C3) explicitly produce a clinical-record artifact and require consent
- **No PHI in logs** — message bodies, snapshot blobs, and transcript chunks never reach console / Sentry / analytics
- **Bandwidth surfacing** — every UI item that changes call quality (B8 picker, E1 adaptive, E2 fallback, F4 battery) tells the user it happened

**Prefix:** `task-video-XN-` where `X` is the sub-batch (A / B / C / D / E / F = T1 / T2 / T3 / T4 / T5 / T6) and `N` is the order within the sub-batch.

---

## Dependency graph (recommended order)

```text
voice Sub-batch 0 P0.B ──HARD GATE──┐ patient-side /consult/join companion exchange
                                     ▼
Sub-batch A — T1 quick wins (8 tasks; ~2 days)
  A1 (mute mic) ────────────────────────┐ Plan 06 enum 'mute_changed' (reuse from voice)
  A2 (camera off/on) ────────────────────┤ Plan 06 enum 'camera_changed' (NEW; first consumer)
  A3 (call timer) ──────────────────────┐
  A4 (end-call confirm) ──── reuse voice <EndCallConfirmModal>
  A5 (self-view position) ──┐
  A6 (self-view mirror)     │ pure CSS toggle; no deps
  A7 (pre-call cam+mic)     │ ── precondition for B1 (lobby extends A7)
  A8 (network bars + video stats) ──┐
       │                  │
       ▼                  ▼
       feeds B2 (caller card overlay)

Sub-batch B — T2 real polish (10 tasks; ~5 days)
  B1 (lobby) ── extends A7 (pre-call check)
  B2 (caller card) ── consumes A3 + A8
  B3 (hold call) ── reuses voice <HoldCallBanner> + 'hold_changed' enum
  B4 (reconnect UX) ── reuses voice useTwilioReconnectState
  B5 (disconnect splash) ── reuses voice classifyDisconnect
  B6 (layout swap) ── consumes A5
  B7 (PiP) ── independent
  B8 (quality picker) ── couples with E1 when E1 lands
  B9 (volume slider) ── reuses voice <VolumeSlider> + gain-node
  B10 (recording pill) ── reads existing useRecordingState

Sub-batch C — T3 clinical workflow (8 tasks; ~10 days)
  C1 (Krisp noise suppression) ── vendor decision; sibling of voice T3.19
  C2 (virtual background) ── independent
  C3 (snapshot capture) ── PHI-gated (Plan 02 / 08 consent)
       ▼
  C4 (annotations) ── consumes C3
  C5 (screen share) ── independent
  C6 (in-call quick actions) ── existing Rx + scheduling services
  C7 (live captions) ── HARD DEP on Plan 10
  C8 (three-way call) ── multi-participant RLS

Sub-batch D — T4 post-call (4 tasks; ~3 days)
  D1 (post-call summary) ── extends voice <CallPostCallSummary>
  D2 (replay player) ── soft dep on Plan 07
  D3 (snapshot review) ── HARD DEP on C3
  D4 (rating) ── existing service-reviews surface

Sub-batch E — T5 reliability (7 tasks; ~12 days)
  E1 (adaptive bitrate) ──┐
  E2 (audio fallback) ──┴─→ Plan 06 enum 'auto_audio_fallback' (NEW; first consumer)
  E3 (multi-tab kick) ── reuses voice useTabPresenceClaim
  E4 (crash recovery) ── reuses voice useCallRejoinCache (renamed)
  E5 (browser push) ── shared web_push_subscriptions table with voice/text
  E6 (QoS migration) ── new video_call_quality table (only schema work)
  E7 (cellular warning) ── independent

Sub-batch F — T6 mobile native (5 tasks; ~10 days)
  F1 (camera switch) ── independent
  F2 (orientation) ── independent
  F3 (foreground notif) ── reuses voice C10 path
  F4 (battery-saver) ── consumes E2
  F5 (MediaSession) ── sibling of voice T6.35 / C6
```

---

## Task index

### Sub-batch A — T1 Quick wins (8 tasks, ~2 days)

| ID | Title | Source item | Effort | Status |
|----|-------|-------------|--------|--------|
| [task-video-A1](./task-video-A1-mute-unmute-mic.md) | Mute / unmute mic (Plan 06 `mute_changed` system message) | T1.1 | XS (~30 min) | ✅ Complete |
| [task-video-A2](./task-video-A2-camera-off-on.md) | Camera off / on (Plan 06 `camera_changed` enum + `<VideoSelfTile>` placeholder) | T1.2 | S (~2h) | ✅ Complete |
| [task-video-A3](./task-video-A3-call-duration-timer.md) | Call duration timer in header (`mm:ss`) | T1.3 | XS (~30 min) | ✅ Complete |
| [task-video-A4](./task-video-A4-end-call-confirmation.md) | End-call confirmation (reuse voice `<EndCallConfirmModal>`) | T1.4 | XS (~30 min) | ✅ Complete |
| [task-video-A5](./task-video-A5-self-view-position-toggle.md) | Self-view position toggle (PiP corners; persisted per-device) | T1.5 | S (~3h) | ✅ Complete |
| [task-video-A6](./task-video-A6-self-view-mirror-toggle.md) | Self-view mirror toggle (default ON; persisted per-device) | T1.6 | XS (~30 min) | ✅ Complete |
| [task-video-A7](./task-video-A7-precall-camera-mic-check.md) | Pre-call camera + mic check screen | T1.7 | M (~5h) | ✅ Complete |
| [task-video-A8](./task-video-A8-network-quality-bars.md) | Network-quality 4-bar indicator + video-stats tooltip | T1.8 | S (~3h) | ✅ Complete |

### Sub-batch B — T2 Real polish (10 tasks, ~5 days)

| ID | Title | Source item | Effort | Status |
|----|-------|-------------|--------|--------|
| [task-video-B1](./task-video-B1-precall-lobby.md) | Pre-call lobby (clinic branding + countdown; extends A7) | T2.9 | M (~5h) | ✅ Complete |
| [task-video-B2](./task-video-B2-caller-card-overlay.md) | Caller-card overlay header (translucent over remote tile) | T2.10 | S (~4h) | ✅ Complete |
| [task-video-B3](./task-video-B3-hold-call.md) | Hold call (mic + camera paused; reuse voice `hold_changed`) | T2.11 | M (~5h) | ✅ Complete (local hold UI; counterparty signal deferred) |
| [task-video-B4](./task-video-B4-reconnection-ux.md) | Reconnection UX (countdown + Try-now / Rejoin) | T2.12 | M (~6h) | ✅ Complete (2026-05-01) — local overlay shipped; voice B1 will import `useTwilioReconnectState` + `<ReconnectionBanner>` verbatim |
| [task-video-B5](./task-video-B5-disconnect-reason-splash.md) | Disconnect-reason splash (reuse voice `classifyDisconnect`) | T2.13 | S (~3h) | ✅ Complete |
| [task-video-B6](./task-video-B6-layout-swap.md) | Layout swap (gallery / speaker / sidebar; persisted) | T2.14 | M (~6h) | ✅ Complete (2026-05-01) — Speaker / Gallery / Sidebar wired; per-device `localStorage["video-layout"]`; mobile degrades Sidebar → Speaker; readonly forcing deferred |
| [task-video-B7](./task-video-B7-picture-in-picture.md) | Picture-in-picture (browser PiP API) | T2.15 | M (~5h) | ✅ Complete (2026-05-01) — `usePictureInPicture` hook + pure `pip-support` capability module shipped; PiP button hidden on Safari pre-iOS 14 + in-app webviews (Instagram / FB / TikTok / WeChat / etc.) per decision §8; auto-exit on unmount; re-enter on B4 reconnect deferred (no fresh user gesture available) |
| [task-video-B8](./task-video-B8-video-quality-picker.md) | Video-quality picker (Auto / 1080p / 720p / 480p / Audio-only) | T2.16 | S (~4h) | ✅ Complete |
| [task-video-B9](./task-video-B9-volume-slider.md) | Volume slider for remote audio (reuse voice B4) | T2.17 | S (~4h) | ✅ Complete |
| [task-video-B10](./task-video-B10-recording-status-pill.md) | Recording-status pill in caller card | T2.18 | XS (~1h) | ✅ Complete |

### Sub-batch C — T3 Clinical workflow (8 tasks, ~10 days)

| ID | Title | Source item | Effort | Status |
|----|-------|-------------|--------|--------|
| [task-video-C1](./task-video-C1-noise-suppression.md) | Background-noise suppression (Krisp; sibling of voice T3.19) | T3.19 | M (~3 days) | ⏸ Deferred (2026-05-01) — vendor decision pending. Three hard blockers: Krisp budget sign-off (decision §11) unsigned, Twilio Krisp credentials not provisioned, voice C9 sibling also blocked on the same decision. Same defer-pattern as C7 (Plan 10 block). See task file §Status for the unblock checklist. |
| [task-video-C2](./task-video-C2-virtual-background.md) | Virtual background / blur (`@twilio/video-processors`) | T3.20 | M (~3 days) | ✅ Complete (2026-05-01) — `off` / `blur-light` / `blur-heavy` shipped via `@twilio/video-processors@^3.2.0` + postinstall asset copy; `image:*` variants typed but hidden behind `IMAGE_OPTIONS_ENABLED = false` until designer JPGs land |
| [task-video-C3](./task-video-C3-snapshot-capture.md) | Snapshot capture (`<canvas>` extraction → signed-URL upload → clinical record) | T3.21 | M (~3 days) | ✅ Complete (2026-05-01) — full pipeline shipped: `frontend/lib/video/snapshot-capture.ts` + `<SnapshotControls>` + `POST /api/v1/consultation/:sessionId/snapshots` + `snapshot-storage-service.ts` (consent gate + service-role storage write + `kind='attachment'` row + `'snapshot_taken'` system banner). **Decision §14 enforced** by Migration 084 (RLS gate hides doctor-of-patient snapshots from patient viewers; system banner remains visible to both); Migration 083 added the `metadata jsonb` column it keys on. Backend-mediated route (JSON+base64; no `multer` dep). 25 new unit tests — 10 SQL drift-guards on 083/084 + 15 service-layer (validation gates, JPEG magic, JWT triage, consent gate). End-to-end smoke deferred to PR-time QA |
| [task-video-C4](./task-video-C4-freeze-frame-annotations.md) | Freeze-frame + annotations (point / circle / text overlay) | T3.22 | M (~3 days) | ✅ Complete (2026-05-01) — full pipeline shipped on top of C3: `frontend/lib/video/snapshot-annotations.ts` (Annotation type union + `drawAnnotations` compositor + `cssToNativeCoords` helper) + `<AnnotationCanvas>` modal (point / circle / arrow / text + 4-color palette + 3 stroke widths + Undo / Save / Cancel + ESC) + `freezeVideoFrame` helper + `captureSnapshot({prerenderedBlob, annotations})` extension + `<SnapshotControls>` Annotate button (`onRequestAnnotate` / `externalToast` props) + `<VideoRoom>` annotation mode state machine (pause / resume with B3-hold-respecting bookkeeping). Backend persists `metadata.annotated=true` + `metadata.annotations=[...]` via `validateAnnotations` (200 cap, hex colors only, finite coords, positive sizes); system banner switches "captured" → "annotated"; decision §14 RLS still applies (doctor-of-patient annotated row hidden from patient JPEG view, banner remains visible). 15 new validator + service-gate tests on `snapshot-storage-service.test.ts` (matrix + ordering — annotations must throw before consent runs). No new migration (reuses C3's `metadata jsonb` column) |
| [task-video-C5](./task-video-C5-screen-share.md) | Screen share (bidirectional; new layout tile) | T3.23 | M (~3 days) | ✅ Complete (2026-05-01) — `useScreenShare` + `<ScreenShareTile>` + pure `screen-share-support` capability module shipped; iOS hides Share button; OS-stop event handled; tile container compresses into horizontal strip while sharing; privacy banner above canvas; system-message wire deferred to enum migration window |
| [task-video-C6](./task-video-C6-in-call-quick-actions.md) | In-call quick actions (Rx / labs / follow-up / consent panels) | T3.24 | L (~5 days) | ✅ Complete (2026-05-01) — Rx + Schedule shipped (Labs + Consent greyed-out per decision §15). Doctor-only FAB → `<InCallActionPanel>` modal hosting `<PrescriptionForm>` (existing) or `<FollowUpInlineBooker>` (new compact wrapper around `getAvailableSlots` + `createAppointment`). Backend: new `consultation-quick-actions-service.ts` (validate-before-auth gate ordering; doctor JWT + session-ownership check) + 2 new system events (`'rx_sent'`, `'follow_up_scheduled'`) on `consultation-message-service.ts` with per-id LRU dedup; banners carry the real Rx / appointment id (via new `onSent(prescriptionId)` callback on `<PrescriptionForm>`). Side-panel implemented as `position: fixed` overlay (no `<VideoRoom>` layout shift — preserves video grid + chat panel). 56 unit tests (24 quick-actions + 8 emitter + 24 inherited). End-to-end smoke deferred to PR-time QA |
| [task-video-C7](./task-video-C7-live-captions.md) | Live captions (HARD DEP on Plan 10) | T3.25 | L (~5 days) | ⏳ Drafted (BLOCKED on Plan 10) |
| [task-video-C8](./task-video-C8-three-way-call.md) | Three-way call (interpreter / family member; multi-participant RLS) | T3.26 | L (~5 days) | ✅ Phase 1 (2026-05-01) — backend + plumbing + minimal UI shipped. New table `consultation_extra_participants` + RLS extension on `consultation_messages` (3rd `extra_participant` SELECT branch, join-only window, snapshot gate; also fixed the `084` doctor-branch regression that had reverted to bare `auth.uid()`). New `'extra_participant'` Supabase JWT role with synthetic sub `extra:<uuid>` resolved by `public.safe_uuid_sub()`. New service `consultation-extra-participant-service.ts` (createInvite / exchange / revoke / list / recordLeft) — doctor-only, gate-ordering doctrine, single-shot invite tokens, optimistic-lock on `joined_at`, Twilio access-token mint on exchange (graceful fallback to chat-only). Two new system events: `participant_joined` + `participant_left`. Routes: literal-path-first ordering, public exchange + leave endpoints, doctor-scoped invite/list/revoke endpoints. Frontend: `<ThreeWayInvitePanel>` doctor FAB action + `/c/video-invite/[token]` lobby + Twilio-direct join page (Phase 1 deliberately bypasses `<VideoRoom>` for the third participant to keep the surface area small). 39 service unit tests + 43 message-emitter unit tests, all green; backend tsc + ESLint and frontend tsc + next lint clean on touched files. ⏸ Phase 2 deferred — `<VideoRoom>` 3-tile layout integration, auto-switch on `participant_joined`, mounting full `<VideoRoom>` on the invite-token page, inline chat-quote of the join banner. |

### Sub-batch D — T4 Post-call (4 tasks, ~3 days)

| ID | Title | Source item | Effort |
|----|-------|-------------|--------|
| [task-video-D1](./task-video-D1-post-call-summary.md) | Post-call summary screen (extends voice `<CallPostCallSummary>`) | T4.27 | M (~1.5 days) → ✅ **Phase 1 Shipped (2026-05-01).** HARD dep (voice B5) was NOT shipped at exec time, so built greenfield as modality-aware `<CallPostCallSummary>` (modality: text/voice/video, mountContext: post-call/history-detail) with backend aggregator at `GET /api/v1/consultation/:id/post-call-summary` (dual-JWT auth: doctor Supabase JWT OR scoped patient/extra-participant JWT). Mounted in `<VideoRoom>` after `<CallDisconnectSplash>` dismisses (post-call inline) AND in doctor's `dashboard/appointments/[id]` (durable history). Voice mount + patient-facing route (e.g. `/c/post-call/[appointmentId]`) + deep-link CTAs (Rx detail / Plan 07 replay / scheduling / E6 peak-resolution) deferred to Phase 2 / when voice B5 ships / when product confirms patient post-call surface (also unblocks D4 patient rating). 24 unit tests green. Backend + frontend tsc + eslint clean. |
| [task-video-D2](./task-video-D2-recording-transcript-playback.md) | Recording + transcript playback (HTML5 `<video>` + transcript sidebar) | T4.28 | M (~1 day) → ✅ **Phase 1 Shipped (2026-05-01).** Audit found the video player extension was already done by Plan 07 (`<RecordingReplayPlayer>` is the shared replay surface) + Plan 08 / Task 44 (Decision 10 LOCKED — audio-default + "Show video" toggle + OTP gate + watermark + signed-URL re-mint already integrated via `<VideoReplayWarningModal>` + `<VideoReplayOtpModal>`). Real Phase 1 scope reduced to: (1) deep-link wire from D.2 `<CallPostCallSummary>` — added "Watch recording" / "Listen to recording" toggle that expands `<RecordingReplayPlayer>` inline using the same bearer JWT (works for doctor + patient JWTs, both `post-call` + `history-detail` mountContexts), and (2) `<TranscriptReplayPlaceholder>` card mounted in `<ConsultArtifactsPanel>` between the player and the existing Task 32 PDF download (sets the layout slot for Plan 10's chunked transcript-replay endpoint, distinguishes interactive timeline from PDF export). Click-to-seek transcript sidebar + 2-column layout deferred to Plan 10 (chunked transcript-replay endpoint with `timestamp_ms` per chunk — not shipped). Frontend tsc + eslint clean. |
| [task-video-D3](./task-video-D3-snapshot-review-attach.md) | Snapshot review-and-attach (doctor only; consumes C3) | T4.29 | M (~1 day) → ✅ **Phase 1 / Phased-MIN Shipped (2026-05-01).** Audit found two spec-vs-reality mismatches: (1) snapshots are `consultation_messages` rows (kind=attachment, metadata.snapshot=true) — no separate `snapshots` table; (2) no clinical-record SOAP tables exist today — reprojection deferred to Plan 11 / clinical-record infra. Phased-MIN scope: 3 doctor-only endpoints under existing `/api/v1/consultation/:sessionId/...` router (`GET snapshots`, `POST snapshots/:snapshotId/attach-to-section`, `POST snapshots/:snapshotId/discard`) with strict doctor-of-session auth (`admin.auth.getUser` + `consultation_sessions.doctor_id` match). Section persisted on `consultation_messages.metadata.clinical_section` (durable source of truth that any future SOAP reprojection reads from). Discard is soft-delete via `metadata.discarded_at` (idempotent — preserves first timestamp; gallery hides discarded by default; restore-from-discard is a v2 affordance). Greenfield `<SnapshotReviewPanel>` (gallery thumbnail grid + section tallies + modal preview + section radio + Save / Discard with confirm) mounted in `<CallPostCallSummary>` (doctor-only "Review snapshots" CTA visible only when `summary.snapshotsCount > 0`) AND `<ConsultArtifactsPanel>` (doctor-only block above transcript placeholder; never rendered for patient-mount variants). Annotations (C4) are burned into the JPEG at capture time, so the modal renders the JPEG as-is — `metadata.annotations` companion record preserved for future audit / reprojection. 23 unit tests green (validation gate / `isClinicalSection` helper / doctor-only auth / `listSnapshots` filters + signed-URL minting / `attachSnapshotToSection` rejects non-snapshot + non-attachment rows / `discardSnapshot` idempotency + cross-session NotFoundError). Backend + frontend tsc + eslint clean. Reprojection into clinical-record SOAP tables deferred (Plan 11); restore-from-discard UI deferred (v2); annotation editing remains immutable per C4 decision; multi-section assignment + bulk select-and-save out of scope. |
| [task-video-D4](./task-video-D4-patient-rating.md) | Patient rating + free-text feedback (existing service-reviews) | T4.30 | S (~4h) → ⏸ Deferred (2026-05-01) — execution-time audit found the assumed `service-reviews` infra is the wrong surface (existing `service-reviews` = ARM-07 back-office staff workflow, NOT patient feedback). No patient-rating table, no `<PostCallSummary>` mount yet, no patient-facing appointment surface. Real scope is L (~2 days) greenfield. Non-blocking per decision §20 (rating is optional). Promote back to active when D.2 ships AND product confirms the patient-side mount surface (in-tab post-`<CallDisconnectSplash>` vs SMS link to `/c/post-call/[appointmentId]` vs both). Sibling voice T4.26 already deferred for the same reason. Audit + unblock checklist captured in [task-video-D4-patient-rating.md](./task-video-D4-patient-rating.md#audit-finding-2026-05-01). |

### Sub-batch E — T5 Reliability / safety (7 tasks, ~12 days)

| ID | Title | Source item | Effort |
|----|-------|-------------|--------|
| [task-video-E1](./task-video-E1-adaptive-bitrate.md) | Adaptive bitrate / simulcast (Twilio `bandwidthProfile` + UI surfacing) | T5.31 | M (~3 days) → ✅ **Phase 1 Shipped (2026-05-02).** Pure adaptive-bitrate state machine in `frontend/lib/video/adaptive-bitrate.ts` (~340 LOC) — `evaluateAdaptiveTransition(state, { now, networkLevel, picker })` decides downgrade after 10s sustained Twilio quality ≤ 1, upgrade after 30s sustained ≥ 4, with 30s cooldown to prevent flapping. Three adaptive levels in v1: `high` (no explicit constraints — camera + Twilio negotiate), `medium` (720p / 24fps), `low` (480p / 20fps); `audio-only` slot reserved for E.4 / E2 (controller never emits it; `applyAdaptiveLevel('audio-only')` is a dev-warn no-op until E.4 wires the audio-only republish path). `<VideoRoom>` integration: 1s tick effect (the `'networkQualityLevelChanged'` event only fires on changes; sustain windows need to accumulate while level holds steady), `applyAdaptiveLevel(level)` callback that mirrors `handleQualityChange`'s unpublish/publish dance + virtual-background re-apply (C2) + cameraOff preservation (A2) but does NOT update the picker `quality` state — the picker stays at 'auto' so the user knows the controller is in charge AND so the next clause of B8's coupling spec doesn't immediately disable us. Toast on downgrade only (per spec — upgrades are silent); reuses the amber-pill pattern from B7 / C2 / C5; per-level copy (720p vs 480p) for honest UX. Refs (`networkLevelRef`, `applyAdaptiveLevelRef`) keep the interval closure stable across renders so sustain accumulation isn't reset on every level sample. Picker switches to '720p' / '480p' / '1080p' / 'audio-only' suspend the controller; switching back to 'auto' resumes from the current network level. `bandwidthProfile.maxSubscriptionBitrate` untouched — Twilio JS 2.34 has no runtime API; B8's set-once value at connect time stands (documented in source comments as a v1 limitation). Backend zero-touch. Pure functions throughout enable future jest tests when frontend test infra lands. Frontend tsc + `next lint` both clean. Manual DevTools-throttling smoke deferred to PR time (spec §"Manual smoke" is the runbook). |
| [task-video-E2](./task-video-E2-auto-audio-fallback.md) | Auto-degrade to audio-only on bandwidth catastrophe | T5.32 | M (~2 days) → ✅ **Phase 1 Shipped (2026-05-02).** Sticky `<AudioFallbackBanner>` (~165 LOC) mounts at top of video canvas when E.3's adaptive controller emits `'audio-only'`; user clicks "Try video again" to re-publish video + arm 60s cooldown (Decision §25 — flapping prevention) + post `auto_audio_recovered` system row with `durationSeconds`. Backend: new `consultation-auto-fallback-service.ts` (~310 LOC) with doctor-only auth lifted from C6 quick-actions service; `postAutoFallbackBannerHandler` controller; new POST `/api/v1/consultation/:sessionId/auto-fallback-banner` route; 28 unit tests (validation matrix + auth gate + dispatch). Two new `SystemEvent` values (`auto_audio_fallback`, `auto_audio_recovered`) + matching emit helpers in `consultation-message-service.ts` — **zero migration** because `system_event` is TEXT not ENUM (audit unblocker discovered during E.4: see Initiative status above). Frontend wiring: `nextLevelDown('low')` now returns `'audio-only'` so the existing 10s sustain window naturally triggers fallback at the 480p floor; new `audioFallbackCooldownActive` input on `evaluateAdaptiveTransition` blocks ONLY the audio-only transition for 60s (other downgrades / upgrades unaffected; sustain windows continue accumulating so cooldown expiry triggers immediately if conditions still hold). `<VideoRoom>` lit up the no-op `applyAdaptiveLevel('audio-only')` stub from E.3 — synchronous unpublish + stop (mirrors `handleQualityChange` 'audio-only' branch) + bookkeeping (per-session attempt ordinal, engaged-at ref, cooldown ref, doctor-only best-effort POST that patient mounts skip). New `handleTryVideoAgain` callback recreates video at 'auto' constraints + sets cooldown FIRST (before await — covers slow-4G corner case where create/publish takes 3s) + clears banner only after publish succeeds (so a failed restore keeps banner up) + resets adaptive controller state to baseline (otherwise still-bad network could fire downgrade immediately). Manual picker override (B8) clears banner without posting paired `restored` row (user took control via non-banner path; engaged row stands alone in chat transcript). Counterparty banner + B8 picker "Audio-only (auto)" chip deferred to Phase 2 — chat system row (visible in patient companion) + A2's `remoteCameraOff` avatar are sufficient v1 transparency contract. Backend tsc + jest (28/28 pass) + eslint clean; frontend tsc + lint clean. |
| [task-video-E3](./task-video-E3-multi-tab-kick.md) | Multi-tab kick (**SHIPS the foundation** — voice C4 + text D2 will reuse the hook + banner) | T5.33 | M (~3 days) → ✅ **Phase 1 Shipped (2026-05-02).** Audit found voice C4 + text D2 both unshipped (`Drafted`); per spec ("If voice hasn't shipped: ship the hook here per voice C4 contract"), Phase 1 ships the foundation HERE. Critically different from E.2's deferral: Supabase Realtime presence is broker-provided (no DB / no backend / no ops handoff); the hook is ~310 LOC pure frontend. New `frontend/hooks/useTabPresenceClaim.ts` — newest-wins reducer for patient (older tab kicked, newer tab is canonical), tolerated-multi-warn for doctor (decision §29 — multi-monitor); pure exported `deriveStatus(input)` helper for unit testing; broadcast over `consult-tab-presence-${sessionId}` with monotonic `claimed_at` ordering (out-of-order delivery on reconnect cannot regress claim state); C5 cache contract via `sessionStorage('tab-was-kicked-${sessionId}')` flag set on kick edge / cleared on `takeOver()` recovery edge. New `frontend/components/consultation/MultiTabKickBanner.tsx` (~165 LOC) — branches by `(status, role)`: doctor pill (top-center amber, `role="status"` + `aria-live="polite"`, "Open in N tabs · audio routes to the newest tab"); patient overlay (full-screen `z-50`, `role="dialog"` + `aria-modal="true"` + auto-focus on [Take over]). `<VideoRoom>` integration (~120 LOC): `effectiveSessionId = companion?.sessionId ?? sessionId`; one-shot kick teardown effect (`tabKickHandledRef` guards re-entry; releases local tracks + `removeAllListeners()` + `room.disconnect()` + audioRouter dispose; intentionally does NOT call `onDisconnect` or `setStatus('disconnected')` — overlay is the only surface); `handleTabKickTakeOver = takeOver() + 200ms flush + window.location.reload()`. Backend zero-touch. Hook unit tests deferred to voice C4 pickup (`deriveStatus` exported pure helper). Frontend tsc + eslint clean. **Voice C4 + text D2 unblocked** — they import + mount, no foundation work needed. |
| [task-video-E4](./task-video-E4-crash-recovery-rejoin.md) | Crash-recovery rejoin (**SHIPS the modality-agnostic foundation `useCallRejoinCache`** — voice C5 + future text rejoin will reuse on pickup) | T5.34 | M (~3 days) → ✅ **Phase 1 Shipped (2026-05-02).** Audit found voice C5 unshipped (`Drafted`); per spec ("If rename can't happen yet, ship `useVideoRejoinCache` here as a thin wrapper"), Phase 1 ships the foundation directly into `useCallRejoinCache.ts` — no wrapper-then-rename dance. Same E.5 "ship the foundation" path: pure sessionStorage, ZERO backend / DB / ops handoff. Backend audit per spec ("verify, don't refactor") confirmed `getConsultationToken` calls read-only `findActiveSessionByAppointment` (idempotent) + `getJoinTokenForAppointment` mints fresh Twilio JWTs but Twilio handles duplicate participants by `identity` replacement (verified against existing reconnect path); cache reuse path doesn't call backend at all (uses cached JWTs until embedded `exp`). New `frontend/hooks/useCallRejoinCache.ts` (~330 LOC, modality-agnostic): `AutoRejoinResult` discriminated union (`ok` / `absent` / `stale` / `kicked`); 8 pure exported helpers (`computeMinExpiryEpochMs`, `decodeJwtExp` [JWT exp claim decode without signature verification — we just minted them locally], `isSnapshotFresh`, `readSnapshot`, `writeSnapshot`, `clearSnapshot`, `consumeKickedFlag`, `findLatestRejoinCandidate` [scans all `call-rejoin-*` keys + picks freshest valid; needed because `/consult/join` strips `?token=` post-exchange so on crash + reload we don't know `sessionId` upfront]); refuses to write effectively-already-expired snapshots (5s `MIN_TTL_WINDOW_MS` floor); `sessionId` binding defensive on `write()`; honors E.5's kicked flag (consumes it one-shot — once acknowledged the flag's job is done, future reloads follow normal cache-fresh-or-stale path). `/consult/join/page.tsx` integration (~140 LOC additions): cache discovery branch in the URL effect that runs BEFORE `initialUrlToken` validation (rescues the URL-stripped reload case); on cache hit restores `videoData` + companion (synthesized `TextConsultTokenExchangeData` with `sessionStatus: 'live'` placeholders for lobby-display fields) + camera/mic device IDs + `setStep('live')` + `setRejoinedFromCache(true)` (skips lobby + URL exchange + companion exchange entirely); cache write at mint success with strict `min(HMAC fallback 24h, Twilio JWT exp, Supabase JWT exp)` TTL; cache clear in `handleDisconnect` on clean call end (kick path deliberately doesn't clear → kicked-flag still blocks future cache use). New `<VideoRoom>` `rejoined?: boolean` prop (default `false`) → `showRejoinBanner` local state + 3s auto-dismiss `useEffect` → emerald-pill banner ("Reconnected — welcome back") above `<AudioFallbackBanner>`; suppressed during multi-tab kick (kick `z-50` overlay precedence). Doctor-side cache write + voice C5 wire-up (`frontend/app/c/voice/[sessionId]/page.tsx`) + hook unit tests deferred (assigned to voice C5 by spec; pure helpers exported for ease of testing). Frontend tsc + eslint clean. **Voice C5 unblocked** — they import + wire, no foundation work needed. |
| [task-video-E5](./task-video-E5-browser-push-remote-joins.md) | Browser push when remote joins (shared push-notification-service) | T5.35 | S (~2 days) → ⏸ **Deferred (2026-05-02).** Execution-time audit found the HARD dep (push backend — voice C3 OR text D6a, whichever shipped first) is NOT shipped at all in this codebase: no `web_push_subscriptions` migration, no `backend/src/services/push-notification-service.ts`, no `frontend/public/sw.js`, no `usePushSubscription` hook, no VAPID keys provisioned, no `web-push` npm dep. Both sibling tasks ([task-text-D6a](./task-text-D6a-web-push-migration-and-service.md) and [task-voice-C3](./task-voice-C3-browser-push-remote-joins.md)) are `Status: Drafted; pending pickup`. Per this task's own spec ("If neither has shipped: ship the migration + service per voice C3 contract — this task gets the burden then"), proceeding now would balloon scope from a 2-day video-trigger task into a 7+ day cross-batch foundation (D6a + D6b + D6c trio + the video trigger + VAPID ops handoff for env vars + doctor opt-in UX + service worker + cross-modality smoke). That foundation belongs in the text-consult batch (D6a is its native home) or the voice-consult batch (C3), not as a side-effect of the video batch. Promote back to active when text D6a OR voice C3 ships — at that point this task collapses back to its original 2-day scope (just the `participant-connected` webhook trigger + cross-modality `tag` extension). See task file's "Audit + scope decision" + "Unblock checklist" for the full criteria. Same defer pattern as D.1 (rating). Non-blocking for the rest of Sub-batch E. |
| [task-video-E6](./task-video-E6-qos-health-metrics.md) | QoS health metrics — `video_call_quality` table + ingest + sampler | T5.36 | M (~3 days) → ✅ **Phase 1 Shipped (2026-05-02).** **The only schema work in the entire video batch.** Audit: voice C2 sibling NOT shipped (no `voice_call_quality.sql`); video stands alone, voice will replicate independently. Migration `086_video_call_quality.sql` (~210 LOC) ships table + 2 indexes (per-session timeline + partial daily-digest) + 2 RLS policies (doctor branch via `safe_uuid_sub()` + participant branch via `auth.jwt() ->> 'session_id'` matching Migration 085's pattern). Critical schema decision documented in migration header: `user_id UUID NOT NULL` semantics adjusted for patient-side rows — doctor uses real `auth.users.uuid` (matches `safe_uuid_sub()`); patient uses `consultation_sessions.id` as a synthetic UUID surrogate because patient JWT subs are non-UUID `patient:{appointmentId}` strings. Backend service `video-call-quality-service.ts` (~440 LOC) does pure validation (bounded numeric ranges; max 256 samples per request) + dual-branch auth resolution (patient = HS256 verify + `consult_role` + `session_id` claim match; doctor = `admin.auth.getUser` + session ownership) + admin-client INSERT (RLS-bypass with TS-enforced auth, matching `consultation-extra-participant-service.ts` precedent). New POST `/:sessionId/video-quality` route. Frontend: extracted shared parsers from `useVideoCallStats` into `frontend/lib/video/twilio-stats-parse.ts` (~270 LOC; preserves existing hook contract; adds `readAudioInputLevel`/`readAudioOutputLevel`/`readPacketLossPct` for the reporter; audio level scaling handles both 0..1 newer-SDK + 0..32767 older-SDK regimes). New `frontend/lib/video/quality-reporter.ts` (~330 LOC): 10s/30s cadence via recursive `setTimeout` (cadence switches at 60s boundary); 60s flush interval; defensive 200-sample high-watermark; idempotent `dispose` with final fire-and-forget flush; PHI-clean (only network + acoustic + video metrics + Twilio room SID); reads `networkQualityLevel` directly from `room.localParticipant`. `<VideoRoom>` integration via dedicated `useEffect` on `[roomState, role]`: doctor uses `inCallActions.doctorToken`; patient uses `companion.patientAccessToken`. Manual smoke (30-min call → ~64 samples per side) + ops percentile query <1s on populated DB deferred to a real-DB session (migration not applied to dev workspace at implementation time; verification queries documented in migration header). Backend tsc + eslint clean (one fix: unused const removed); frontend tsc + lint clean. |
| [task-video-E7](./task-video-E7-cellular-data-warning.md) | Cellular-data warning (one-time prompt on first cellular video session) | T5.37 | S (~3h) → ✅ **Phase 1 Shipped (2026-05-02).** Pure cellular detection + MB/min estimate helpers in `frontend/lib/video/data-estimate.ts` (tri-state `classifyConnection` + per-quality MB/min map matching Decision §30 figures: 1080p≈10 / 720p≈6 / 480p≈3 / audio-only≈0.5; `'auto'` treated as 720p per `<VideoRoom>`'s connect-time default chain). Self-gating `<CellularDataWarning>` modal mounted at the patient join page level (outside the centered pre-call column so the scrim covers `<VideoConsultLobbyHeader>` + countdown + `<VideoConsultPreCall>`); renders nothing on Safari (no `navigator.connection`), non-cellular connections, OR when the `video-cellular-warning-shown` localStorage flag is already set. Three CTAs: "I'll switch to Wi-Fi" (dismiss-only — does NOT set the flag, so the warning re-fires next cellular attempt — intended nudge), "Continue with audio-only" (writes `localStorage["video-quality"] = "audio-only"` → the same key `<VideoRoom>` reads at connect time → call mounts audio-only without a video track + sets the one-time flag), "Continue on cellular" (just sets the flag). Duration hard-coded to 30 min in the copy + estimate (matches task spec; component exposes a `durationMinutes` prop for when scheduling exposes per-appointment duration). Coordinates with F4 (battery-saver) via the same `video-quality` storage key. No backend, no migrations, no frontend Jest infra in repo (matches D.2 / D.3 / D.4 pattern); helpers are pure + ready to test when frontend test infra lands. Frontend tsc + eslint clean. Manual smoke deferred to PR time (cellular Android + iPhone Safari + desktop Wi-Fi matrix in task file). |

### Sub-batch F — T6 Mobile native (5 tasks, ~10 days)

| ID | Title | Source item | Effort |
|----|-------|-------------|--------|
| [task-video-F1](./task-video-F1-front-back-camera-switch.md) | Front / back camera switch (`enumerateDevices` + `getUserMedia({ deviceId })`) | T6.38 | M (~2 days) → ✅ **Phase 1 Shipped (2026-05-02).** New `frontend/hooks/useCameraSwitch.ts` (~470 LOC) sibling-hook to A7's existing `useCameraDevices` (the F1 spec asked for `useCameraDevices` but that name was already taken by A7's pre-call enumeration with a different return shape — divergence documented in the hook docstring). Internally consumes `useCameraDevices()` for raw enumeration; layers a label-keyword facing heuristic (`front`/`user`/`selfie`/`facetime` vs `back`/`rear`/`environment`/`world`) with index-fallback (index 0=front, 1=back) when ALL labels are empty (iOS pre-permission case). `switchTo(deviceId)` mirrors `<VideoRoom>`'s `handleQualityChange` Twilio dance: pre-flight (room state + same-device + reentrancy guards) → `createLocalVideoTrack({deviceId:{ideal}})` → bail if room torn down mid-await → unpublish + stop old → `publishTrack` new → push to `localTracksRef` → `onAttachLocal` (re-bind to `<video>` element) → preserve `cameraOff` via `.disable()` → `onApplyBackground` (re-apply C2 virtual background) → `writeStoredDeviceId` → `onDeviceChanged` (host hook). `flip()` picks the OTHER facing's first match with "any other device" fallback. localStorage key `'video-camera-device-id'` (distinct from A7's `'video-precall-camera-id'` per spec). 800ms `isFlipping` cooldown clears regardless of success/failure. Initial-current resolution priority: stored → published track `mediaStreamTrack.getSettings().deviceId` → `initialDeviceId` (pre-call) → null. Field rename `current` → `currentDeviceId` to avoid React `.current` ref-convention shadow that confused ESLint exhaustive-deps; stable `currentDeviceIdRef` alias still exposed for republish call-sites. New `frontend/components/consultation/CameraSwitchButton.tsx` (~250 LOC) with `useIsDesktopViewport` (matchMedia `min-width:768px` + legacy Safari `addListener` fallback per Decision §31 viewport-not-UA detection); mobile = circular flip button (h-9 w-9) with inline `FlipCameraGlyph` SVG + dynamic aria-label naming the OTHER facing; desktop = bordered `<select>` dropdown with facing-badged labels ("FaceTime HD Camera · Front"); returns `null` when `hasMultipleCameras` is false; `forceLayout` opt-in for tests; hydration-safe. `<VideoRoom>` edits: `cameraOffRef` mirror added next to `cameraOff` state; hook mounted after `useBatterySaver` block; threaded `onAttachLocal` + `onApplyBackground` + `onDeviceChanged` (latter patches E.4 rejoin cache via `writeSnapshot` + auto-flips A6 `mirrorSelf` based on facing — front=true, back=false, persisted to existing `MIRROR_STORAGE_KEY`); local `cameraSwitchDeviceIdRef = useRef<string\|null>(null)` mirrored from `cameraSwitch.currentDeviceId` via `useEffect` so ESLint recognises stable-ref pattern; replaced 3 republish sites' `chosenCameraId` reads with `effectiveDeviceId = cameraSwitchDeviceIdRef.current ?? chosenCameraId` (`handleQualityChange`, `applyAdaptiveLevel`, `handleTryVideoAgain`) so a flipped camera survives picker swaps + adaptive downgrades + try-video-again recoveries; `<CameraSwitchButton>` rendered in controls bar between Camera and Hold (gated on `!isAudioOnly && !hold.onHold`). Frontend tsc + Next lint clean (`✔ No ESLint warnings or errors`); A7's `<VideoConsultPreCall>` consumer of `useCameraDevices` unchanged. **Known gaps deferred:** no unit tests (matches F.4 / E.4 / E.6 / D.4 precedent); no `mode='readonly'` gate (Plan 07 history viewer prop doesn't exist yet); no telemetry emission (camera flips aren't an interesting product metric); multi-camera simultaneous publish + auto-flip on rotation + AI-detected best camera all explicitly out of scope per spec. |
| [task-video-F2](./task-video-F2-orientation-lock-landscape-layout.md) | Orientation lock + landscape-aware layout | T6.39 | M (~3 days) → ✅ **Phase 1 Shipped (2026-05-02).** New `frontend/hooks/useScreenOrientation.ts` (~250 LOC) — `orient` state from `matchMedia('(orientation: portrait)')` with modern + legacy-Safari listener fallback (matches the existing `<CameraSwitchButton>` viewport pattern); `canLock` from runtime detection of `screen.orientation.lock` as a function (returns false on iOS Safari — silent-degrade path); `lock(target)` returns `Promise<boolean>` with try/catch swallow + dev-only warn (false on rejection: fullscreen required, browser refuses, etc.); `unlock()` only fires if THIS hook acquired the lock (`isLockedByUsRef` flag preserves lock state set by other surfaces); auto-unlock on unmount via cleanup effect. `OrientationLockTarget` enum narrowed from W3C's full type to `'portrait' \| 'landscape' \| 'natural'` (UX-irrelevant `-primary`/`-secondary`/`'any'` not surfaced). New `frontend/components/consultation/OrientationLockButton.tsx` (~130 LOC) — silent-degrade null-render when `!canLock`; tap-to-lock current orientation + tap-to-unlock; inline SVG lock + open-shackle glyphs (no icon dep — matches B6/B8/F1 convention); `aria-pressed` + dynamic `aria-label`/`title`. `<VideoTile>` floating container gains `landscape:w-24 landscape:h-16 md:landscape:w-44 md:landscape:h-32` so the mobile-landscape PiP trims to ≈18% of canvas width vs ≈25% in mobile-portrait (spec ratio satisfied via fixed-px variants — matches A5's existing sizing strategy without flipping to percentage everywhere). `<VideoLayoutSwitcher>` Sidebar option visibility widens to `'hidden md:inline-flex landscape:inline-flex'` so mobile-landscape can pick Sidebar. `<VideoRoom>` mounts the hook, widens `effectiveLayout` derivation (Sidebar→Speaker only when sub-`md` AND portrait — mobile landscape can host Sidebar legitimately), adds `landscape:grid-cols-2` (gallery), `landscape:flex-row` (sidebar parent), `landscape:basis-[70%]/landscape:flex-grow` + `landscape:basis-[30%]/landscape:flex-shrink-0` (sidebar children) so mobile landscape gets the wide-canvas treatment; renders `<OrientationLockButton>` immediately right of `<VideoLayoutSwitcher>` (gated on `hidden={hold.onHold}`). **Pure Tailwind variants — no CSS file.** v3.4.15 ships built-in `landscape:` + `portrait:` responsive variants; F2 spec budgeted `frontend/styles/video-layouts.css` (~50 LOC) but inline class composition is cheaper to maintain (no parallel source of truth, JS-driven `effectiveLayout` and CSS-driven layout stay in lockstep). **Lock button placement diverged from spec.** Decision §32 calls for the button in a `<VideoControlsBar>` overflow menu — no overflow menu exists in the bar today (A4 was supposed to extract `<VideoControlsBar>` but never shipped). Inlined right of the layout switcher (semantically the right cluster — both control "how the video is presented"); compact icon-only design keeps the strip uncluttered; the button can move into an overflow menu when one ships, no API changes needed. **Cross-task verifications.** C4 freeze-frame annotations — coordinate basis pinned to canvas pixels of the frozen snapshot; orientation rotations resize the canvas but don't shift the basis (verified no-op). F4 battery-saver — listens only to `levelchange` + `chargingchange` (W3C Battery Status API); orientation tracking uses an independent matchMedia query (no event-source overlap, no over-reporting risk). **One TS error caught + fixed mid-flight:** `interface ScreenWithOrientation extends Screen { orientation?: ScreenOrientationLike }` collided with `lib.dom.d.ts`'s non-optional `orientation: ScreenOrientation` declaration; switched to a duck-typed `unknown` cast at the access boundary so polyfill-style narrowing doesn't fight the lib types. Frontend `tsc --noEmit` + `next lint --dir hooks --dir components` both green; ReadLints clean across all 5 touched files. Hook unit test deferred (matches F.1 / F.4 / E.4 / E.6 / D.4 precedent — Jest infra not stood up). |
| [task-video-F3](./task-video-F3-android-foreground-notification.md) | Persistent foreground notification (Android; reuse voice C10 path) | T6.40 | L (~1 week) → ✅ **Phase 1 Shipped (2026-05-03) — BEARS THE VOICE C10 FOUNDATION.** Voice C10 had not shipped pre-audit (no `useCallMediaSession`, no `/sw.js`, no `manifest.json`, no `serviceWorker.register` calls anywhere); F.4 took the spec's "If voice C10 has NOT shipped" branch and shipped the FULL hook + SW + iOS banner. New `frontend/public/sw.js` (~210 LOC) — install/activate lifecycle (`skipWaiting()` + `clients.claim()` so upgrades land without reload), message handlers for `'show-call-notification'` (showNotification with `tag: call:${sessionId}` + `requireInteraction: true` + `silent: true` + Mute/End actions; body copy switches between "Call paused" / "Microphone muted" / "Tap to return" based on isMuted+isOnHold flags from the page) and `'hide-call-notification'` (close matching tag), `notificationclick` handler (action 'mute'/'end' → postMessage `'call-notification-action'` to client + focus; bare-tap → focus existing client matching `sessionId` URL fragment, fallback openWindow(deeplink) where deeplink = `window.location.href` captured at show time so no route-shape knowledge in SW — works for `/c/voice/[sessionId]` AND `/c/video-invite/[token]` AND any future surface), version probe handler. NO `fetch` interception (messaging-only SW; lighter footprint, no surprises). NO icon assets referenced (browsers fall back to favicon). New `frontend/hooks/useCallMediaSession.ts` (~375 LOC) — modality-aware (`'voice' \| 'video'`); lazy SW registration on mount (idempotent — repeated mounts return existing registration, marketing pages stay SW-free); MediaSession metadata (title="Voice consult"/"Video consult", artist=callerName, album="Clariva", NO artwork v1 per decision §33); setActionHandler for `pause`/`play`/`stop`/`stoptransport` (decision §14 — pause ALWAYS routes to mute, NEVER to hold; stop + stoptransport BOTH registered because Chrome prefers stoptransport while other browsers use plain stop); `playbackState` synced to `'paused'` when EITHER `isMuted` OR `isOnHold` (accurate signal for OS lock-screen widget); visibilitychange listener (hidden → postMessage `'show-call-notification'` to SW.controller with sessionId/callerName/modality/deeplink/isMuted/isOnHold; visible → `'hide-call-notification'`; cleanup on unmount also posts hide so a stale notification doesn't linger after teardown); SW message bridge listening for `'call-notification-action'` (sessionId guard so a multi-consult browser profile doesn't cross-trigger; routes 'mute' → onPauseRef.current(), 'end' → onStopRef.current()); latest-callback refs for onPause/onPlay/onStop AND latest-state refs for isMuted/isOnHold/callerName so the listeners read the FRESHEST values at the moment of background, not values captured at first mount; mount-only capability detection (`supported`, `isStandalone` via `(display-mode: standalone)` MQ + legacy `navigator.standalone`, `isIOSPWA` = iOS UA AND standalone). New `frontend/components/consultation/IOSPWABanner.tsx` (~75 LOC) — null-renders when `!isIOSPWA` (non-iOS-PWA gets nothing — a permanent banner on regular iOS Safari would be condescending); amber sticky family (matches reconnect / battery / privacy banners); inline SVG info glyph; "Call may pause if you switch apps — keep this tab in the foreground for the best experience." copy; honors `hidden` prop (suppressed during hold). `<VideoRoom>` (+60 / -2 LOC) imports both, mounts hook AFTER `handleEndConfirmConfirm` declared (~L3870 so action handlers can route to in-app handlers unchanged) with `sessionId: sessionId ?? ""` (no-op when `<VideoRoom>` mounts without a session prop), `callerName: remoteLabel` (NO PHI — uses existing "Doctor"/"Patient" role label since real names aren't wired through join token yet, see B2 comments), `modality: 'video'`, `isMuted: micMuted`, `isOnHold: hold.onHold`, `onPause: handleToggleMic`, `onPlay: handleToggleMic` (browser only surfaces ONE button at a time based on `playbackState` — passing toggle is safe), `onStop: handleEndConfirmConfirm`; renders `<IOSPWABanner>` above the canvas next to the screen-share privacy banner family with `hidden={hold.onHold}`. **No icon assets shipped** — `/icons/call-active.png` etc. don't exist in `frontend/public/`; `icon` field omitted from notification + `artwork` omitted from MediaSession metadata; browsers fall back gracefully (favicon for notification; v1-acceptable per decision §33). **No `Notification.requestPermission()` prompt this PR** — permission gating is a higher-trust moment (patient lobby pre-call); SW notification silently fails when permission isn't granted (no error toast, no production console noise). **No track keep-alive** — Twilio's WebRTC heartbeat handles connection-keep-alive; the spec's "5s no-op canvas tick" trick is decision §33 maybe — deferred until real-world OEM reports show track suspension. **Cross-task confirmations:** F.1 battery-saver and `<IOSPWABanner>` are independent banners — both can render simultaneously. F.2 camera switch — Twilio handles unpublish/publish; page stays in foreground; SW notification stays hidden. B3 hold — `playbackState` flips to `'paused'` and notification body switches to "Call paused"; mute action still routes to `handleToggleMic` (decision §14). A1 mute — MediaSession `pause` calls SAME `handleToggleMic` the in-app Mute button uses (single source of truth). A4 end-call confirm — MediaSession `stop` + SW `end` both route to `handleEndConfirmConfirm` which dismisses the dialog (already-open) AND calls `handleLeave()`. **Verified clean:** Frontend `tsc --noEmit` (~24s) + `next lint --dir hooks --dir components` both green ("✔ No ESLint warnings or errors"); ReadLints clean across all 4 touched files. **F.5 unblocked** — hardware volume / MediaSession sibling had a HARD dep on F.3's MediaSession declaration; that declaration is now in place. **Deferred (Phase 2):** `manifest.json` + PWA install prompt; icon assets; `Notification.requestPermission()` prompt; track keep-alive (5s no-op tick); OEM smoke matrix on Samsung/Pixel/Xiaomi/OnePlus hardware (spec acknowledges out-of-PR-scope: "Tests: Manual smoke matrix (cannot reliably E2E test SW notifications)."); F.1 battery banner text update on fallback; MediaSession artwork (decision §33 — static icon v1 + last-known frame v2); voice C10 mount in `<VoiceConsultRoom>` (would expand scope; voice C10 will mount the same hook with `modality: 'voice'`); frontend hook unit tests (matches A1-F.3 precedent — Jest infra not stood up). |
| [task-video-F4](./task-video-F4-battery-saver-downgrade.md) | Battery-saver auto-downgrade (15% prompt; 5% force audio-only) | T6.41 | S (~3h) → ✅ **Phase 1 Shipped (2026-05-02).** New `frontend/hooks/useBatterySaver.ts` (~265 LOC) wraps the W3C Battery Status API with idempotent prompt/force latches via refs (no re-render thrash on every `levelchange`); exported threshold constants (`BATTERY_PROMPT_THRESHOLD = 0.15`, `BATTERY_FORCE_THRESHOLD = 0.05`, `BATTERY_RECOVER_THRESHOLD = 0.20` — 20% recover > 15% prompt debounces flapping); iOS Safari graceful degradation via `'getBattery' in navigator` short-circuit; mount-time evaluation for users joining already-low. New `frontend/components/consultation/BatteryWarningBanner.tsx` (~210 LOC) with three modes (amber `'prompt'` + Switch/Keep video, red `'forced'` + plug-in copy, emerald `'charging'` + optional Re-enable video CTA); inline SVG glyphs (no icon dep); `data-mode` attrs for smoke tests. **Decision §34 honored** — reuses E.2's `auto_audio_fallback` system event with new `meta.reason` discriminator (`low_bandwidth` | `battery_low` | `battery_critical`) instead of a new event name (zero migration; `system_event` is TEXT). Backend `emitAutoAudioFallback` extended with optional `reason` (defaults `'low_bandwidth'` for back-compat); per-reason body copy ("slow connection" vs "low battery"); correlation key gained `reason` suffix so battery + bandwidth attempts dedup independently; new exported `AutoAudioFallbackReason` union. Validator + service relax `thresholdLevel` to optional (`number \| null`) — required only when `reason === 'low_bandwidth'`. Frontend `applyAdaptiveLevel(level, engageOptions?)` extension threads `reason` into the engaged POST body (`thresholdLevel: reason === 'low_bandwidth' ? 1 : null`). `<VideoRoom>` mounts the hook + state machine (`showBatteryPrompt`, `batteryFallbackForced`, `showBatteryCharging`); forward refs (`batteryApplyAdaptiveRef`, `handleTryVideoAgainRef`) bridge the hook callbacks to callbacks declared further down (sync `useEffect`s mirror alongside the existing `applyAdaptiveLevelRef`); JSX block renders `<BatteryWarningBanner>` next to `<AudioFallbackBanner>` with mode-resolution priority (forced > charging > prompt; prompt suppressed when bandwidth fallback is active to avoid stacking amber banners). Re-enable video CTA in charging mode routes through `handleTryVideoAgain` — same E.2 restore primitive the bandwidth banner uses (inherits 60s cooldown + matching `auto_audio_recovered` chat row pairing). Backend tsc + ESLint clean; frontend tsc + Next lint clean. **Known gaps deferred:** B8 picker `disabledOptions` to grey out audio-only during forced state (recorded for B8 follow-up; current "Plug in to re-enable video" copy is the v1 escape); F.3 foreground-notification text wiring (picked up when F.3 ships its primitive); predictive battery-drain estimation + hardware power-saving mode detection (out of scope per spec); frontend hook unit tests (matches existing E.4/E.6/D.4 precedent — Jest infra not in place). |
| [task-video-F5](./task-video-F5-hardware-volume-mediasession.md) | Hardware volume keys + MediaSession (sibling of voice T6.35 / C6) | T6.42 | XS (~1h) → ✅ **Phase 1 Shipped (2026-05-03) — closes Sub-batch F (5 of 5).** Smoke verification of F.4's MediaSession surface with two surgical patches landing in `frontend/hooks/useCallMediaSession.ts` (+75 / -0 LOC; ZERO new files; ZERO `<VideoRoom>` changes — F.4's mount already satisfied F.5's contract). **Audit confirmed F.4 covers 4 of 6 acceptance items** — `navigator.mediaSession.metadata` set on call start with title/artist/album, action handlers for `pause`/`play`/`stop`/`stoptransport` registered (decision §14: pause = mute), `playbackState` synced to `'paused'` when isMuted OR isOnHold else `'playing'`, hardware volume keys route to call audio via Android Chrome browser default (no code needed — once `playbackState === 'playing'` is declared, browser auto-routes hw-vol input to the media-session-tagged `<audio>` element; Twilio's `track.attach()` uses standard `<audio>` so it inherits). **Patch 1: defensive null-setters for unsupported MediaSession actions.** Inside the metadata/action-handlers `useEffect`, after pause/play/stop/stoptransport registrations, the hook now explicitly calls `setActionHandler(action, null)` for each of `seekto` / `seekbackward` / `seekforward` / `nexttrack` / `previoustrack` / `skipad`. Three motivations: (a) **Stale handler hygiene** — MediaSession is a `navigator`-level singleton; handlers persist across navigations until explicitly cleared; a prior hook revision that registered `seekto` would surface a scrub bar even after the new revision stopped touching it. (b) **Misleading UX** — a scrub bar / skip button on a LIVE call has no semantics; some OEMs throw `NotSupportedError` from the JS callback when tapped, surfacing an OS-level "media error" toast. (c) **Xiaomi MIUI compliance** — known to render every action even when `playbackState === 'paused'`; explicit clears keep the OEM-stripped widget tidy. Each null-set wrapped in its own try/catch — some browsers throw `NotSupportedError` for unrecognised action names; one bad name shouldn't skip the rest of the loop. **Patch 2: OEM degradation matrix added to hook JSDoc** — comprehensive doc block enumerating: Pixel/stock Android (full support; lock-screen widget shows "{Voice\|Video} consult / {caller}" with pause/end actions; hw-volume routes via browser default), Samsung One UI (some builds add a Samsung-branded "Phone call" widget INSTEAD of standard MediaSession surface — actions still work but title bar reads "Internet call" not "Video consult"; out of our control), Xiaomi MIUI (notorious for stripping MediaSession on aggressive battery-saver modes; widget may not appear at all even with `playbackState='playing'` declared; no code-side patch possible — MIUI's Battery Saver intercepts SW + MediaSession before browser engine sees them; F.5 defensive null-setters keep stripped widget tidy when it DOES show), iOS Safari regular tab (MediaSession metadata + action handlers work for lock-screen "Now Playing" widget on iOS 15+; hw-volume routes by default), iOS Safari PWA / standalone (Apple gates lock-screen call controls — requires native shell; out of scope; `<IOSPWABanner>` warns up-front), Bluetooth headset media buttons (voice T6.34 owns explicit BT mapping; F.5 inherits BT pause/play through MediaSession by default on most BT stacks). **Cross-task confirmations:** F.4 foreground notification — defensive null-setters run AFTER pause/play/stop/stoptransport registrations so they don't accidentally clear actions we DO want (verified via code review — `for` loop only touches unsupported-action list). A1 mute / A4 end-call — no regression; action handlers still route to `handleToggleMic` / `handleEndConfirmConfirm` exactly as F.4 wired them. Voice C6 hardware-volume-key (sibling) — voice C6 will get the same `playbackState='playing'` signal automatically when it mounts `useCallMediaSession({modality:'voice', …})` in `<VoiceConsultRoom>`; no additional code needed. Voice C10 foreground notification (sibling) — same hook; voice C10's mount inherits the F.5 defensive cleanup + OEM doc. **Verified clean:** Frontend `tsc --noEmit` (~13s) + `next lint --dir hooks` both green ("✔ No ESLint warnings or errors"); ReadLints clean across the touched file. No new tests required per spec ("extension of F3"). **Sub-batch F closure:** F.5 is the final task in Sub-batch F. With this PR landing, Sub-batch F is 5 of 5 complete (F.1 + F.2 + F.3 shipped 2026-05-02; F.4 + F.5 shipped 2026-05-03). **Known gaps deferred:** OEM smoke matrix execution needs Samsung Galaxy S22 + Pixel + Xiaomi Redmi Note + iOS Safari (16+) hardware (spec acknowledges hardware-bound; deferred to QA window). Custom hardware key remapping out of scope. iOS lock-screen call controls out of scope (requires native shell). Bluetooth media-control button mapping owned by voice T6.34. `MediaSession.setPositionState` explicitly NOT called — would render a scrub bar; calls aren't seekable. |

---

## Code anchors (existing — audit at execution time)

| Area | Location |
|------|----------|
| Video consult room (every task touches this) | `frontend/components/consultation/VideoRoom.tsx` (~630 lines) |
| Patient consult page (HMAC exchange + companion bootstrapping) | `frontend/app/consult/join/page.tsx` |
| Voice room companion mount (parity reference; many components reused) | `frontend/components/consultation/VoiceConsultRoom.tsx` |
| Text consult room (companion chat host inside video) | `frontend/components/consultation/TextConsultRoom.tsx` |
| Video escalation (Plan 08) | `<VideoEscalationButton>`, `<VideoConsentModal>`, `<VideoRecordingIndicator>` |
| Recording controls (Plan 02 / 05) | `<RecordingControls>`, `<RecordingPausedIndicator>` |
| Replay flow (Plan 07) | `<VideoReplayOtpModal>`, `<VideoReplayWarningModal>` |
| Twilio Video JS SDK | `connect`, `Participant`, `LocalVideoTrack`, `LocalAudioTrack`, `getStats()`, `bandwidthProfile` |
| Notification service | `backend/src/services/notification-service.ts` |
| Push backend (shared with voice C3 / text D6a) | `backend/src/services/push-notification-service.ts` |
| Diagnose script (run after E6 migration) | `backend/scripts/diagnose-text-consult-jwt.ts` |
| Plan 06 system-message enum (extends in A1 + A2 + B3 + E2) | `consultation_messages.system_subtype` |

---

## Convention reminders

- **Mark tasks `Status: Implementation complete (YYYY-MM-DD)`** when the PR merges; flip to `Shipped (YYYY-MM-DD)` after staging smoke.
- **When a sub-batch closes,** update the parent batch plan (`plan-video-consult-selected-features.md`) tier-section row and the source product plan's `[SELECTED 2026-04-29]` marker → `[SHIPPED YYYY-MM-DD]`.
- **Migration numbering:** Sub-batch C (snapshot) claimed `083` (`metadata jsonb` column) + `084` (snapshot visibility RLS) on 2026-05-01. Sub-batch C8 (extra participants) claimed `085` on 2026-05-01. Sub-batch E (E.7 QoS) claimed `086_video_call_quality.sql` on 2026-05-02. Next free number is `087`. Voice batch will claim one for `voice_call_quality` when C2 ships; text batch had been earmarked `083`–`086` (now negotiate `087`+ since the lower numbers are taken).
- **Plan 06 enum extensions:** combine `camera_changed` (A2) and `auto_audio_fallback` (E2) into a single migration if A2 and E2 ship in the same window. If they're far apart, ship them as one-line ALTER TYPE migrations.
- **No task is allowed to:** rewrite an existing RLS policy without reusing `safe_uuid_sub()` (Plan F04 invariant); ship a UI affordance that's visible in `mode='readonly'` (unless explicitly read-only); log message bodies / snapshot blobs / transcript chunks to console / Sentry / analytics; introduce a new vendor beyond Krisp (C1) which is shared with voice T3.19.
- **Cross-batch coordination required for:** A1 (`mute_changed` enum — voice batch shipped first); B3 (`hold_changed` enum — voice batch shipped first); E4 (rename voice `useVoiceRejoinCache` → `useCallRejoinCache`); E5 (push backend share with voice C3 / text D6a); F3 (foreground notification path share with voice C10); C1 (Krisp budget share with voice T3.19); D1 (rename voice `<VoicePostCallSummary>` → `<CallPostCallSummary>`).

---

**Last Updated:** 2026-04-30

# W<NN> postponed

> Copy to `Business/<YYYY>/<Month>/W<NN>-<YYYY-MM-DD>/postponed.md`.
> Tasks taken off a day **on purpose**. Not leftovers (those go in `pending.md`).
> One line each: what, track id, why later. Do not invent a date.

- [ ]

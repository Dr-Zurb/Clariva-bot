# Inbox

Add new items **below** this line (or at the top of the list—stay consistent). Uncheck ` [ ] ` until promoted or done.

---

- [ ] 2026-04-22 — **Plan 09 · Task 55 follow-up — appointment detail page mount for `<ModalityHistoryTimeline>`:** Task 55 ships the component + the `GET /modality-change/history` endpoint but does not mount the widget anywhere. The appointment detail page lives under `frontend/app/...` (exact path is a PR-time probe — candidates: `app/appointments/[id]/page.tsx`, `app/dashboard/appointments/[id]/page.tsx`, `app/(doctor)/...`). Wiring: (a) identify the detail-page component for both doctor + patient views; (b) render `<ModalityHistoryTimeline>` below the recording artifacts section with `className="mt-6"`; (c) guard the mount with `initialModality !== currentModality || upgradeCount > 0 || downgradeCount > 0` (no transitions → skip rendering entirely); (d) wrap with a `<details><summary>Modality timeline</summary>` collapsible on mobile (defaults open on desktop via `md:open`); (e) thread `viewerRole` / `doctorName` / `patientName` from the page's existing session-fetch call. Un-defer trigger: first QA pass on a completed consult that had mid-consult modality switches OR Plan 09 go-live pre-flight.

- [ ] 2026-04-22 — **Plan 09 · Task 55 follow-up — backend integration test `modality-history-endpoint.test.ts`:** Task 55's acceptance criteria list a `backend/tests/integration/modality-history-endpoint.test.ts` with five scenarios (ordering `occurred_at ASC`, RLS 403 for non-participants, zero-transition empty-entries + `initialModality == currentModality`, paid-upgrade + auto-refund-downgrade round-trip with correct fields, refund-pending with `razorpay_refund_id IS NULL`). Deferred because the `backend/tests/integration/` harness targets a live Postgres instance that isn't part of the default test runner (matches Task 46/47/48/49 deferrals). Bundle with the Plan 09 integration sweep — whichever live-DB harness lands first authors the fixtures for modality history reads + writes. Un-defer trigger: live-Postgres integration harness bootstraps OR Plan 09 go-live pre-flight.

- [ ] 2026-04-22 — **Plan 09 · Task 55 follow-up — frontend component tests for `<ModalityHistoryTimeline>`:** Deferred per the repo-wide frontend-test-harness deferral. Minimum set when the harness lands: (a) renders `Started as TEXT` + `Consult ended` anchors on a happy path; (b) variant copy across all 6 `(initiatedBy × billingAction × viewerRole)` combos — patient-paid-upgrade as doctor viewer, patient-free-upgrade as doctor viewer, doctor-free-upgrade as patient viewer, patient-no-refund-downgrade as doctor viewer, doctor-auto-refund-downgrade settled as patient viewer, doctor-auto-refund-downgrade pending + permanent-fail; (c) refund-status badges render green/amber/red with the correct `aria-label`; (d) loading skeleton + `role="alert"` error surface + 403 branch shows the no-access copy; (e) `compact={true}` hides secondary detail lines + renders timestamps inline; (f) `endedAt == null` renders `Consult in progress`; (g) `reason` appears italicised beneath the headline when present, hidden when null; (h) icon colours match `▲=emerald-600` / `▼=amber-600`. Un-defer trigger: frontend test harness bootstraps.

- [ ] 2026-04-22 — **Plan 09 · Task 55 follow-up — shared modality-timeline helpers extraction:** `<ModalityHistoryTimeline>` inlines `classifyDirection` + `titleCaseModality` + `formatLocalTime` + `capitalize` + `classes` helpers. `classifyModalityDirection` already exists in `backend/src/types/modality-history.ts` and `titleCaseModality` is duplicated in `backend/src/services/modality-change-service.ts`. Extract the frontend-side helpers to `frontend/lib/modality-timeline-helpers.ts` (or `frontend/lib/modality-direction.ts` — pick one home with the Task 50/51/52 modals' existing direction logic) when a second consumer lands. Un-defer trigger: the v1.1 appointment-list-popover consumer ships OR a second card surfaces the same copy (e.g. doctor dashboard modality-change event feed).

- [ ] 2026-04-22 — **Plan 09 · Task 55 follow-up — `compact` prop consumer (appointment-list popover):** `<ModalityHistoryTimeline compact={true}>` renders a one-line-per-entry form suitable for embedding in an appointment-list-row hover popover or the doctor's dashboard bulk-event feed. v1 doesn't mount it anywhere — the prop is implemented + styled but zero consumers today. Wiring target: extend the existing appointment-list-row component to show a `[🔀 N modality changes]` pill beside the recording pill; clicking opens a popover containing `<ModalityHistoryTimeline compact>`. Un-defer trigger: product raises the "at-a-glance modality history in the appointment list" request OR the doctor dashboard v1.1 adds an event feed.

- [ ] 2026-04-22 — **Plan 09 · Task 55 follow-up — Plan 07 Task 32 PDF transcript coordination for modality-switch rows:** Task 55's task doc notes that the PDF transcript (Plan 07 Task 32) should include the modality timeline implicitly via the `modality_switched` system messages Task 53 emits (which already land in `consultation_messages`). Confirm at Task 32 PR time that the PDF renderer does NOT filter out `system_event = 'modality_switched'` rows — the current renderer includes them but a future "clinical messages only" filter would regress the audit trail. Cross-reference with the existing Task 53 inbox item on PDF visual treatment (bold + 🔀 prefix + italic reason). Un-defer trigger: Plan 07 Task 32 PR lands.

- [ ] 2026-04-22 — **Plan 09 · Task 55 follow-up — extend `<ModalityChangeProvider>` (Task 54 inbox) to surface the timeline snapshot:** The Task 54 follow-up `hoist the Realtime channel into a shared <ModalityChangeProvider>` will own a single `consultation-sessions:${sessionId}` channel per room. The post-consult timeline fetches from a different endpoint (`GET /modality-change/history` vs `GET /modality-change/state`) but both share the `consultation_modality_history` INSERT stream. When the provider lands, thread a `useModalityHistory(sessionId)` hook into it so `<ModalityHistoryTimeline>` can subscribe to the same channel (mid-consult live timeline in the doctor dashboard would then reflect transitions in real time). v1's post-consult-only positioning means this is low priority — the page is a read-once surface; Realtime is over-engineered. Un-defer trigger: doctor dashboard asks for a mid-consult live modality feed OR the `<ModalityChangeProvider>` lands and has spare capacity.

- [ ] 2026-04-19 — **Plan 09 · Task 53 follow-up — per-perspective client-side copy for `modality_switched` banners:** v1 persists one neutral 3rd-person body per `consultation_messages` row (`"Patient upgraded to Video. Payment of ₹500 processed."` / `"Doctor downgraded the consult to Voice. Patient refunded ₹50. Reason: …"`). Task 53's spec drafted a 9-variant per-perspective matrix (`"You upgraded to Video"` vs `"Patient upgraded to Video"` vs `"Dr. {name} upgraded the consult…"`). The Plan 06 Task 37 emitter persists a single body column — no per-viewer projection today — so Task 53 stores structured data on `meta` (`initiatedBy`, `billingAction`, `amountPaise`, `reason`, `historyRowId`) and leaves per-perspective copy to a future client-side layer. When Plan 06 Task 38's copy renderer in `frontend/lib/system-message-copy.ts` (currently does nothing for `system_event = 'modality_switched'` — renders the persisted body verbatim) is evolved to dispatch on `system_event + meta`, drop in `renderModalitySwitchedCopy(meta, perspective)` matching Task 53 spec §"Copy variants by perspective × initiatedBy × billingAction". The backend body is kept as a fallback for consumers that don't read `meta` (Plan 07 Task 32 PDF, Plan 10 AI pipeline). Un-defer trigger: Task 38's system-message rendering layer ships OR user feedback surfaces per-perspective copy as a quality gap (currently both parties see the same neutral string, which is functional but not personal). Frontend test-harness inbox follow-up covers the test matrix if per-perspective copy lands.

- [ ] 2026-04-19 — **Plan 09 · Task 53 follow-up — reconsider `refund_completed` sibling event (success signal):** Task 49's worker emits `modality_refund_processing` once at retry-attempt-1 (any outcome) and `modality_refund_failed` on 24h permanent-stuck. There is no success-signal banner — i.e. the patient sees "refund of ₹X is processing — expect within 3 business days", then nothing unless it fails permanently. Task 53's spec originally proposed a `refund_completed` event fired by the worker on `sync_success` so patients explicitly see the refund landed. Doctrine chosen for v1: the Razorpay bank-statement reconciliation is the authoritative signal; emitting two system messages for the same refund ("processing" + "completed") pollutes the chat without carrying new information (the patient's bank app tells them when the refund lands). Revisit if ops reports patient queries of "did my refund go through" after the initial banner — at that point, emit a `refund_completed` event at worker `sync_success` time, dedup on `${historyRowId}::refund_completed`. Un-defer trigger: ops surfaces patient confusion around refund status OR Plan 10 AI pipeline requests an explicit completion signal.

- [ ] 2026-04-19 — **Plan 09 · Task 53 follow-up — Plan 07 Task 32 PDF transcript visual treatment for modality-switch rows:** Task 53's spec noted "Task 32 PDF needs a visual treatment for modality-switch messages (bold + 🔀 prefix, reason italicized)". v1 renders the neutral prose inline with the rest of the chat transcript — readable but not visually distinct. Coordination with Plan 07 Task 32 owner at PR time: add a conditional renderer block for `system_event = 'modality_switched'` that:  (a) bolds the first sentence (the transition + billing summary); (b) italicizes the `Reason: …` suffix if present; (c) optional 🔀 prefix emoji (depends on Task 32's font stack supporting the symbol). Un-defer trigger: Plan 07 Task 32 kicks off OR a legal-review pass asks for visually-distinct audit trail items in the PDF.

- [ ] 2026-04-19 — **Plan 09 · Task 54 follow-up — direct mounting of `<ModalityChangeLauncher>` inside `<TextConsultRoom>` / `<VoiceConsultRoom>` / `<VideoRoom>`:** Task 54 v1 mounts the launcher at the `<LiveConsultPanel modalitySwitchSlot>` layer (doctor side only) rather than inside each room component as the spec lists. Rationale: rooms have heterogeneous prop surfaces (e.g. `<VideoRoom>` only knows `accessToken + roomName`, not `sessionId` / Supabase JWT / user role — the launcher needs all three) and passing that tuple through each room doubles prop counts for a feature that already has a clean host-side slot available. Follow-up: either (a) thread `sessionId + supabaseJwt + userRole + pricing` as explicit optional props into each room (uniform shape shared via a small `RoomModalityContext` type) and render the launcher inline in each room's controls bar, or (b) keep v1's slot-based mounting and document the deferral as intentional. Decision point at PR time — (b) is likely the right long-run answer because rooms stay stateless + testable in isolation. Un-defer trigger: Plan 06 Task 38's companion-panel variant ships (the video controls bar acquires a persistent "change modality" row) OR UX feedback shows users missing the launcher on direct room pages (patient-side `/c/*` routes without `<LiveConsultPanel>`).

- [ ] 2026-04-19 — **Plan 09 · Task 54 follow-up — patient-side `<ModalityChangeLauncher>` integration into `/c/text/[sessionId]`, `/c/voice/[sessionId]`, and `/consult/join`:** Patient pages don't currently mount `<LiveConsultPanel>` — they wire their room components directly. Task 54 therefore has zero patient-side coverage in v1. Minimum set: (a) decide whether patient pages adopt `<LiveConsultPanel>` (preferred — unifies banner + recording + launcher slots with the doctor side) or whether the launcher is mounted inline on each page; (b) thread the patient's Supabase JWT + `sessionId` + `userRole='patient'` + `doctorDisplayName` into the launcher; (c) verify `GET /modality-change/state` RLS accepts the patient token (it already does per Migration 076 `modality_participants_select` policy, so this is just wiring); (d) for `/consult/join` specifically, the launcher must stay hidden until the token exchange completes. Un-defer trigger: first patient beta that exercises mid-consult modality switching OR Plan 09 go-live pre-flight.

- [ ] 2026-04-19 — **Plan 09 · Task 54 follow-up — thread `pricing` prop into `<ModalityChangeLauncher>`:** Launcher accepts an optional `pricing: { text, voice, video }` for the `▲ Voice — normally ₹X more` / `▼ Text — auto-refund ₹X` copy + to compute `refundAmountPaise` for the doctor-downgrade modal. v1 integrations omit the prop (copy degrades gracefully to `Voice` without the ₹ suffix; doctor-downgrade sends `refundAmountPaise=0`). Fix depends on the existing Task 51 inbox item "extend `GET /modality-change/state` to include a `pricing` block" — once that block is exposed, the launcher can fetch pricing alongside state in a single call and hoist the result into the modal props automatically. Un-defer trigger: Task 51 pricing-block follow-up lands OR product decides to show exact fee copy from day-one (then an inline `fetchModalityPricing()` call is a viable short-term bridge).

- [ ] 2026-04-19 — **Plan 09 · Task 54 follow-up — hoist the Realtime channel into a shared `<ModalityChangeProvider>`:** `<ModalityChangeLauncher>` subscribes to `postgres_changes` on `modality_change_pending_requests` (INSERT + UPDATE) and `consultation_modality_history` (INSERT) on a channel named `modality-launcher:${sessionId}`. The auto-open hooks (`useDoctorPendingUpgradeApproval`, `usePatientPendingUpgradeConsent`) + the Task 50 modal's own FSM each open *their own* channel against effectively the same filters. With up to 4 concurrent channels per session (launcher + launcher-initiated modal + auto-open hook + auto-open modal), we are close to (but not above) the Supabase Realtime free-tier per-client quota and we waste WebSocket frames on duplicate row deliveries. Solution: wrap the room surface in a React context provider that owns **one** channel per sessionId + exposes a `useModalityChangeSnapshot()` hook returning `{ state, activePending, onAppliedTransition$ }`. Consumers (launcher + modals + hooks) all read from the provider. Un-defer trigger: metrics show churn in production OR a fifth consumer lands (Task 55 timeline will be the fifth).

- [ ] 2026-04-19 — **Plan 09 · Task 54 follow-up — component tests for `<ModalityChangeLauncher>`:** Deferred per the frontend-test-harness inbox note. Minimum set when the harness bootstraps: render in all 3×2 (current modality × role) combinations and assert the visible menu items; `upgradeCount=1` → upgrade items `disabled` with the cap tooltip; `downgradeCount=1` → downgrade items `disabled`; `activePendingRequest` truthy → outer button `aria-disabled="true"` with role-aware tooltip; click upgrade → correct click-launched modal mounts (Task 50 / 51 / 52 shape); Realtime `consultation_modality_history` INSERT → `onTransitionApplied` fires with the new modality; Escape + outside-click close the popover; `currentModality='text'` → downgrade section hidden; `currentModality='video'` → upgrade section hidden; aria landmarks (`menu` / `menuitem`) visible via RTL queries. Un-defer trigger: frontend test harness bootstraps.

- [ ] 2026-04-19 — **Plan 09 · Task 50 follow-up — frontend component tests for `<ModalityUpgradeRequestModal>` + `useModalityUpgradeFSM`:** Task 50 ships without component tests per the repo-wide deferral on the frontend test harness. The FSM's `reducer` is exported as a pure function in `frontend/hooks/useModalityUpgradeFSM.ts` so it's trivially testable once the harness lands. Minimum set: (a) reducer transitions for each of the 10 state × action pairs (idle→submitting→awaiting_approval; awaiting_approval × APPROVED_PAID/APPROVED_FREE/DECLINED/TIMED_OUT/PROVIDER_FAILURE; checkout_ready→checkout_opened→applying_transition/idle; applying_transition→applied + 15s GENERIC_ERROR); (b) reducer guards — `APPROVED_PAID` ignored when `approvalRequestId` mismatches, `APPLIED` ignored outside `applying_transition`/`free_upgrade_approved`, terminal states don't regress on late broadcasts; (c) modal integration — re-hydration on mount fetches `GET /state` + direct Supabase row lookup, pending-row UPDATE with `response='approved_paid'` but `razorpay_order_id=null` does NOT advance, next UPDATE (with ids populated) advances; (d) Razorpay SDK mock — `openCheckout` dispatches `OPEN_CHECKOUT`, SDK `handler` → `CHECKOUT_SUCCEEDED`, `ondismiss` → `CHECKOUT_DISMISSED`; (e) countdown sync from `expiresAt` (not local 90s); (f) `onAppliedTransition` fired exactly once; (g) ESC closes only in safe-dismiss states (`idle`/`declined`/`timeout`/`error`/`applied`/`free_upgrade_approved`); (h) aria-live announcements on every state transition; (i) `prefers-reduced-motion` suppresses `.animate-pulse` / `.animate-fade-in`. Un-defer trigger: frontend test harness bootstraps (React Testing Library + jest-dom + next-router mock).

- [ ] 2026-04-19 — **Plan 09 · Task 50 follow-up — orphan Razorpay order cleanup on checkout dismiss:** When the patient hits `checkout_ready` and dismisses the Razorpay modal without paying, the FSM returns to `idle` and leaves `modality_change_pending_requests` at `response='approved_paid'` with `razorpay_order_id` stamped. Razorpay auto-expires unused orders after ~15 min, but there is currently no webhook-driven cleanup that stamps `response='checkout_cancelled'` on the pending row — so the row sits in an intermediate state until the backend modality-pending-timeout worker reaps it at `expires_at` (which for patient-upgrade is 90s from request, long past). Fix: (a) subscribe `webhook-worker.ts` to Razorpay `order.expired` / `payment.failed` events, (b) look up the pending row by `razorpay_order_id`, (c) `UPDATE modality_change_pending_requests SET response='checkout_cancelled', responded_at=now() WHERE razorpay_order_id=$1 AND response='approved_paid'`. Alternative: extend the patient modal with a POST endpoint that explicitly writes `checkout_cancelled` on dismiss — simpler but trust-the-client; the webhook route is defence-in-depth. Un-defer trigger: first production decline-then-dismiss reported OR Task 52 PaymentOps dashboard surfaces stuck pending rows.

- [ ] 2026-04-19 — **Plan 09 · Task 50 follow-up — thread `newAccessToken` through the applied-transition broadcast:** `ModalityUpgradeRequestModal`'s `onAppliedTransition` fires with `{ toModality }` only. Task 50 spec wants `{ toModality, newAccessToken? }` so the launcher (Task 54) can remount the next room without a fresh token-mint round-trip. Blocker: Task 48's executor surfaces `newAccessToken` synchronously on text→voice / text→video, but Task 47's commit-side code does NOT currently rebroadcast it (see existing Task 48 inbox item "patient-token Realtime rebroadcast for text→voice/video"). Landing order: ship Task 48's rebroadcast first, then extend the frontend Realtime listener here (`useModalityUpgradeFSM`'s `postgres_changes` on `consultation_modality_history` will need to be augmented with a `broadcast` channel listener since the token cannot live in a DB column — it's per-participant JWT). Un-defer trigger: Task 48 rebroadcast lands OR Task 54 launcher implementation blocks on the token-swap path.

- [ ] 2026-04-19 — **Plan 09 · Task 50 follow-up — extract shared `formatInr` + `formatCountdown` utilities:** `frontend/components/consultation/ModalityUpgradeRequestModal.tsx` defines inline `INR_FORMATTER` + `formatInr(paise)` + `formatCountdown(totalSec)` helpers. Tasks 51 (doctor modals) and 52 (patient self-downgrade) will re-implement the same shapes. Extract to `frontend/lib/format-inr.ts` + `frontend/lib/format-countdown.ts` once any second consumer lands (deferred now to keep Task 50 diff minimal + to avoid speculating on the exact signatures Task 51/52 need). Un-defer trigger: Task 51 or Task 52 begins implementation.

- [ ] 2026-04-19 — **Plan 09 · Task 49 follow-up — modality pricing: per-service resolution when session carries `service_key`:** `backend/src/utils/modality-pricing.ts` v1 collapses multi-service `doctor_settings.service_offerings_json` catalogs down to the **MAX enabled price per modality** across all services. Rationale: `SessionRecord` does not currently expose the `service_key` that the appointment was booked under, so the helper cannot pick the exact service row to price against. MAX is conservative on both sides (upgrades never undercharge, refunds never over-refund) but it will over-charge a ₹100-text-service patient trying to upgrade to voice if a sibling ₹500-premium service on the same doctor exists. Fix: thread `service_key` through `consultation_sessions` → `SessionRecord` → `getModalityFeesForDoctor({ serviceKey })` and take the exact service row; fall back to the MAX-across-services rule only when the session is pre-Plan-09 (no `service_key` column yet). Un-defer trigger: Plan 03's service_catalog rollout threads `service_key` into session creation (Plan 04 booking flow may already carry it — audit first), OR a pricing-correctness bug lands in production where a multi-service doctor is reported as overcharging on upgrades.

- [ ] 2026-04-19 — **Plan 09 · Task 49 follow-up — admin `admin_payment_alerts` read + acknowledge endpoint + PaymentOps dashboard card:** Migration 077 ships the `admin_payment_alerts` table + the refund retry worker writes `refund_stuck_24h` rows, but there is no admin-facing surface today. Deferred to keep the Task 49 PR focused on the payment-critical worker + idempotency work. Minimum set: `GET /admin/payment-alerts?acknowledged=false` (role-gated, returns unacked alerts with the history-row + session + amount snippet); `POST /admin/payment-alerts/:id/acknowledge` (stamps `acknowledged_at + acknowledged_by`); optional `POST /admin/payment-alerts/:id/escalate` that writes a Slack/PagerDuty ping via the existing notification channel. Dashboard card: list unacked alerts sorted by oldest `created_at`; clicking an alert opens a detail view with the raw refund failure reason + `related_entity_id` → `consultation_modality_history` deep-link so ops can manually process the refund via Razorpay dashboard + then acknowledge. Un-defer trigger: first `refund_stuck_24h` alert fires in production OR Task 52 (PaymentOps dashboard) kicks off.

- [ ] 2026-04-19 — **Plan 09 · Task 49 follow-up — live Razorpay sandbox integration tests:** `backend/tests/integration/modality-billing-against-razorpay-sandbox.test.ts` ships with 6 `describe.skip`-gated tests (gate: `RAZORPAY_SANDBOX_TEST=1`) covering Orders API happy path + idempotency, Refunds API happy path + `Idempotency-Key` dedup + permanent-fail classification ("fully refunded" / "no such payment") + DB stamp round-trip. Bodies are placeholders today; the real bodies require (a) a disposable Razorpay sandbox key + (b) a pre-captured sandbox payment id fixture to refund against + (c) a teardown that issues compensating refunds on any orders that weren't paid during the run. Bundle with the Plan 09 Task 47 / 48 + Plan 08 Task 44 follow-ups under one harness: whichever lands first authors `sandbox-fixtures.ts` covering both `TWILIO_SANDBOX_TEST=1` and `RAZORPAY_SANDBOX_TEST=1`. Un-defer trigger: Plan 09 go-live pre-flight (must run against sandbox before production deploy) OR the shared sandbox jest harness lands.

- [ ] 2026-04-19 — **Plan 09 · Task 49 follow-up — observability metrics + alerts for mid-consult Razorpay ops:** Code-complete PR ships with structured logs (correlation-id threaded through every billing call + every worker tick) but does NOT yet emit metrics. Add: counter `modality_billing_orders_created_total{from,to,result=ok|idempotent_hit|razorpay_error}`; counter `modality_billing_refunds_total{reason,result=sync_success|queued|permanent_fail}`; counter `modality_billing_refund_retries_total{attempt=1..6,result=success|requeued|stuck}` (cardinality bounded by attempt × 3 outcomes = 18); histogram `modality_billing_razorpay_latency_ms{op=orders_create|payments_refund}`; counter `modality_billing_admin_alerts_written_total{alert_kind}`; **critical alert:** `sum(rate(modality_billing_admin_alerts_written_total{alert_kind='refund_stuck_24h'}[1h])) > 0` → someone's refund is permanently stuck; immediate ops page. Un-defer trigger: Plan 2.x alerting pipe lands (same as the Task 47/48 observability follow-ups — bundle them together so metrics + alerts land in one pass).

- [ ] 2026-04-19 — **Plan 09 · Task 49 follow-up — cron scheduler for `/cron/modality-refund-retry`:** route registered (`backend/src/routes/cron.ts`, `verifyCronAuth`-gated, 15 min cadence) + worker is unit-tested (`runModalityRefundRetryJob` 11/11 green), but the actual 15-minute trigger is deployment-layer wiring (same pattern as `/cron/modality-pending-timeout` and `/cron/video-escalation-timeout`). Share the same scheduler entry point across all `/cron/*` routes once Plan 2.x picks the scheduler (Render cron / Supabase scheduled edge function / GitHub Actions cron). Un-defer trigger: first deploy of Plan 09 to any environment.

- [ ] 2026-04-19 — **Plan 09 · Task 49 follow-up — remove deprecated `BillingNotImplementedError` alias:** `backend/src/services/modality-billing-service.ts` retains `BillingNotImplementedError` as a deprecated alias so Task 47's state-machine test fixtures (which still import it from the stub era) keep compiling. Task 47 tests should be audited + migrated to `BillingNotConfiguredError`, then the alias + the deprecation comment deleted. Un-defer trigger: next time we touch Task 47's state-machine fixtures (or when cruft cleanup gets a dedicated sweep).

- [ ] 2026-04-19 — **Plan 09 · Task 48 follow-up — consolidate direction helpers onto `classifyModalityDirection`:** Task 48's spec requested a new `backend/src/utils/modality-order.ts` exporting `directionOf(from, to)` + the `text=1, voice=2, video=3` numeric mapping. Task 46 already shipped the same logic as `classifyModalityDirection` in `backend/src/types/modality-history.ts`; creating a second copy would fork the invariant that the `modality_history_reason_required` CHECK depends on (must stay aligned with the `consultation_modality` enum order). Task 48 skipped `modality-order.ts` and reused the existing helper through the state-machine's direction field. Follow-up: audit callers of `classifyModalityDirection` + any future "numeric rank" needs (Task 55 timeline is the most likely consumer); if a second callsite wants a numeric order, add `modalityRank` alongside `classifyModalityDirection` rather than creating a new file. Un-defer trigger: Task 55 (timeline) or any task that asks for the numeric mapping lands.

- [ ] 2026-04-19 — **Plan 09 · Task 48 follow-up — patient-token Realtime rebroadcast for text→voice/video:** The executor returns `newPatientAccessToken` synchronously on text→voice and text→video. Task 48's spec additionally mandates a Supabase Realtime broadcast on `consultation-sessions:{sessionId}:modality-change` with `{ kind: 'transition_applied', toModality, newAccessToken, newProviderSessionId }` so the patient's `<TextConsultRoom>` can swap-mount to `<VoiceConsultRoom>` / `<VideoRoom>` without a page reload. Task 47's commit currently receives the result but does NOT rebroadcast — the state machine's HTTP response carries the tokens only to the caller (doctor). Wiring target: `executeAndCommitTransition` (after the successful counter UPDATE) publishes a structured Realtime event via `supabase.channel(...).send({ type: 'broadcast', event: 'modality-change', payload: ... })`. Security: channel is RLS-protected to session participants via migration 052's custom-claim policy; adding an HMAC handshake is belt-and-suspenders that complicates the client without adding real security (documented as rejected in task-48 Notes §Access-token minting). Un-defer trigger: Task 51 (client-side modality-change launcher) begins implementation and needs the event stream to drive the swap-mount.

- [ ] 2026-04-19 — **Plan 09 · Task 48 follow-up — live Twilio sandbox integration tests:** `backend/tests/integration/modality-transition-executor-against-sandbox.test.ts` ships with 7 `describe.skip`-gated tests (gate: `TWILIO_SANDBOX_TEST=1`) that document the full 6-branch matrix + a teardown-orphan-check test. All bodies are placeholder assertions; the real bodies require a disposable Twilio project + a fixtures harness (`sandbox-fixtures.ts`) to materialise a pre-flight consultation session per run and clean up all rooms post-run. Same gate-lift pattern as the Plan 08 Task 45 / Plan 09 Task 46 / Task 47 inbox follow-ups — they all wait on the same `TWILIO_SANDBOX_TEST=1` + `RAZORPAY_SANDBOX_TEST=1` harness. Un-defer trigger: the Twilio sandbox jest harness lands (can be packaged alongside Plan 09's go-live pre-flight, which must run against sandbox before production deploy anyway).

- [ ] 2026-04-19 — **Plan 09 · Task 48 follow-up — `transitionLatencyMs` histogram wiring:** The executor returns `transitionLatencyMs` on every successful branch and Task 47 threads it through structured logs, but neither layer emits the `modality_change_transition_latency_ms{transition}` histogram mandated by Task 48's Observability section (SLO targets: voice↔video <500ms p95; text→voice/video <3000ms p95; voice/video→text <1500ms p95). This overlaps with the broader Plan 09 observability follow-up filed under Task 47; merge the two when the Plan 2.x alerting pipe lands so the histogram + alerts (`sustained p95 breach`) ship together. Concrete action: add a `transition` label derived from `${from}->${to}` (6 cardinality) to keep the histogram bounded. Un-defer trigger: Plan 2.x alerting pipe lands (same as the Task 47 observability follow-up).

- [ ] 2026-04-19 — **Plan 09 · Task 47 follow-up — advisory-lock RPC + transactional commit:** v1 compresses the three-layer concurrency model (`pg_advisory_xact_lock` + `SELECT FOR UPDATE` + CHECK) down to two layers (atomic counter `UPDATE … WHERE counter = 0` + CHECK + pending-row `response IS NULL` predicate) because Supabase JS client cannot open user-managed transactions or call advisory locks without a SECURITY DEFINER RPC. Tiny residual race: (a) executor succeeds, (b) history INSERT succeeds, (c) counter UPDATE returns 0 rows (concurrent second writer won) — history row orphaned, `provider_failure` returned to the caller. State-machine logs loudly (`'counter UPDATE raced … history row orphaned'`) and the ops observability task (below) surfaces the count. Fix: ship `public.call_modality_advisory_lock(session_id UUID)` SECURITY DEFINER RPC that wraps `pg_advisory_xact_lock(hashtext('modality:' || session_id::text))` and call it at the top of `executeAndCommitTransition` inside an explicit transaction block (also RPC). Un-defer trigger: observability shows ≥1 orphan/week in production OR before Decision 11's billing-critical threshold is crossed.

- [ ] 2026-04-19 — **Plan 09 · Task 47 follow-up — live Twilio + Razorpay sandbox integration tests for the state machine:** the task's integration-test acceptance criteria (patient paid upgrade text→voice end-to-end; doctor downgrade video→text w/ refund; rate-limit deadlock; executor-failure-after-pay compensating refund; two-tab concurrent `POST /modality-change/request`) are deferred in the code-complete PR to match the Plan 08 + Plan 09 Task 46 convention. Unit-layer covers the branch matrix (39 assertions); sandbox tests pin the end-to-end wiring. Un-defer trigger: `TWILIO_SANDBOX_TEST=1` + `RAZORPAY_SANDBOX_TEST=1` jest harness lands, OR first production deploy of Plan 09 (must run against sandbox before go-live).

- [ ] 2026-04-19 — **Plan 09 · Task 47 follow-up — observability metrics + alerts for the state machine:** the state machine is v1's single-most-critical state machine (Decision 11 LOCKED, double-billing + stuck-rooms failure modes documented in the task). Code-complete PR ships with structured logs (correlation-id threaded through every log line) but does NOT yet emit metrics. Add: counter `modality_change_requests_total{initiator,direction,result=pending|applied|rejected,reason?}` (cardinality bounded by the ~10 reject-reason enum); counter `modality_change_pending_timeouts_total{initiator}`; counter `modality_change_provider_failures_total{phase=executor|history|counter|refund}`; counter `modality_change_compensating_refunds_fired_total{trigger=executor_failure|modality_drift}`; histogram `modality_change_duration_ms{handler}` from entry to terminal result; **critical alert:** `sum(rate(modality_change_provider_failures_total{phase='counter'}[15m])) > 0` → orphan history row; immediate ops page (see advisory-lock follow-up above for the structural fix). Un-defer trigger: Plan 2.x alerting pipe lands.

- [ ] 2026-04-19 — **Plan 09 · Task 47 follow-up — Razorpay mid-consult webhook wiring into `webhook-worker.ts`:** the state machine exports `handleModalityChangePaymentCapturedHook(input: ModalityPaymentCapturedInput)` from `backend/src/controllers/modality-change-controller.ts` but the existing `backend/src/workers/webhook-worker.ts` does NOT yet dispatch into it — the hook sits idle until Task 49 lands. The dispatch requires Razorpay signature verification (existing machinery) + a branch that distinguishes "original booking payment" (existing `processPaymentSuccess` path) from "mid-consult upgrade payment" (new `handleModalityChangePaymentCapturedHook` path). Disambiguation vector: presence of a row in `modality_change_pending_requests` for the webhook's `razorpay_order_id` (already indexed via `idx_modality_pending_razorpay_order`). Un-defer trigger: Task 49 (which owns Razorpay + webhook wiring end-to-end) begins implementation.

- [ ] 2026-04-19 — **Plan 09 · Task 47 follow-up — cron scheduler for `/cron/modality-pending-timeout`:** route registered (`backend/src/routes/cron.ts`) + worker is unit-tested (`runModalityPendingTimeoutJob`), but the actual 5-second trigger is deployment-layer wiring (Render cron / Supabase scheduled edge function / GitHub Actions cron — whichever Plan 2.x adopts) and lives outside this repo. Share the same scheduler as `/cron/video-escalation-timeout` once it lands (both run on 5s cadence; no reason to spawn a dedicated scheduler). Un-defer trigger: first deploy of Plan 09 to any environment.

- [ ] 2026-04-19 — **Plan 09 · Task 46 follow-up — live-Postgres integration tests for Migration 075:** the task's Integration-test acceptance criteria (10 INSERT round-trip cases against the real schema) are deferred to an inbox item matching the Plan 08 Task 45 convention, because the repo has no live-Postgres CI harness today (`backend/tests/integration/` exists but only holds the legacy `test-webhook-verification.ts` script; no jest `*.test.ts` files). Content-sanity + query-shape layers cover the contract pre-merge. Minimum set once a harness lands: (a) four row variants one-per `billing_action`, each satisfying `modality_history_billing_shape`; (b) `paid_upgrade` without `amount_paise` rejected by CHECK; (c) `auto_refund_downgrade` inserted with `razorpay_refund_id = NULL` accepted, then UPDATE `razorpay_refund_id = 'rfnd_…'` accepted; (d) doctor-initiated row without `reason` rejected by `modality_history_reason_required`; (e) patient-initiated downgrade (voice → text) without reason rejected; (f) patient-initiated upgrade (text → voice) without reason accepted; (g) `from_modality = to_modality` rejected by `modality_history_from_to_differ`; (h) existing `consultation_sessions` row post-migration has `current_modality = modality` (backfill pin); (i) `UPDATE consultation_sessions SET upgrade_count = 2` rejected by rate-limit CHECK (+ downgrade twin); (j) a second doctor's RLS SELECT returns empty. Un-defer trigger: repo adopts a Supabase-dev-project jest harness (or similar) that Plans 08 / 09 share.

- [ ] 2026-04-19 — **Plan 09 · Task 46 follow-up — manual Supabase dev-project smoke test:** four assertions from the task spec that need a live DB: `SELECT enum_range(NULL::modality_billing_action)` returns `{paid_upgrade,free_upgrade,no_refund_downgrade,auto_refund_downgrade}` in exactly that order; `SELECT enum_range(NULL::modality_initiator)` returns `{patient,doctor}`; `SELECT enum_range(NULL::consultation_modality)` returns `{text,voice,video}` (the load-bearing ordering the `modality_history_reason_required` CHECK depends on — **must re-verify before any Plan 09 downstream task merges**); `UPDATE consultation_sessions SET upgrade_count = 5` rejected by CHECK; backfill confirmed across every pre-migration row. If the `consultation_modality` ordering differs from `text < voice < video`, the CHECK must be rewritten to enumerated pairs `(from, to) IN (('video','voice'), ('video','text'), ('voice','text'))` before Task 47 ships. Un-defer trigger: Migration 075 applies in any dev environment.

- [ ] 2026-04-19 — **Plan 09 · Task 46 follow-up — observability pre-provisioning for Task 47 state-machine:** the content-sanity layer landed Migration 075 + query helpers but did NOT pre-provision metrics / structured-log shapes for Task 47's state-machine (it writes every modality-history row + updates `consultation_sessions` counters transactionally). Plan 09 ships cleaner if Task 47 lands with metrics already pre-agreed. Propose: counter `consult_modality_transition_total{direction=upgrade|downgrade, billing_action=…, initiator=patient|doctor}`; counter `consult_modality_transition_rejected_total{reason=rate_limited|reason_missing|transition_illegal}`; histogram `consult_modality_transition_latency_ms`; structured log per transition with `correlationId` threading the history row id + session id + Razorpay ids. Alert: `sum(rate(consult_modality_transition_rejected_total{reason='rate_limited'}[5m])) > 5` → possible spam / UX bug (rate-limit should be greyed-out in UI, not server-enforced). Un-defer trigger: Task 47 kickoff (ship the metric names in Task 46's post-commit doc so Task 47 uses the agreed shapes).

- [ ] 2026-04-19 — **Plan 08 · Task 44 follow-up — unit + integration + end-to-end tests for `video-replay-otp-service` + video branch of `recording-access-service`:** Deferred from the Task 44 acceptance criteria to keep the shipped PR focused on behavioural code. Minimum set before merge: (a) `isVideoOtpRequired` — no-row → `required:true`, row inside 30d → `required:false + lastVerifiedAt`, row at 30d boundary (DST-aware; e.g. `verified_at = 2026-03-14T00:00:00Z`, replay at `2026-04-13T00:00:00Z` must be inside), row > 30d → `required:true`. (b) `sendVideoReplayOtp` — happy path inserts a row with hashed code + salt and calls `twilio-sms-service#sendSms` exactly once with the expected copy; `already_verified` short-circuits without SMS; rate-limit 4th call in an hour → `VideoOtpRateLimitError` with `retry_after_seconds` populated, row NOT inserted; `sendSms` failure → row marked consumed so a brute-force guess can't revive it + `VideoOtpSmsUnavailableError` propagates; `patients.phone` missing → `no_patient_phone_on_file`. (c) `verifyVideoReplayOtp` — matching code upserts `video_otp_window` + stamps `consumed_at`; wrong code → `attempt_count++`, returns `wrong_code`; 5th wrong → `too_many_attempts`, row locked; expired row → `expired`; consumed row → `expired` (idempotent re-verify). (d) `recording-access-service` video branch — patient without OTP → `VideoOtpRequiredError` thrown with `lastVerifiedAt` set to the freshest row's timestamp (null when none), NO denial audit written (UX gate, not access decision); patient with OTP → mints URL, writes `recording_access_audit` with `access_type='full_video'`, fires `notifyReplayWatcher({ artifactKind:'video' })`; doctor caller skips OTP gate entirely; empty `videoCompositions` → `no_video_artifact` (404 at controller). (e) End-to-end integration (`video-replay-end-to-end.test.ts`, skipped unless `TWILIO_SANDBOX_TEST=1`) — state-fetch → send → verify → mint video URL → audit row pinned → doctor dashboard event pinned; second replay same hour skips OTP; 31 days later re-requires OTP. Frontend tests (pending test-harness bootstrap): `VideoReplayWarningModal` focus-trap + Escape + Cancel callbacks; `VideoReplayOtpModal` 6-digit input auto-advance + paste-split + 30s resend cooldown + 5-min expiry countdown + wrong/expired/too-many-attempts/rate-limited/SMS-unavailable/no-phone-on-file error-state matrix; `RecordingReplayPlayer` Show-video toggle gated by `hasVideo`, TTL re-mint preserves mode, doctor callers skip OTP modal.

- [ ] 2026-04-19 — **Plan 08 · Task 44 follow-up — observability metrics + alerts for video-replay OTP:** Code-complete PR ships without emitting the metrics listed in the acceptance criteria. Add: counter `video_replay_otp_sent_total{}`, counter `video_replay_otp_verified_total{reason=verified|wrong_code|too_many_attempts|expired|rate_limited|sms_unavailable}`, counter `video_replay_access_total{access_type=audio_only|full_video}` incremented on each successful mint, histogram `video_replay_audit_write_latency_ms`. Structured log on every OTP send / verify / video access with `correlationId` threading (some of this exists; audit the log callsites for completeness). Alert: `sum(rate(video_replay_otp_verified_total{reason='too_many_attempts'}[1h])) > 10` → possible credential-stuffing / abuse; routes through the same Plan 2.x alerting pipe tracked elsewhere. Un-defer trigger: first production deploy of Task 44 OR the Plan 2.x alerting pipe lands (the metrics need somewhere to land before they're worth emitting).

- [ ] 2026-04-19 — **Plan 08 · Task 44 follow-up — manual smoke test of the full video-replay friction loop:** Once a Twilio sandbox composition exists for a completed consult + an Indian-carrier SMS-capable Twilio number is wired in, walk the patient through: open replay page → audio loads by default → `[ ] Show video` visible → click → warning modal → continue → OTP modal (first time in 30d window) → SMS arrives within 5–60s → enter code → verify → video element swaps in with corner watermark `{name} · {timestamp}` → doctor dashboard shows `patient_replayed_video` row with 🎥 prefix in the feed → DM to doctor carries 🎥 prefix. Repeat replay within same hour → skips OTP, goes straight to video. 31d later → OTP required again. Specifically validate: (1) fullscreen doesn't strip the watermark overlay (z-index correctness), (2) `controlsList="nodownload"` honored in Chrome/Edge/Firefox/Safari (Safari sometimes ignores), (3) right-click context menu doesn't expose "Save video as…", (4) TTL re-mint at 14-min mark preserves video mode (does NOT silently drop to audio).

- [ ] 2026-04-19 — **Plan 08 · Task 44 follow-up — doctor-side video-replay friction (if Legal wants it):** v1 scoped OTP gate to **patient** callers only — doctors have separate compliance obligations (signed-in session, NDA, role-scoped dashboard) and an OTP every 30 days on their own patient's recordings would be workflow-hostile. Documented in Task 44's Out-of-Scope list. Un-defer trigger: Legal flags doctor-side replay as a PHI-exposure risk requiring explicit re-authentication (e.g. after jurisdictional audit, or after a data-breach incident that implicates doctor credentials). Implementation would be ~trivial once triggered — reuse `isVideoOtpRequired` against the doctor's phone, but likely swap SMS for email+2FA to match doctor onboarding.

- [ ] 2026-04-19 — **Plan 08 · Task 42 follow-up — unit + integration tests for `patientRevokeVideoMidCall` + `<VideoRecordingIndicator>`:** Deferred from the acceptance criteria to keep the Task 42 PR focused; bundles with the existing Task 41 test harness note so both land in one pass. Minimum set before merge: (a) `patientRevokeVideoMidCall` authZ — caller ≠ session patient returns 403 without writing any rows or making Twilio calls; (b) idempotent — when `getCurrentRecordingMode` returns `audio_only`, the function returns `{ status: 'already_audio_only' }` without UPDATE / audit / system-message / dashboard-event side effects; (c) happy path — atomic UPDATE stamps `revoked_at` + `revoke_reason`, Twilio flip called once, `consultation_recording_audit { action: 'patient_revoked_video_mid_session' }` written, `emitVideoRecordingStopped` called with `(byRole='patient', reason='patient_revoked')`, `insertDashboardEvent` called; (d) atomic race — two concurrent revoke calls both return success but only one writes the audit + system-message (the losing UPDATE gets 0 rows back); (e) Twilio failure — `revertToAudioOnlyRecording` throws → `revoked_at` row stays (Twilio rollback state tolerated; next `getCurrentRecordingMode` read re-synchronises) AND no dashboard event fires; (f) missing `doctor_dashboard_events` table / CHECK — caught, logged at warn, revoke still succeeds (graceful degrade); (g) rate-limit interaction — a revoked allow row counts as one attempt + the next request's cooldown starts from the ORIGINAL `requested_at` (regression pin for Decision 10 ledger). Frontend tests (pending test-harness bootstrap): `<VideoRecordingIndicator>` visibility matrix (doctor × patient × active × inactive × prefers-reduced-motion), `[Stop]` tap opens tooltip, `[Yes, stop]` calls `revokeVideoRecording` and disables CTA during async, tap-outside / Esc / Cancel dismiss without calling, `aria-live` announces start/stop politely (debounced).

- [ ] 2026-04-19 — **Plan 08 · Task 42 follow-up — `consult_started_at` payload enrichment on `doctor_dashboard_events { event_kind: 'patient_revoked_video_mid_session' }`:** v1 passes `consult_started_at: null` because `SessionRecord` (returned by `findSessionById`) doesn't expose `actualStartedAt` and adding a second session fetch just for this field would double the revoke's DB round-trips. Doctor dashboard renders the event without it for now (falls back to `createdAt` of the event row — "X stopped recording 2 min ago" reads fine without the consult-start context). Un-defer trigger: the first time a doctor surfaces a "started X min into the consult" UX requirement, OR `SessionRecord` gains `actualStartedAt` as part of unrelated work (then a trivial passthrough swap). Preferred fix: widen `SessionRecord` to include `actualStartedAt` (read-only, already on the underlying row) so every callsite gets the field for free — cheaper than a second fetch and matches how `findSessionById`'s cost is already paid.

- [ ] 2026-04-19 — **Plan 08 · Task 42 follow-up — smoke-test the mid-call revoke loop end-to-end:** Once a Twilio sandbox video room + browser-auth Supabase sessions are available on both doctor + patient sides, walk through the full flow documented in the task doc (doctor requests escalation → patient allows → `<VideoRecordingIndicator>` appears on both sides → patient taps `[Stop]` + confirms → Twilio flip to audio-only → indicator fades out on both sides ~500ms later → doctor's `<VideoEscalationButton>` re-enters cooldown with the original `requested_at` as the anchor → doctor dashboard event feed gets a new row → next escalation attempt is rate-limited until 5 min from original request). Specifically validate the doctor-side Postgres-changes subscription actually fires the UPDATE event (RLS allows it per Migration 070) and the indicator fade-out is visible without a tab-refresh.

- [ ] 2026-04-19 — **Plan 08 · Task 41 follow-up — cron scheduler for `/cron/video-escalation-timeout`:** route is registered (`backend/src/routes/cron.ts`) and the worker is unit-testable via `runVideoEscalationTimeoutJob(correlationId)`, but the actual 5-second trigger is deployment-layer wiring (Render cron / Supabase scheduled edge function / GitHub Actions cron) and lives outside this repo's app code. Un-defer trigger: first deploy of Plan 08 to any environment that runs real video escalations. Until then, local dev can poke the route via `curl -H "x-cron-secret: $CRON_SECRET" POST /cron/video-escalation-timeout` or via the in-test direct call.

- [ ] 2026-04-19 — **Plan 08 · Task 41 follow-up — doctor display-name on the patient consent modal:** v1 renders "Your doctor would like to record video" because the patient's Supabase auth context does not yet have RLS-backed SELECT on `doctors.display_name` (participants can see *session* columns but not directory metadata). Upgrade when the doctor-directory facade exposes a patient-scoped read (e.g. `get_session_doctor_display_name(session_id)` SECURITY DEFINER RPC) — patient-visible doctor identity is already appropriate for live consults, so this is UX polish, not a security change.

- [ ] 2026-04-19 — **Plan 08 · Task 41 follow-up — unit + integration tests for `recording-escalation-service` + frontend consent modal:** deferred from the acceptance criteria to keep the initial implementation PR focused. Minimum set before merge: (a) rate-limit state machine (max 2, 5-min cooldown, no-stacking — each branch against `video_escalation_audit` fixtures), (b) atomic UPDATE race between `patientResponseToEscalation` and `video-escalation-timeout-worker` (fast-patient wins; slow-patient returns `already_timed_out`), (c) Twilio retry-and-fail path (second failure stamps `twilio_error_code`, emits `video_recording_failed_to_start`, patient response row stays `'allow'`), (d) controllers' 429/409/400 mapping + `meta.availableAt` passthrough, (e) Vitest coverage of `<VideoConsentModal>` for allow / decline / network-error / timeout-mid-view / submit-after-timeout (server returns `accepted:false, reason: already_timed_out`).

- [ ] 2026-04-19 — **Plan 08 · Task 41 follow-up — legal review before merge:** highest-risk task in Plan 08 (failure modes A/B/C in the task doc directly leak PHI or bypass consent). Owner-confirmed legal sign-off required on (1) consent-modal copy, (2) `video_recording_failed_to_start` system-message wording, (3) decline being hidden from chat (task-41 Note #3 — document why in the privacy policy), (4) retention on `video_escalation_audit` (participant SELECT under RLS is already scoped; question is whether 7-year retention per Decision 10 is compatible with jurisdictional right-to-erasure). Block merge until signed.

- [ ] 2026-04-19 — **Plan 08 · Task 41 follow-up — patient Realtime auth refresh:** the Supabase browser-session token drives the Postgres-changes subscription. If the patient's session token expires mid-consult (default 1h), the channel silently stops receiving updates and the modal never surfaces. Trigger: first time a consult runs > 1h OR a patient refreshes late in the session. Fix: either (a) extend `usePatientVideoConsentRequest` to re-subscribe on `auth.onAuthStateChange(TOKEN_REFRESHED)`, or (b) add a heartbeat that re-runs the `refresh()` GET every 2min and re-subscribes if the token rotated. Option (a) is cleaner; option (b) is more defensive.

---

- [x] 2026-04-19 — **Plan 08 · Task 45 (migrations 069 + 070) — video-recording audit-schema extensions shipped:** Decision 10's three data-model extensions landed in a single pass, gating every downstream Plan 08 task:
  - **Migration 069 — `recording_access_type` ENUM + column on `recording_access_audit`.** ENUM with `('audio_only','full_video')` using the idempotent `DO`-block pattern; column ADD nullable → back-fill existing rows to `'audio_only'` → lock `NOT NULL` + `DEFAULT 'audio_only'`. Plan 07 Tasks 29 + 32 rows (audio replay, transcript download) are audio-derived so the back-fill is correct; Task 44 is the sole `'full_video'` writer. ENUM name prefixed (`recording_access_type`, not bare `access_type`) to avoid namespace collisions; column name stays `access_type`. Reverse migration documented in-file.
  - **Migration 070 — `video_escalation_audit` + `video_otp_window` tables.** `video_escalation_audit`: one row per doctor-initiated video-recording request, `session_id` FK + CASCADE, `doctor_id` intentionally unFK'd (account-deletion carve-out; matches Migration 051 `sender_id` pattern), `reason TEXT CHECK (char_length BETWEEN 5 AND 200)`, `preset_reason_code CHECK` in `('visible_symptom','document_procedure','patient_request','other')`, `patient_response CHECK` in `('allow','decline','timeout')`, and a row-shape CHECK `video_escalation_audit_response_shape` pinning `(patient_response, responded_at)` as co-present. Index `(session_id, requested_at DESC)` covers Task 41's rate-limit query. RLS enabled with a participant-scoped SELECT policy; no client-write policies (service-role doctrine). `video_otp_window`: PK = `patient_id` FK + CASCADE (operational cache, not audit — cascade on account delete is the right semantic), `last_otp_verified_at NOT NULL`, `last_otp_verified_via TEXT + CHECK IN ('sms')` so Plan 2.x can widen to email / authenticator / biometric additively. Index on `last_otp_verified_at` pre-provisioned for a future nightly eviction worker. RLS self-scoped SELECT policy.
  - **Types — `backend/src/types/video-recording-audit.ts`** (new module, co-located with `consultation-transcript.ts` rather than `database.ts` to match the per-domain post-consultation convention). Exports `RecordingAccessType`, `VideoEscalationPresetReasonCode`, `VideoEscalationPatientResponse`, `VideoOtpVerificationMethod` unions + row / insert / update shapes. Documented in file docstring why it lives outside `database.ts`.
  - **Query helpers — `backend/src/services/video-recording-audit-queries.ts`** (new module). Five thin Supabase-admin adapters (`fetchRecentEscalationsForSession`, `insertVideoEscalationRequest`, `resolveVideoEscalationResponse`, `fetchVideoOtpWindow`, `upsertVideoOtpWindow`) that pin the schema contract Tasks 41 + 44 will consume. Zero business logic — rate-limit arithmetic, cooldown windows, and escalation rules belong in the downstream tasks.
  - **Tests — `backend/tests/unit/migrations/video-recording-audit-extensions-migration.test.ts`** (30 content-sanity assertions): ENUM shape + step ordering, all CHECK constraints, index shapes, RLS SELECT policies, absence of client-write policies, reverse-migration blocks. **`backend/tests/unit/services/video-escalation-audit-query.test.ts`** (12 query-shape round-trips): exact Supabase chain (`from → select → eq → order → limit` for rate-limit, `gt(last_otp_verified_at, windowStart)` for OTP-window), camelCase ↔ snake_case mapping, null passthrough, error paths.
  - **Decisions taken at impl-time:** (1) Plan 02 Task 29's pre-existing "block" on the base `recording_access_audit` table is stale — Migration 065 (Plan 07 Task 29) already shipped the table; both migrations 069 + 070 went out together. (2) Types live in `types/video-recording-audit.ts` (per-domain convention) rather than `database.ts`. (3) Shipped the query-helpers module as part of Task 45 so Tasks 41/44 import a pre-tested primitive rather than re-implementing chains. (4) `'other'` is a first-class CHECK value on `preset_reason_code`; NULL is reserved for future non-modal callers (admin/AI-initiated). (5) `video_otp_window.patient_id` gets the CASCADE FK (operational cache); `video_escalation_audit.doctor_id` stays unFK'd (audit carve-out).
  - **Verification:** `tsc --noEmit` clean; migration-test suite 30/30 green; query-test suite 12/12 green; full backend `jest` suite **1702/1702 across 132 suites** (was 1690 before Task 45, +12 net); ReadLints clean on all 6 new/changed files.
  - **Known follow-ups** (tracked below): composite `(session_id, access_type)` index on `recording_access_audit` deferred to Task 44 telemetry; nightly eviction worker for `video_otp_window` deferred until table exceeds 100k rows (index already in place); `video_escalation_audit.twilio_composition_sid` denormalization deferred to Task 43 artifact-retrieval telemetry; patient-visible access-history surface explicitly deferred by Decision 10 (the SELECT policy permits the future UX without a schema bump).
  - **Smoke test (manual, owner-gated):** deferred pending Supabase dev-project access. The four smoke assertions (`enum_range`, default back-fill, row-shape CHECK rejection, cross-patient RLS isolation) are pinned at the content-sanity + query-shape layer; the dev-project run is belt-and-braces.

- [ ] 2026-04-19 — **Task 45 follow-up — composite `(session_id, access_type)` index on `recording_access_audit`:** v1 queries are session-keyed only; `access_type` is read back but never filtered on at SELECT time. Un-defer trigger: Task 44 telemetry shows a per-session "audio only vs video" breakdown query that scans the session bucket too hot.

- [ ] 2026-04-19 — **Task 45 follow-up — nightly eviction worker for `video_otp_window`:** v1 keeps expired rows forever (table is tiny — one row per patient who ever did video replay; ~50 bytes × 10k patients = 500 KB negligible). Task 44's own skip-check correctly returns "OTP required" for stale rows, so there's no correctness pressure. Un-defer trigger: table exceeds 100k rows OR support metrics show a pattern of "patient claims to have verified but told to re-verify" that stems from an incorrectly-set `last_otp_verified_at`. Index `idx_video_otp_window_verified_at` already covers the future `DELETE WHERE last_otp_verified_at < now() - interval '30 days'` query.

- [ ] 2026-04-19 — **Task 45 follow-up — optional `video_escalation_audit.twilio_composition_sid` denormalization:** Today the composition SID for a per-escalation video recording is found via the session's Twilio room SID + the `requested_at` time-range. If Task 43's artifact-retrieval path needs a direct link for speed (avoid the range scan on Twilio's side), add the column additively. v1 no.

- [ ] 2026-04-19 — **Task 30 follow-up — extend `backend/src/types/database.ts` with `doctor_dashboard_events`:** Today the table is reached only through `dashboard-events-service.ts`, which carries its own typed DTOs (`DashboardEventRow`, `InsertDashboardEventInput`). The unified database type was deliberately skipped to avoid touching unrelated callers. Un-defer trigger: a second module wants to query the table directly (e.g. an admin tool, a worker, or the patient portal feed mirror).

- [ ] 2026-04-19 — **Task 30 follow-up — frontend test harness for the dashboard event feed:** `DoctorDashboardEventFeed` + `DashboardEventsBell` were shipped without component tests because the frontend repo doesn't yet ship a Jest/RTL setup. When the test harness lands, add: (a) feed rendering + empty state, (b) "Mark as read" optimistic update + acknowledge call, (c) time-ago formatting, (d) bell polling cadence + visibility-aware pause, (e) copy pinning per `artifactType`.

- [ ] 2026-04-19 — **Task 30 follow-up — manual smoke test of the mutual replay notification loop:** Once Task 29's `mintReplayUrl` is live against an IG-DM/SMS sandbox, walk through the full loop documented in the task doc (doctor replay → patient DM, patient replay → doctor dashboard event, idempotency + RLS checks, "mark as read" round-trip).

- [ ] 2026-04-19 — **Task 30 follow-up — Supabase Realtime on `doctor_dashboard_events`:** v1 polls `unread=true&limit=1` every 60s for the bell badge. If doctors keep the dashboard open for hours and complain about staleness, swap the polling for a Realtime subscription on the table (RLS already restricts to `doctor_id = auth.uid()`).

- [ ] 2026-04-19 — **Task 30 follow-up — retention worker for `doctor_dashboard_events`:** The table will grow by one row per replay forever. v1 ships no sweeper because volume is single-digit per doctor per month. When a doctor accumulates >1000 acknowledged events or table size becomes meaningful, ship a worker that deletes acknowledged rows older than 90 days.

- [ ] 2026-04-19 — **Task 30 follow-up — aggregate consecutive replays into one DM:** A doctor who replays the same consult three times in an hour today sends three patient DMs. Aggregation (one DM per consult per day per artifactType) is documented in the task's out-of-scope list as the obvious v2 improvement; revisit once we see telemetry on per-doctor replay frequency.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 13 (Phase 1.7) — staff-feedback learner now writes into `examples[]`; autofill-modernization slice closed end-to-end:** Pre-Task-13 contract on `appendMatcherHintsOnDoctorCatalogOffering` (the writer invoked when staff corrects a misroute on the review inbox) was **always-legacy** — even on rows where Task 06 had migrated `matcher_hints` to `examples[]` and Task 11 had stopped the AI suggest from re-emitting legacy fields, every staff correction silently appended the patient fragment to `keywords` / `include_when` instead. That's exactly the dual-write Routing v2 exists to remove: doctor sees pristine "Example phrases" in the editor, staff correction lands in legacy fields beneath, resolver picks v2, learner contribution is invisible to the doctor and ignored by the matcher. The deferral was documented in the function header from Task 04 with explicit un-defer triggers (Task 06 ships editor + Task 11 ships AI suggest); both shipped earlier today, so this task pulls the trigger. **Implementation:** new pure helper `appendExamplesEntry(existing, fragment, maxCount?, maxLen?)` exported from `backend/src/services/doctor-settings-service.ts` — defaults pull `MATCHER_HINT_EXAMPLE_MAX_CHARS` (120) and `MATCHER_HINT_EXAMPLES_MAX_COUNT` (24) from `service-catalog-schema.ts` so the helper auto-tracks any future schema-cap change without a separate code update. Helper trims the fragment, truncates to `maxLen`, runs case-insensitive trimmed dedupe against existing entries (returns `changed: false` on hit so the caller short-circuits the DB write), and on append-past-cap **FIFO-evicts** the oldest entry (`shift()` until `length <= maxCount`). **Eviction strategy decision** (locked in Task 13's Decision log): FIFO because corrections from this week are more relevant than corrections from last quarter; alternative provenance-aware eviction (drop oldest *learner-added* entry only) would require a per-entry source field on `serviceMatcherHintsV1Schema.examples` plus a UI badge in the editor, deliberately deferred until that telemetry exists. Main writer `appendMatcherHintsOnDoctorCatalogOffering` now branches on `(offering.matcher_hints?.examples?.length ?? 0) > 0`: **v2 branch** collapses `inc || kw` into a **single** `examples[]` entry via the helper (v2 has no asymmetry between keywords-token vs include-phrase, both are patient-style example phrases — `inc` preferred because it's already example-shaped, `kw` falls back if only that's set), routes `exc` through `appendMatcherHintFields({exclude_when}, {exclude_when})` for the existing single-string semicolon merge (same field in v1 and v2), preserves any pre-existing legacy `keywords` / `include_when` strings **byte-identical** via spread (mixed-shape defense — re-touching them would re-introduce dual-write, the exact problem this task removes), short-circuits when neither `examplesChanged` nor `excludeChanged`. **Legacy fallback** (`examples` absent or empty) runs the original `appendMatcherHintFields` semicolon-merge byte-for-byte unchanged for not-yet-migrated rows. **Mixed-shape note**: a row with both `examples` AND legacy fields populated is rare today (Task 06 + Task 07's `draftsToCatalogOrNull` drops legacy on save when examples present, Task 11's normalizer strips dual-emit from LLM output) but possible during transitional states (un-saved drafts, AI suggest pre-Task-11 outputs persisted before the migration); the v2 branch handles it correctly without re-introducing dual-write — pinned by the `mixed-shape offering ... legacy keywords byte-identical` test. **Header comment** rewrite — replaced the Task-04 deferral paragraph with a `Routing v2 contract (Plan 19-04, Task 13 — un-defers the Task-04 hold)` block documenting the v2-vs-legacy branching rule, the `inc || kw` precedence reasoning, the mixed-shape defense, and back-references to `resolveMatcherRouting`. **Coverage:** 12 new tests in `backend/tests/unit/services/doctor-settings-append-matcher-hints.test.ts`: 5 pure-helper tests on `appendExamplesEntry` (append + trim returns `changed: true`, idempotent dedupe with whitespace + mixed-case input, truncate fragments past `MATCHER_HINT_EXAMPLE_MAX_CHARS`, empty/whitespace returns `changed: false`, FIFO eviction at the 24-cap with last entry = newest); 6 new integration tests in a `Routing v2 examples-aware branch (Task 13)` describe block covering the 5 acceptance-criteria cases (v2 + new fragment → appended to `examples[]` with no legacy fields, v2 + duplicate → no DB write returns `false`, v2 at 24-cap → FIFO drops `existing-phrase-0` keeps the rest, mixed-shape → `examples` updated + legacy `keywords` byte-identical, legacy-only → existing legacy-append regression unchanged) plus 2 bonus cases (v2 + `exclude_when`-only payload → routes through single-string merge correctly, v2 + `keywords`-only payload → exercises the `inc || kw` fallback when caller sets only `kw`); 1 new top-level legacy-only regression test pinning the back-compat branch. **All 8 pre-existing tests pass without modification — total 21/21.** Verification: `npx tsc --noEmit` clean; `npx jest tests/unit/services/doctor-settings-append-matcher-hints.test.ts` 21/21; `npx jest` (full backend) **1076/1076 across 82 suites** (was 1064 — +12 net, zero unrelated regressions); `npx eslint src/services/doctor-settings-service.ts` clean. Grep across `backend/src` confirms no other writer assigns to legacy `matcher_hints.keywords` / `include_when` directly (writes route through helper functions, not direct field assignment) — the two known writers (`appendMatcherHintFields` legacy fallback and `setMatcherHintsOnDoctorCatalogOffering` full-replace) build a fresh `ServiceMatcherHintsV1` object instead of mutating in place, so the only remaining v1-emitting code path is the legacy-fallback branch in this very function. Files touched: `backend/src/services/doctor-settings-service.ts` (new `appendExamplesEntry` helper, v2 branch with mixed-shape defense, rewritten header comment, new schema-cap constants imported), `backend/tests/unit/services/doctor-settings-append-matcher-hints.test.ts` (12 new test cases), `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-13-feedback-learner-writes-examples.md` (Status: Done, all acceptance criteria checked off with implementation notes, Decision log filled, Shipped section added), `docs/Development/Daily-plans/April 2026/19-04-2026/Plans/plan-service-catalog-matcher-routing-v2.md` (Task 13 row marked ✅), `docs/Development/service-catalog-matching-stages.md` (code map gains Task 13 note). **Telemetry follow-ups** (no code change required, post-deploy): dashboard the rate of v2-branch hits vs legacy-fallback hits in the first week of staff corrections — if no legacy hits in 30 days, file a follow-up to delete the legacy branch + the `appendMatcherHintFields` helper; optionally add `provenance: 'staff_correction'` per-entry tagging on `examples[]` so the editor can show "added by your AI receptionist learning from a misroute" badges (separate task — needs schema + UI work). **Closes the autofill-modernization slice end-to-end:** every code path that **writes** `matcher_hints` on a v2 row now writes to `examples[]` exclusively (Task 06 editor saves, Task 07 catalog-banner conversion, Task 11 AI suggest outputs, Task 13 staff-feedback learner); every legacy-shaped row continues to round-trip unchanged through the back-compat fallbacks. The reader half landed in Task 03 (`resolveMatcherRouting`); both halves of Routing v2 are now done.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 11 (Phase 1.6) — autofill loop closed; AI suggest now emits `examples[]`:** The earlier complaint — *"when I add starter catalog using AI, it shows legacy fields, all those AI autofills are generating old fields"* — is now **fully fixed end-to-end**. Tasks 02–07 had migrated persistence + resolver + matcher + editor to v2 (`matcher_hints.examples[]`), but the **AI generation prompts** in `backend/src/services/service-catalog-ai-suggest.ts` still hardcoded the legacy `keywords` + `include_when` shape, so every sparkle / starter / review-suggested-card landed in the editor with empty `examples` and populated legacy fields → triggered Task 07's amber "older matching hints" callout immediately on AI-generated cards. Six surgical changes, all in the AI-suggest module: (1) **`SCHEMA_BLOCK_FOR_CARDS`** flipped — `matcher_hints` schema now lists `examples: string[]` + `exclude_when: string` only, with explicit emission guidance ("4–12 entries, lower-case, patient words not clinical jargon, no PHI, ≤120 chars each, deduped") and a hard "Do NOT emit \"keywords\" or \"include_when\" — those legacy fields have been retired" line so the LLM sees the migration intent in-prompt, not just in our heads; (2) **`SCHEMA_BLOCK_FOR_REVIEW`** `suggestedCard.matcher_hints` flipped to the same v2 shape — the `overlap` issue-type description was updated to *"share too many `examples[]` phrases (or, on un-migrated rows, legacy keywords / include_when text)"* so the LLM still understands legacy rows when reviewing existing catalogs without being told to emit them; (3) **`SCOPE_MODE_RULE_BLOCK`** wording updated — both the strict-card rule ("Strict cards must always carry **a concrete `examples[]` list**") and the configuration-bug line ("if you produce strict, also produce a non-empty `examples[]` array") now reference the v2 vocabulary; the JSDoc on the constant explains the v1→v2 wording flip and pins this constant as the single source of truth shared between the AI-suggest prompt and the matcher prompt — but **the matcher service has its own inline copy of the strict-rule wording (`service-catalog-matcher.ts` line 355) and was intentionally left alone** so the matcher snapshot tests stay quiet (matcher already reads via `resolveMatcherRouting` regardless of vocabulary, so changing matcher prose would be churn for zero behavior change); (4) **`summarizeExistingCatalogForLlm`** line label flipped from `keywords="…"` to `examples="…"` — the data was already coming from `resolveMatcherRouting.examplePhrases` since Task 05, only the misleading legacy label remained, which was biasing the LLM back into emitting `keywords` even when the catalog summary actually showed v2 phrases; (5) **`buildSingleCardPrompt`** existing-hints block now has **three rendering branches**: examples-bearing (neutral header — "Doctor's existing hints for this card (refine or extend; do not silently drop them):"), legacy-only (a brand-new **legacy header** — `"Doctor's existing matching cues (legacy — please convert to \`examples[]\` in the output, do not echo \`keywords\` / \`include_when\`):"` with each line tagged `(legacy)`), and empty (no-hints copy unchanged) — pre-Task-11 the legacy-only payload rendered under the same neutral header, so the LLM routinely *mirrored* the legacy fields back as part of its output ("the doctor sent me keywords, I'll send back keywords"); (6) **`normalizeRawLlmCard`** **defense** — when `examples.length > 0`, the saved `matcher_hints` includes only `{ examples, exclude_when? }` and any leaked `keywords` / `include_when` is dropped. Dual-emit logs `service_catalog_ai_suggest: dropped legacy matcher_hints fields in favour of examples[] (Task 11 defense)` at **info** level (not warn — a model that obeys the new schema never emits both shapes, so dual-emit is a non-fatal model-drift signal we want to dashboard, not a code bug) with `service_key` + counts — PHI-safe, no patient text. Legacy-only emits flow through unchanged for back-compat with older models and the staff-feedback writer (Task 13 still appends to legacy fields today; tightening this branch further would race ahead of Task 13 and lose data on rows produced by older models — companion back-compat test pins this contract). **Important non-changes documented in Decision log:** (a) **`exclude_when` stays a string** (not an array) — orthogonal to the routing-vocabulary unification, separate schema discussion; (b) **no `service_offerings_json` `version` bump** — the additive `examples` field already exists on v1 (Task 02), no breaking change; (c) **no Stage A behavior change** so the `dm-routing-golden` corpus baseline held without re-snapshotting; (d) **no prompt snapshot files** — this suite uses inline `toContain` / `not.toContain` assertions (more diff-readable than `.toMatchSnapshot()` for prose-heavy prompt strings). **Coverage:** 9 new tests in a dedicated `Task 11 — AI suggest prompts emit examples[] (no legacy keywords/include_when)` describe block in `backend/tests/unit/services/service-catalog-ai-suggest.test.ts` covering: scope-rule wording flip (3 positive + 2 negative assertions), single_card schema (`"examples":` present, no `"keywords":"..."` / `"include_when":"..."` JSON KV — anchored on the regex `/"keywords"\s*:\s*"/` so prose mentions in the `overlap` description don't trip the assertion), starter schema (same), review schema (same, plus `suggestedCard.matcher_hints` JSON shape), legacy-header rendering (legacy-only payload renders under "please convert" header with `(legacy)` tags), examples-header rendering (examples-bearing payload does NOT show the legacy header), normalizer defense (dual-emit input → output has only `examples` + `exclude_when`, no `keywords` / `include_when`), normalizer back-compat (legacy-only input → legacy fields flow through unchanged so older models still produce savable cards), and end-to-end single_card with v2 stub LLM (full `generateAiCatalogSuggestion` round-trip → returned `card.matcher_hints` has `examples` only). 3 pre-existing assertions on `summarizeExistingCatalogForLlm` (`keywords="…"`) flipped to `examples="…"`; the legacy-fallback existing-hints test now asserts `(legacy)` tags + the convert header. **Verification:** `npx tsc --noEmit` ✅; `npx jest tests/unit/services/service-catalog-ai-suggest.test.ts` ✅ (67 tests, 9 new); `npx jest` ✅ (**1064 / 1064 across 82 suites** — was 1055; +9 net, zero unrelated regressions); `npx eslint src/services/service-catalog-ai-suggest.ts` ✅. Matcher prompt snapshot in `tests/unit/services/service-catalog-matcher.test.ts` unaffected — matcher uses its own inline copy of the strict-rule wording. Files touched: `backend/src/services/service-catalog-ai-suggest.ts` (schema blocks + scope-rule wording + summarizer label + existing-hints branches + normalizer defense + extensive JSDoc anchoring every change to "Plan 19-04, Task 11"), `backend/tests/unit/services/service-catalog-ai-suggest.test.ts` (flipped 4 existing assertions + added 9-case Task 11 describe block), `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-11-ai-suggest-emits-examples-v2.md` (Status: Done, all acceptance criteria checked off with detailed shipped notes, Decision log filled, Shipped section added), `docs/Development/Daily-plans/April 2026/19-04-2026/Plans/plan-service-catalog-matcher-routing-v2.md` (Task 11 row marked ✅). **Independence:** Task 12 (per-mode token budget — shipped earlier today) was a prerequisite **only for review/starter modes' reliability** (truncation → malformed JSON), not for Task 11; the two are fully independent. Task 13 (staff-feedback learner → `examples[]`) is the next logical task — once it ships, the back-compat branch in `normalizeRawLlmCard` can be tightened further. **Telemetry follow-up** (no code change required): dashboard the rate of `service_catalog_ai_suggest: dropped legacy matcher_hints fields…` info logs in production — if a specific model snapshot accounts for >5% of suggestions, file a ticket to either pin a newer snapshot or tighten the prompt. **Manual QA pending the user** — click sparkle on an empty card → drawer should show Example phrases populated, no amber "older matching hints" callout. Click "Generate starter catalog" → none of the generated rows should show the callout. The autofill loop is now closed: doctor uses AI autofill → card lands in editor with `examples` → save persists `matcher_hints.examples` → matcher reads via resolver → no migration callout ever fires on AI-generated cards.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 10 (Phase 4, hybrid landing) — plan now Done end-to-end:** Dev-flag-gated "Try as patient" preview shipped as the first slice of Phase 4 (the plan explicitly invited this — "may be **dev-only** first"). Endpoint: `POST /api/v1/catalog/preview-match` registered in `backend/src/routes/api/v1/catalog.ts` **only** when `resolveCatalogPreviewMatchEnabled({ flag, nodeEnv })` returns true (defaults to enabled for `NODE_ENV !== 'production'`; can be force-on via `CATALOG_PREVIEW_MATCH_ENABLED=true` for staging or force-off via `=false`). When disabled, the route is **not mounted at all** so production traffic gets a clean 404 instead of a 403, which matches Express conventions and avoids leaking the route's existence. Auth: `authenticateToken` (doctor scope) regardless of flag — same posture as `ai-suggest`. Body schema (strict): `{ catalog: ServiceCatalogV1, reasonForVisitText: string (1..2000, trimmed), recentUserMessages?: string[] (max 8 × 2000), doctorProfile?: { practiceName?, specialty? } | null }` validated via `serviceCatalogV1BaseSchema` (no catch-all enforcement on purpose — an in-progress draft missing the catch-all is exactly the kind of state a doctor would want to preview, and the matcher already handles a missing catch-all gracefully via first-row fallback). Behavior: **single** call to `matchServiceCatalogOffering` (not two — `result.source` already distinguishes Stage A / Stage B / fallback unambiguously) → translated via the new pure exported helper `summarizePreviewMatchResult(result, label, llmAvailable)` into `{ path, matchedServiceKey, matchedLabel, suggestedModality, confidence, autoFinalize, mixedComplaints, reasonCodes, llmAvailable }` where `path: 'stage_a' | 'stage_b' | 'fallback' | 'single_fee'`. Translation rules: `source==='llm'` → `stage_b`; `source==='fallback'` → `fallback`; `source==='deterministic'` + `SINGLE_FEE_MODE` reason code → `single_fee`; otherwise `stage_a`. `llmAvailable` is set from `Boolean(getOpenAIClient())` so the UI can warn the doctor if a `fallback` simply means "no OPENAI_API_KEY in this env" rather than "no semantic match exists." PHI handling: the matcher already calls `redactPhiForAI` on `reasonForVisitText` before any LLM hop and never logs the raw text — the new route adds **zero new logging surface**. Frontend: new component `frontend/components/practice-setup/CatalogPreviewMatchPanel.tsx` (textarea + "Run preview" button + result card with colored Stage badge, confidence, suggested modality, matched label/key, conditional `!llmAvailable` warning, conditional `mixedComplaints` line) + new helpers `postCatalogPreviewMatch` / `PreviewMatchRequest` / `PreviewMatchResponse` / `PreviewMatchPath` in `frontend/lib/api.ts` (404 from backend → "Preview is not enabled on this backend (set CATALOG_PREVIEW_MATCH_ENABLED=true)" so flag-mismatch is debuggable without DevTools). Mounted in `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx` **only** in `multi_service` mode (single-fee mode has nothing to route) and **only** when `process.env.NEXT_PUBLIC_CATALOG_PREVIEW_MATCH_ENABLED === 'true'` OR `process.env.NODE_ENV === 'development'` — both are inlined at build time so production builds without the flag don't ship the panel at all. The panel runs against the **current draft catalog** (mirrors the existing `runServerReview` pipeline: `draftsToCatalogOrNull` → `safeParseServiceCatalogV1`; if invalid → render disabled), so doctors preview their **on-screen unsaved edits**, not the persisted DB row — which is exactly what makes the preview useful during catalog construction. `previewDoctorProfile` derived from `settings.practice_name` + `settings.specialty` so the LLM gets specialty context in Stage B. **Locked product decisions** (documented in Task 10's Decision log): (1) telemetry — misroute → suggested example phrases — is **explicitly deferred** with un-defer triggers (graduate to telemetry only after the Phase 3 sibling overlap rule has 2+ weeks of shadow data so suggestions aren't seeded by guesses); (2) in-DM-style transcript preview is **explicitly deferred** — DM copy has its own test surface (Plan 04 — Patient DM copy polish) and conflating the two would create a brittle integration test; (3) production-grade RBAC + audit log + rate-limiter hardening is **explicitly out of scope** — the route is dev-flag-gated and admits this. The deliberate decision was: **ship the high-leverage half (preview) in ~2 hours instead of the full 12–24 h slice (preview + telemetry + production hardening) that has no measured signal demanding it yet**. Same pattern as Task 09's hybrid landing. New tests: 16 cases in **new file** `backend/tests/unit/routes/catalog-preview-match.test.ts` (first test under `tests/unit/routes/`) across 3 describe blocks: `previewMatchRequestSchema` (6 — accepts minimal valid payload, accepts optional fields, rejects empty `reasonForVisitText` after trim, rejects missing `catalog`, rejects unknown top-level keys via strict, caps `recentUserMessages` at 8); `summarizePreviewMatchResult — source → path translation` (7 — every `source` × `reasonCodes` combo pinned: `llm→stage_b`, `fallback→fallback`, `deterministic` no-SINGLE_FEE_MODE→`stage_a`, `deterministic`+SINGLE_FEE_MODE→`single_fee`, `llmAvailable=false` propagation on fallback so UI can warn, missing `suggestedModality`→null coercion, `mixedComplaints` passthrough); `resolveCatalogPreviewMatchEnabled — env gating` (3 — `flag=true` always on, `flag=false` always off, `flag=undefined` (auto) on for dev/test off for production). Test pattern note: kept the gating helper **pure** (takes `{ flag, nodeEnv }` rather than reading `env` directly) so unit tests can pin every combination without re-parsing `process.env` — same pattern used for `summarizePreviewMatchResult`. Verification: backend `tsc --noEmit` clean; frontend `tsc --noEmit` clean; `jest tests/unit/routes/catalog-preview-match.test.ts` 16/16; full backend suite **1045/1045 across 82 suites** (+16 vs Task 09's 1029/81), 57/57 snapshots intact; `eslint src/routes/api/v1/catalog.ts src/config/env.ts` clean; `eslint components/practice-setup/CatalogPreviewMatchPanel.tsx app/dashboard/settings/practice-setup/services-catalog/page.tsx lib/api.ts` (frontend) clean. **No matcher logic change** so the Stage A `dm-routing-golden` corpus baseline holds without re-snapshotting. Files touched: `backend/src/config/env.ts` (new `CATALOG_PREVIEW_MATCH_ENABLED` knob with `'auto'` semantics), `backend/src/routes/api/v1/catalog.ts` (new route + 3 exported helpers), `backend/tests/unit/routes/catalog-preview-match.test.ts` (new file, 16 tests), `frontend/lib/api.ts` (new helper + 3 types), `frontend/components/practice-setup/CatalogPreviewMatchPanel.tsx` (new file), `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx` (gated mount + memoized `previewCatalog` / `previewDoctorProfile`), `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-10-preview-widget-stage-a-vs-b.md` (Done — hybrid + Decision log + Shipped section), `docs/Development/Daily-plans/April 2026/19-04-2026/Plans/plan-service-catalog-matcher-routing-v2.md` (Phase 4 acceptance ticked + status footer expanded — **plan now Done end-to-end with hybrid landings on Phases 3 + 4**), `docs/Development/service-catalog-matching-stages.md` (code map gains preview-widget row, Last-updated bumped). Plan status: **all 4 phases done** (0 + 1 + 2 + 3 + 4); deferred items (sibling per-row schema fields, preview telemetry, in-DM transcript preview, production hardening) carry explicit un-defer triggers in their respective task docs.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 09 (Phase 3, hybrid landing):** Sibling boundaries shipped as **prompt-only**; the per-row schema half (`confused_with_service_keys`, `prefer_other_when`) is **explicitly deferred with rationale** in both the task doc's Decision log and the plan's Phase 3 acceptance bullet. Decision rationale (priority order): (1) philosophical fit — adding a doctor-facing field that competes with `examples` for attention contradicts the "doctors fill examples, AI handles disambiguation" rule from `AI_BOT_BUILDING_PHILOSOPHY.md`; (2) the LLM is not blind today — `buildAllowlistPromptLines` already serializes every sibling row's resolved `keywords=` / `include_when=` / `exclude_when=` / `[scope]` for Stage B, so misroutes are a prompt-engineering problem, not an information-availability problem; (3) reversibility — schema changes are forever (migration, Zod, frontend, validation, doctor education), prompt rules tune in one PR; (4) no telemetry yet — building schema for an unmeasured problem is the exact pattern the user has pushed back on. Un-defer triggers documented: (a) shadow telemetry from the Apr 12 learning system shows a recurring sibling-pair misroute the prompt rule cannot fix, or (b) doctors organically ask for explicit "send to the other row" controls. Two changes in `backend/src/services/service-catalog-matcher.ts`: (1) **new rule 6 — Sibling tie-breaker** added to `buildServiceCatalogLlmSystemPrompt` that tells the LLM "prefer the row whose hints contain the more specific phrase the patient text matched verbatim → on equal specificity prefer `[scope: strict]` over `[scope: flexible]` → if still tied pick `'other'` rather than guessing → never split the difference by routing a sibling-overlapping complaint to a third unrelated row." Old rules 6 (specialty defaults) → 7 and 7 (use "other") → 8 with rule 8's "rules 1–6" reference updated to "rules 1–7"; (2) new exported helper `detectSiblingExampleOverlaps(catalog)` + internal renderer `buildSiblingDisambiguationBlock(catalog)` that read through `resolveMatcherRouting` (so v2 `examples` and legacy `keywords` CSV feed the same path), tokenize each phrase (lowercase, ≥ 4 chars, conservative stop-word filter — `today`, `tomorrow`, `patient`, `doctor`, `service`, etc.), exclude the catch-all row (`other`), require a token in 2+ rows, and cap at 5 deterministic entries (token asc, service_keys per token asc). When overlap exists, a `Disambiguation hints (rows whose example phrases share a token — apply the sibling tie-breaker rule, pick by the patient's most specific cue):` block is injected between the JSON schema and the allowlist listing each shared token and the rows that contain it. When no overlap, the block is omitted entirely so clean catalogs stay clean. **Critical edge case pinned in tests**: legacy `include_when`-only rows do NOT synthesize false-positive overlap — the resolver returns `examplePhrases=[]` for those, so the detector ignores them (matches the documented asymmetry: `include_when` is a gate, never a positive signal). New `Phase 3 sibling tie-breaker + disambiguation hints (Routing v2, Plan 19-04, Task 09)` describe block in `service-catalog-matcher.test.ts` adds 10 named cases: shared-token detection with sorted output, catch-all exclusion (even when the catch-all carries matching examples), no-overlap → empty, legacy `include_when`-only → empty, stop-word + short-token exclusion (`appointment` surfaced; `the`/`for`/`today`/`tomorrow` filtered), deterministic 5-entry cap with token-asc ordering, block injection on overlap (anchored on the unique parenthetical block-only header `Disambiguation hints (rows whose…` to disambiguate from the rule-6 mention in prose), block omission on no-overlap (same anchor), rule renumbering (rule 6 = sibling tie-breaker, rule 7 = specialty, rule 8 = use "other" w/ "rules 1–7" reference), and block positioning between Schema and Allowlist. **Verification**: `tsc --noEmit` clean; `jest tests/unit/services/service-catalog-matcher.test.ts` 51/51 pass (41 pre-existing + 10 new); full backend suite **1029/1029 pass across 81 suites**, 57/57 snapshots intact (+10 vs Task 08's 1019); `eslint src/services/service-catalog-matcher.ts` 0 errors (1 pre-existing warning at line 549, unrelated). **No Stage A behavior change** so the `dm-routing-golden` corpus baseline holds without re-snapshotting. Files touched: `backend/src/services/service-catalog-matcher.ts`, `backend/tests/unit/services/service-catalog-matcher.test.ts`, `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-09-sibling-boundary-schema-prompt.md` (marked Done with full Decision log + Shipped section), `docs/Development/Daily-plans/April 2026/19-04-2026/Plans/plan-service-catalog-matcher-routing-v2.md` (Phase 3 acceptance ticked as hybrid + status footer updated), `docs/Development/service-catalog-matching-stages.md` (Stage B summary gains Phase 3 bullet, code map gains sibling-disambiguation row, Last-updated bumped). Plan status: Phase 0 + 1 + 2 + 3 done. Only Phase 4 (preview / telemetry — flagged optional) remains.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 08 (Phase 2):** Strict / flexible × resolved-hints behavior matrix pinned in three places that must move together so any regression points the reviewer at one named test: (1) JSDoc on `runDeterministicServiceCatalogMatchStageA` in `backend/src/utils/service-catalog-deterministic-match.ts` carries the full 9-cell table with cell IDs `A1..C3`; (2) `docs/Development/service-catalog-matching-stages.md` gains a new "Strict / flexible × resolved hints — Stage A behavior matrix (Phase 2 / Task 08)" section before the Stage B summary, with the matrix, a "reading the matrix" key, and the deferred "Prefer assistant matching" flag noted as the future flag-on column; (3) `backend/tests/unit/utils/service-catalog-deterministic-match.test.ts` gets a new `Phase 2 matrix (Routing v2, Plan 19-04, Task 08)` describe block with one named test per cell — every test forces the multi-row Stage A code path on `catalogNcdGpOther()` so cells exercise the same branch the production matcher takes (instead of accidentally hitting the "single non-catch offering" fast-path shortcut). Cells covered: A1/B1 (`examples` + hit → `medium`, `autoFinalize=false`), A2/B2 (`examples` + no overlap → `null` → Stage B), A3 (`examples` + `exclude_when` overlap → `null`, red flag wins), B3 (flexible + label-only → `high`, `autoFinalize=true`), C1 (strict + label-only → `medium` downgrade), C2 (strict + legacy `include_when` only → `null`, no example-phrase corroboration), C3 (flexible + legacy `include_when` only with no examples / no label hit → `null`, `legacy_merge` yields 0 score). Pure documentation + regression tests; **no Stage A behavior change** (Task 04 already lifted the strict-corroboration check to read `ResolvedRoutingHints.examplePhrases`). What this protects against: re-introducing "include_when alone is enough corroboration on strict" trips C2; dropping the strict label-only downgrade trips C1 (matches B3); silently ignoring `exclude_when` when `examples` is set trips A3; letting strict `examples` hit auto-finalize trips A1 (auto-finalize is reserved for the label fast path and even there strict downgrades it). Out of scope (deferred): "Prefer assistant matching" product flag — when/if added, the matrix becomes the flag-off default and a flag-on column gets added that returns `null` for cells A1/B1 (forcing Stage B); doc already calls this out so the next implementer doesn't have to rediscover it. Verification: `npx tsc --noEmit` (backend) clean; `npx jest tests/unit/utils/service-catalog-deterministic-match.test.ts` 30/30 pass (21 pre-existing + 9 new matrix cells); `npx jest` (full backend suite) **1019/1019 / 57 snapshots** across 81 suites (+9 vs Task 07's 1010); `npx eslint src/utils/service-catalog-deterministic-match.ts` clean. Files touched: `backend/src/utils/service-catalog-deterministic-match.ts`, `backend/tests/unit/utils/service-catalog-deterministic-match.test.ts`, `docs/Development/service-catalog-matching-stages.md`, `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-08-strict-flexible-deterministic-matrix.md`, `docs/Development/Daily-plans/April 2026/19-04-2026/Plans/plan-service-catalog-matcher-routing-v2.md`. Plan Phase 1 + Phase 2 acceptance checkboxes both ticked; only Phase 3 (sibling boundaries) and Phase 4 (preview / telemetry) remain — both flagged as optional in the plan.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 07 (Phase 1.5):** Doctor-facing migration UX shipped — un-migrated rows no longer have an "empty" matcher UI. Per-card change in `frontend/components/practice-setup/ServiceOfferingDetailDrawer.tsx`: the Task 06 `<details>` disclosure is replaced by an **always-visible** amber callout (`data-testid="drawer-legacy-hints-migration-callout"`) that explains routing keeps working via the resolver, exposes the editable Keywords / Book-this-when textareas in-place, and adds a one-tap **Convert to example phrases** button (`data-testid="drawer-convert-legacy-hints"`) backed by the new pure helper `convertLegacyHintsToExamples` in `frontend/lib/service-catalog-drafts.ts` (splits `matcherKeywords` on `[,;\n]+`, `matcherIncludeWhen` on `\r?\n`, merges with any existing `matcherExamples`, runs through `normalizeMatcherExamplesDraft` for trim/dedupe/clamp to 24 × 120 chars, zeros both legacy fields). Catalog-level addition in `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`: dismissible amber banner above the multi-service editor (`data-testid="catalog-routing-v2-migration-banner"`) with pluralized count copy ("1 service…" / "N services…") + Dismiss button — dismissal persists per-browser in `localStorage` (`clariva.routing-v2-migration-banner.dismissed`); banner auto-hides when the legacy-only count hits 0, and the `multi_service` JSX gating means single-fee doctors never see it. `hasUnmigratedLegacyHints` is now exported from the drawer module so the page banner and per-card callout share a **single precedence rule** mirroring the backend resolver. **Locked product decision** (documented in Task 07 doc): when a row's `matcherExamples` becomes non-empty, `draftsToCatalogOrNull` writes `matcher_hints.examples` (+ `exclude_when`) and **drops** legacy `keywords` / `include_when` — no audit-only retention; the always-visible callout means the doctor sees legacy values *before* converting so nothing meaningful is silently cleared. localStorage failure modes (private mode / quota) wrapped in try/catch on both read + write paths, fail open (banner shows). Verification: frontend `tsc --noEmit` clean; `next lint --dir lib --dir components/practice-setup --dir app/dashboard/settings/practice-setup` clean; backend untouched (1010/1010 baseline holds). Manual QA checklist in PR (drawer convert flow, mixed/v2 rows skip the callout, banner dismiss persists, per-browser scope, single-fee mode hides banner). Files touched: `frontend/lib/service-catalog-drafts.ts`, `frontend/components/practice-setup/ServiceOfferingDetailDrawer.tsx`, `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`. Plan + task marked done; `service-catalog-matching-stages.md` code map gains Catalog-banner row + Task 07 narrative + Last-updated line refreshed.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 06 (Phase 1.4):** Practice Setup editor now exposes **Example phrases** as the primary doctor-facing matcher input, with `ServiceOfferingDraft.matcherExamples: string[]` round-tripping through `offeringToDraft` / `draftsToCatalogOrNull` / `aiSuggestedCardToDraft` / `applyAiSuggestionToDraft`. Save-side contract is **single-source-of-truth**: when a row has at least one example phrase, `draftsToCatalogOrNull` writes `matcher_hints.examples` (+ `exclude_when`) and **drops** legacy `keywords` / `include_when` — no silent dual-write, fully aligned with the resolver's precedence rules. Un-migrated rows (no examples) keep the pre-v2 byte-for-byte legacy emit so existing catalogs round-trip unchanged. New helpers: `normalizeMatcherExamplesDraft` (mirrors `backend/src/utils/matcher-routing-resolve.ts#normalizeMatcherExamplePhrases`), `exampleTextToList`, `exampleListToText`. UI: new `ExamplePhrasesField` subcomponent in `ServiceOfferingDetailDrawer.tsx` provides a newline-separated textarea + chip preview + remove-chip + count/cap indicator (24-phrase × 120-char limit). **Not this service when…** stays as the secondary input. Legacy `keywords` / `include_when` textareas are tucked behind a `<details>` disclosure shown **only** for un-migrated rows (`hasUnmigratedLegacyHints`); new rows never see them — Task 07 will polish this into a full migration banner. AI suggest plumbing extended end-to-end: `AiSuggestSingleCardPayload.existingHints.examples?: string[]` + `AiSuggestCardV1.matcher_hints.examples?: string[]` in `frontend/lib/api.ts`; drawer sparkle button + page-level `fill_with_ai` action both forward `matcherExamples` to the backend; diff modal renders an Example-phrases row and prefers v2 examples on apply (legacy fields zeroed once doctor accepts examples). Backend route (`backend/src/routes/api/v1/catalog.ts`) `existingHints` schema accepts `examples: string[]` (max 24 × 200 chars, looser than persisted bounds so in-progress drafts don't 400) and counts it toward the single_card "has input" guard. `buildSingleCardPrompt` renders `examples: a | b | c` as a dedicated line and **suppresses** legacy `keywords` / `include_when` lines when `examples` is present so the LLM never sees two competing routing vocabularies for the same card. Local catalog-quality badge (`frontend/lib/catalog-quality-local.ts`) is now resolver-aware: `hasEmptyHints` / token count treat v2 `examples` as routing material, and `include_when` is suppressed when `examples` is non-empty (matches Task 05's resolver-driven `runDeterministicCatalogReview`). 4 new backend Jest tests under `buildSingleCardPrompt — existingHints.examples (Routing v2 Task 06)`: examples renders + suppresses legacy on the same card, legacy fallback when examples absent/empty, route schema accepts `examples`, route schema stays strict (rejects unknown matcher-hint fields). Verification: frontend `tsc --noEmit` clean; `next lint` clean on `lib/`, `components/practice-setup/`, `app/dashboard/settings/practice-setup/`. Backend `tsc --noEmit` + ESLint clean on touched files; full backend suite **1010/1010 / 57 snapshots** across 81 suites (+4 vs Task 05's 1006). Grep across `frontend/`: the only writer of `matcher_hints.keywords` is the legacy fallback branch of `draftsToCatalogOrNull` (un-migrated rows only — required for byte-for-byte legacy round-trip until Task 07 ships). Files touched: `frontend/lib/service-catalog-drafts.ts`, `frontend/lib/api.ts`, `frontend/lib/catalog-quality-local.ts`, `frontend/components/practice-setup/ServiceOfferingDetailDrawer.tsx`, `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`, `backend/src/routes/api/v1/catalog.ts`, `backend/src/services/service-catalog-ai-suggest.ts`, `backend/tests/unit/services/service-catalog-ai-suggest.test.ts`. Plan + task marked done; `service-catalog-matching-stages.md` code map + Routing v2 note + Last-updated line refreshed; v2 (`examples`-only) rows now round-trip end-to-end from the editor through Stage A, the LLM prompt, AI suggest, and review.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 05 (Phase 1.3):** AI suggest, catalog review, and the doctor-context AI prompt now consume `resolveMatcherRouting` exclusively — v2 (`examples`-only) rows route end-to-end from Stage A through review without bypassing the resolver. `service-catalog-ai-suggest.ts#summarizeExistingCatalogForLlm` sources `keywords="..."` from `resolved.examplePhrases.join(', ')` (so v2 rows surface in `single_card`, `starter`, and `review` system prompts); `tokenizeOfferingKeywords` (renamed from `tokenizeKeywords`) tokenizes resolved phrases so a v2 examples-only sibling still trips `keyword_overlap_with_sibling`; deterministic review counters (`runDeterministicCatalogReview` + `hasEmptyMatcherHints`) use `resolved.examplePhrases.length` and `resolved.legacyIncludeWhen` so `strict_thin_keywords` / `strict_empty_hints` honor v2 hints (and `exclude_when`-only cards stay empty — preserves pre-v2 asymmetry). `consultation-fees.ts#formatMatcherHintsForAiContext` signature changed from `(hints)` to `(offering)` and routes through the resolver; LLM-facing vocabulary (`keywords=…; include_when=…; exclude_when=…`) intentionally preserved to avoid prompt drift — v2 rows fill `keywords=` from resolved phrases and omit `include_when=`. AI suggest's LLM-output normalizer (`normalizeAndValidateCard`) now passes through `matcher_hints.examples[]` so future LLM contracts (Task 06) can emit v2 hints without further plumbing. 10 new tests: 7 in `service-catalog-ai-suggest.test.ts` (catalog summary v2 render, v2 examples win over legacy in summary, strict_thin_keywords on v2 row, no false-positive on rich v2 row, no strict_empty_hints on v2-filled row, exclude_when-only stays empty, sibling-overlap on v2 examples-only siblings) + 3 in `consultation-fees.test.ts` (v2 row renders via resolver / no legacy bleed, legacy row backward-compat, v2+legacy mixed row prefers examples). `tsc --noEmit` clean; ESLint clean on all 4 touched source files; full backend suite **1006/1006 / 57 snapshots** across 81 suites (+10 vs Task 04's 996/996). Files: `backend/src/services/service-catalog-ai-suggest.ts`, `backend/src/utils/consultation-fees.ts`, `backend/tests/unit/services/service-catalog-ai-suggest.test.ts`, `backend/tests/unit/utils/consultation-fees.test.ts`. Grep across `backend/src/`: no routing/LLM consumer reads `matcher_hints.keywords` / `include_when` directly outside `matcher-routing-resolve.ts`. Allow-listed remainders are all writer-side (`appendMatcherHints*`, `service-match-hint-sanitize`, schema merge — Task 06 owns the v2 push), payload validation (`routes/api/v1/catalog.ts`), and LLM input/output schema-shaping in AI suggest (`existingHints` payload + LLM-output legacy-field normalization). Plan + task marked done; `service-catalog-matching-stages.md` code map + Routing v2 note updated.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 04 (Phase 1.3):** Stage A + LLM prompt builder now consume `resolveMatcherRouting` exclusively. Stage A (`backend/src/utils/service-catalog-deterministic-match.ts#matcherHintScore`) operates on `resolved.examplePhrases` (`+4` per substring hit), `resolved.legacyIncludeWhen` (loose-overlap gate, legacy rows only), and `resolved.excludeWhen` (`-1` red flag). Stage B snippet (`service-catalog-matcher.ts#matcherHintsSnippetForLlm`) reads via the same resolver but **keeps the LLM-facing vocabulary** (`keywords=…; include_when=…; exclude_when=…`) so the existing matching-policy rules in `buildServiceCatalogLlmSystemPrompt` continue to bind without prompt-text drift — only the data source flips. v2 (`examples`-only) rows feed `keywords=` from `resolved.examplePhrases.join(', ')` and omit `include_when=`; legacy rows produce byte-identical snippets to pre-v2. Resolver field renamed `llmIncludeWhen` → `legacyIncludeWhen` (more honest: both Stage A and Stage B legitimately read `include_when` on legacy rows). SFU-18 strict-mode rule preserved verbatim — strict requires a positive `examplePhrases` hit, an `include_when` overlap alone never auto-routes. `appendMatcherHintsOnDoctorCatalogOffering` (`doctor-settings-service.ts`) keeps appending to legacy fields until Task 06 ships the editor's `examples` UI; extended JSDoc explains the deferral (avoid silent dual-write while editor still surfaces only legacy text areas; reader path is already safe via resolver, so writer deferral doesn't gate anything). 8 new tests across `service-catalog-deterministic-match.test.ts` (5: v2 strict examples-only routes, v2 wins over legacy, v2 ignores legacy `include_when` gate, v2 honors `exclude_when`, blank examples falls through to legacy_merge) + `service-catalog-matcher.test.ts` (3: v2 row serializes into `keywords=` snippet, v2 wins over legacy in snippet, legacy-only keeps `include_when=` snippet). All pre-existing tests pass unmodified — zero behavior drift on legacy rows. `tsc --noEmit` clean; ESLint clean on all 4 touched source files; full backend suite **996/996 / 57 snapshots** across 81 suites (+8 vs Task 03's 988). Files: `backend/src/utils/matcher-routing-resolve.ts`, `backend/src/utils/service-catalog-deterministic-match.ts`, `backend/src/services/service-catalog-matcher.ts`, `backend/src/services/doctor-settings-service.ts`, `backend/tests/unit/utils/matcher-routing-resolve.test.ts`, `backend/tests/unit/utils/service-catalog-deterministic-match.test.ts`, `backend/tests/unit/services/service-catalog-matcher.test.ts`. Remaining direct readers are out of scope for Task 04: `service-catalog-ai-suggest.ts` + `consultation-fees.ts#formatMatcherHintsForAiContext` (AI suggest paths — Task 05), `routes/api/v1/catalog.ts` (request-payload validation, not routing). `service-catalog-matching-stages.md` Stage A + Stage B sections + code map updated.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 03 (Phase 1.2):** New module `backend/src/utils/matcher-routing-resolve.ts` exports `resolveMatcherRouting(offering): ResolvedRoutingHints` plus helpers (`normalizeMatcherExamplePhrases`, `legacyKeywordsToPhraseParts`, types `ResolvedRoutingHints` and `ResolvedRoutingLegacySource`). Contract: `{ examplePhrases, excludeWhen?, legacySource: 'examples' | 'legacy_merge', llmIncludeWhen? }` — `llmIncludeWhen` is set **only** in legacy_merge so Stage A never receives `include_when` prose as keyword tokens (preserves today's asymmetry, but centralized). Legacy merge algorithm frozen in JSDoc + tests: split `keywords` on `[,;\n\r]+`, trim, dedupe case-insensitive (preserve first-seen order), clip per-phrase to `MATCHER_HINT_EXAMPLE_MAX_CHARS`, cap at `MATCHER_HINT_EXAMPLES_MAX_COUNT`. When `examples` normalize to non-empty, legacy strings are ignored entirely (no dual-feed). 18 unit tests in `backend/tests/unit/utils/matcher-routing-resolve.test.ts` across 4 describe blocks (examples path, legacy_merge golden fixtures, edge cases, helpers). No callers wired yet — Tasks 04–05 swap Stage A + LLM prompt + AI suggest off direct `matcher_hints.keywords` / `include_when` reads. `tsc --noEmit` clean; full backend suite **988/988** (81 suites). `service-catalog-matching-stages.md` code map updated.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 02 (Phase 1.1):** Optional `matcher_hints.examples: string[]` on `ServiceMatcherHintsV1` — max 24 items × 120 chars (`MATCHER_HINT_EXAMPLE_MAX_CHARS`, `MATCHER_HINT_EXAMPLES_MAX_COUNT`) in `backend/src/utils/service-catalog-schema.ts` + `frontend/lib/service-catalog-schema.ts`. Legacy `keywords` / `include_when` retained; JSDoc marks routing v2 + `resolveMatcherRouting` (Task 03). `appendMatcherHintFields` preserves `examples` when merging legacy strings. Tests in `backend/tests/unit/utils/service-catalog-schema.test.ts`. `service-catalog-matching-stages.md` code map updated.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 01 (Phase 0):** Added `docs/Development/service-catalog-matching-stages.md` (Stage A deterministic → Stage B LLM only when Stage A returns null; single-fee / skipLlm / no-client shortcuts; code map; FAQ). Linked from `docs/Reference/AI_BOT_BUILDING_PHILOSOPHY.md` Related section as the intentional hybrid exception to “LLM first.” `@see` pointers in `backend/src/services/service-catalog-matcher.ts` and `backend/src/utils/service-catalog-deterministic-match.ts`. Practice setup: second paragraph under **Matching hints** in `frontend/components/practice-setup/ServiceOfferingDetailDrawer.tsx` (two-step matching, keywords + “book when” both matter when exact keywords missing). Plan + task marked done.

code related 

[] save and leave  on all pages of settings just like how its set for services catalog
[] description on service catalog 
[] booking flow improvments - consent taking flow issue 
[] doctor availability time on payment slot links in case opd mode is set to queue ,
[] always verify from doctor before booking first mode, some docs may prefer this 
[] only onE APPOINTMETN FEE FLOW ?  in case any doc only wants one appointment fee , others/not listed coloumn should be present only if multiple services are to be selected by doc with different prices ?
[] placeholder appears if want confirmation 
[] notification system for docs ( match reviews and other things )
- [x] Pre-existing test failure on `main`: `backend/tests/unit/workers/webhook-worker-characterization.test.ts` — "confirm_details yes → consent (self booking)" expects `/Anything else|extras/i` but current extras prompt uses "Any special notes for the doctor — like allergies, medications, or preferences". Update either the assertion or the copy to realign. → Fixed 2026-04-16 by widening the regex to `/special notes|allergies|Anything else|extras/i`.
- [ ] Follow-up to task-04 (service scope mode): add a dismissible one-time migration banner in practice setup explaining "New services default to Strict matching; existing services default to Flexible". Needs localStorage or `doctor_settings.ui_flags_json` persistence. Inline helper in the drawer was shipped as interim education.
- [ ] Follow-up to task-06 (AI auto-fill): per-doctor token-budget telemetry for `POST /api/v1/catalog/ai-suggest` (currently only the OpenAI helper logs token counts globally). Open question 1 from plan-02.
- [ ] Follow-up to task-06: add a route-level test harness for `POST /api/v1/catalog/ai-suggest` (express + supertest + auth-token mock). Service layer is fully unit-tested in `tests/unit/services/service-catalog-ai-suggest.test.ts`; the route file itself is currently only covered by tsc + Zod schema validation.
- [ ] Follow-up to task-06: bootstrap Jest in `frontend/` so we can add a unit test for the `aiSuggestionMeta` round-trip in `lib/service-catalog-drafts.ts` (`offeringToDraft` strips it, `draftsToCatalogOrNull` ignores it). Today the frontend has Playwright e2e only.
- [ ] Follow-up to task-06: manual end-to-end click-through of all four AI trigger points (empty-catalog starter, new-card inline banner, sparkle on empty hints, sparkle on filled hints → diff modal) against a real doctor profile to validate copy + UX with live LLM output.
- [ ] Follow-up to task-07 (catalog quality checks): implement `[Fix all with AI]` bulk dispatch — runs each issue's `autoFixAvailable` action sequentially, shows one combined toast at the end, and auto-snapshots `service_offerings_json` before the run so the doctor can revert. Drafts only; never auto-persists. Blocked until snapshot/undo model is agreed on.
- [ ] Follow-up to task-07: wire `enable_modality` and `reprice` one-tap fixes once the per-channel pricing context (per-modality default prices + modality availability from `doctor_settings`) is plumbed through `applyAiSuggestionToDraft`.
- [ ] Follow-up to task-07: frontend component tests for `CatalogCardHealthBadge` (4 states + scope-aware tooltip) and `CatalogReviewPanel` (grouping, fix dispatch, save-anyway). Blocked on the existing "bootstrap Jest in `frontend/`" inbox item above.
- [ ] Follow-up to task-07: cross-workspace schema-parity runner (jest project or node script) that imports `backend/src/types/catalog-quality-issues.ts` + `frontend/lib/catalog-quality-issues.ts` and asserts enum + action equality. Today parity is enforced by `backend/tests/unit/types/catalog-quality-issues.test.ts` plus manual review.
- [ ] Follow-up to task-07: manual end-to-end of catalog review — hand-craft a catalog that triggers one of each issue type, apply each `[Fix with AI]` action, and confirm the resulting draft state.
- [ ] Follow-up to task-07: surface the mixed-complaint signal in `review` mode once Plan 01 Task 05 telemetry (per-service misroute counts) lands.
- [x] 2026-04-18 — Plan 04 (Patient DM copy polish) Task 01 shipped: `backend/src/utils/dm-copy.ts` now owns patient-facing DM strings; golden-snapshot harness at `backend/tests/unit/utils/dm-copy.snap.test.ts`; `nonTextAck` migrated (verbatim) as the seed entry. Tasks 02–11 will migrate their strings into this module. Plan: `docs/Development/Daily-plans/April 2026/18-04-2026/plan-patient-dm-copy-polish.md`.
- [x] 2026-04-18 — Plan 04 Task 02 shipped: `buildConfirmDetailsMessage` now renders a multi-line labeled block ("**Name:** …\n**Age:** …\n…\n\nIs everything correct? Reply **Yes** …") instead of the old comma chain ("Let me confirm: **X**, **Y**, …"). Builder lives in `dm-copy.ts`; `collection-service.ts` re-exports it so existing imports are unchanged. 7 new snapshot cases cover full / long-reason / missing-email / missing-reason+email / gender normalization (MALE → Male, female → Female) / whitespace-only reason. Full suite: 80 suites / 844 tests / 8 snapshots green. Follow-up: manual DM screenshot in staging once deployed.
- [x] 2026-04-18 — **Bug-fix: "Review my catalog" now critiques the on-screen draft, not the stale DB row.** Reported behavior: open `Settings → Practice setup → Services catalog`, hit `Review my catalog`, click `Add suggested card`, then click `Run review again` → the same gap suggestion came back. `Close → Save → Review my catalog` produced a fresh suggestion instead. Root cause: the `add_card` (and every other) one-tap fix only mutated local React state (`services` in `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`); `runServerReview()` then called `POST /api/v1/catalog/ai-suggest` with `{ mode: "review" }` and the backend (`loadAiSuggestContext` in `backend/src/services/service-catalog-ai-suggest.ts`) rehydrated the catalog from `doctor_settings.service_offerings_json` — which still didn't contain the just-added card. So the LLM critiqued the saved snapshot, not what the doctor was looking at. Fix (Option B — "always send the on-screen draft"): (1) extended `AiSuggestRequest.catalog?: ServiceCatalogV1 | null` with three explicit states — `undefined` = use DB (legacy callers / smoke), `null` = "draft is empty" (deterministic `missing_catchall` still fires without a DB read), object = use this exact catalog. Documented the contract in JSDoc on both backend (`AiSuggestRequest`) and frontend (`api.ts AiSuggestRequest`) types. (2) `loadAiSuggestContext` gained a `LoadAiSuggestContextOptions` parameter; when `catalogOverride` is set we use it as-is and skip `safeParseServiceCatalogV1FromDb`. Telemetry: a new `service_catalog_ai_suggest: catalog source resolved` info log records `catalogSource: 'client_draft' | 'db'` + `serviceCount` (PHI-safe) so we can spot if a future caller drops the override. (3) Route schema (`backend/src/routes/api/v1/catalog.ts`) accepts `catalog: serviceCatalogV1BaseSchema.nullable().optional()` — deliberately the *base* schema (not `serviceCatalogV1Schema`) because an in-progress draft missing the catch-all is exactly the case the deterministic review should flag, so we must not reject it at the boundary. (4) `runServerReview()` now runs the same `draftsToCatalogOrNull` + `safeParseServiceCatalogV1` pipeline `performSave` uses — validation symmetry — and refuses the round-trip with a doctor-facing error in `reviewError` if the draft is invalid mid-edit (e.g. catch-all deleted, empty label, bad follow-up window) instead of silently stripping rows and shipping a misleading critique. The deterministic `localIssues` panel still renders, so the doctor isn't left with no feedback. (5) The post-save background review path (`performSave` → `void runServerReview().then(…)`) automatically benefits because it now sends the freshly-saved draft as the override too, eliminating any race between the PATCH commit and the review reading the previous DB snapshot. Coverage: 7 new unit tests in `backend/tests/unit/services/service-catalog-ai-suggest.test.ts` describe-block `catalog override (post-Plan-04 bug-fix)` lock all three states at both the `loadAiSuggestContext` layer and the `generateAiCatalogSuggestion` end-to-end (review mode + single_card mode), plus assert that omitting `catalog` keeps the DB path intact and that `null` skips the LLM call entirely while still firing `missing_catchall`. `tsc --noEmit` clean (backend + frontend); ESLint clean on `service-catalog-ai-suggest.ts`, `routes/api/v1/catalog.ts`, `frontend/lib/api.ts`, and `services-catalog/page.tsx`. Full backend suite: **80 suites / 964 tests / 57 snapshots** green (net +9 tests, +0 snaps vs. post-Plan-04 baseline). Files: `backend/src/services/service-catalog-ai-suggest.ts`, `backend/src/routes/api/v1/catalog.ts`, `backend/tests/unit/services/service-catalog-ai-suggest.test.ts`, `frontend/lib/api.ts`, `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`. Backend retains the DB-load fallback (`catalog === undefined`) on purpose so future server-side workflows (e.g. cron-driven catalog audits, admin tooling, the staff-review surface) can keep calling without an in-flight draft. Follow-up: extend the override to the editor's `single_card` triggers (sparkle on hints, fill_with_ai from the review panel) so AI sibling-summaries also see unsaved cards; for now `handleApplyFix`'s `fill_with_ai` / `switch_to_strict_and_fill` calls still pass no `catalog`, which is acceptable because the bug the user reported was specifically about review re-runs.
- [x] 2026-04-18 — **Plan 04 (Patient DM copy polish) complete — all 11 tasks shipped.** Task 11 (P3, PR-5) closes the plan with a softer **non-text acknowledgement**: when a patient sends an image, voice note, sticker, or reaction, `buildNonTextAckMessage()` in `backend/src/utils/dm-copy.ts` now returns `"I can't read images or voice notes yet — could you type your message instead? I'll take it from there."` instead of the pre-2026-04-18 bot-framed `"I can only process text messages right now. Please type your request and I'll help you."`. Rationale captured in an extended JSDoc above the function (supersedes Task 01's "verbatim seed" placeholder): the old copy framed the problem from the bot's side ("I can only process…") — patients care that their message didn't land and what to do next. New copy names the two inputs patients most plausibly expect a response to (images, voice notes); stickers and reactions are deliberately unnamed because they rarely carry a question. English-only by design, matching the plan's non-goals (if localized variants are needed, add them as a follow-up task). No signature change: the single caller in `backend/src/workers/instagram-dm-webhook-handler.ts:1253` (`const nonTextAck = buildNonTextAckMessage();`) is untouched, and the suppression / audit-log logic around it is untouched too (scope guard was explicit about not re-gating when the ack fires). Coverage: the pre-existing `dm-copy snapshots nonTextAck / default` snapshot was regenerated via `jest -u` (snap count unchanged at 57; this is the single snapshot that was intentionally updated in place, which is why the plan's snapshot-suite baseline stays at 57 rather than 58). Added a new `buildNonTextAckMessage invariants (Task 11)` describe block in `backend/tests/unit/utils/dm-copy.snap.test.ts` with 2 tests guarding the *shape* independently of the *wording*: (a) single-line only (`split('\n').length === 1`), no markdown bolding (`**` banned), no emoji (explicit negative assertion against the Unicode symbol + pictograph ranges `\u{1F300}-\u{1FAFF}` and `\u{2600}-\u{27BF}`), names "images" + "voice notes" + contains "type" so the patient is always told what to do; (b) drops the legacy `process text messages` wording so it can't silently resurface via a future copy-paste. `tsc --noEmit` clean; ESLint clean on both touched files (`dm-copy.ts`, `dm-copy.snap.test.ts`). Full suite: **80 suites / 955 tests / 57 snapshots** green — net +2 tests, +0 snaps vs. the post-Task-10 baseline, zero regressions. **Plan 04 final state:** all 11 tasks `[x]`, the plan's "Acceptance for the plan" checklist is 4-of-5 complete (the last item — staging smoke — is deferred to PR-4 + PR-5 rollout per individual task docs). Source of truth for patient-facing DM copy is now `backend/src/utils/dm-copy.ts` + locale-specific helpers in `complaint-clarification.ts` + `reason-first-triage.ts`; golden-snapshot suite covers every message in `backend/tests/unit/utils/__snapshots__/dm-copy.snap.test.ts.snap`. Before / after on Task 11 example: image DM → old `"I can only process text messages right now. Please type your request and I'll help you."` → new `"I can't read images or voice notes yet — could you type your message instead? I'll take it from there."`. Follow-up: staging smoke — send an image, a voice note, and a sticker to the bot; all three should trigger the same new copy (the suppression logic already dedupes a second ack within the throttle window, unchanged).
- [x] 2026-04-18 — Plan 04 Task 10 shipped (P3, PR-5): reason-first triage **ask-more-after-deflection** copy split from one cramped sentence into two scannable paragraphs across all 3 locale leaves in `backend/src/utils/reason-first-triage.ts` (`clinicalDeflectionAskMoreEnglish` / `clinicalDeflectionAskMoreHi` / `clinicalDeflectionAskMorePa`) × 3 sub-branches (blank snippet / multi-line `\n`-bearing snippet / single-line snippet) = 9 rendered variants. English target shape for the blank-snippet branch: `**Is there anything else** you'd like the doctor to address at this visit?\n\nIf that's the full picture, reply **nothing else** and we'll move to **booking** or **fees**.`; snippet-bearing branches use the shortened `**Is there anything else** you'd like the doctor to address?` question and `If that covers it, reply **nothing else** and we'll move to **booking** or **fees**.` escape-hatch, preserving the `**So far we've noted:**` header (block variant uses `\n\n`-wrapped snippet; single-line uses inline `**{snippet}**.`). Hinglish and Roman-Gurmukhi copy kept byte-for-byte — only `\n\n` was inserted after the question mark (and, where applicable, the `phir …` / `ta …` next-step sentence moved to its own paragraph). **Scope correction vs. the original task doc:** doc projected 15 snapshots (3 sub-branches × 5 locales including Roman Hindi / Roman Punjabi). In practice `detectSafetyMessageLocale` (`backend/src/utils/safety-messages.ts`) collapses Roman-script markers (`mujhe`, `mera`, `menu`, `naal`, …) into the `hi` / `pa` return values, and the existing `reason-first-triage.ts` strings for those locales are already transliterated — so Roman and Devanagari/Gurmukhi inputs route through the same leaf helper. Real target is 3 × 3 = 9 snapshots; the task doc top-note records this. **Tests:** 9 snapshots added to the shared `dm-copy.snap.test.ts` via `formatClinicalReasonAskMoreAfterDeflection(userText, snippet)` as the public entry point (keys `dm-copy snapshots triage / {en,hi,pa} / {blank, single-line, multi-line} snippet`), plus 6 invariant tests under a new `formatClinicalReasonAskMoreAfterDeflection invariants (Task 10)` block — asserting the `?\n\n` break presence, snippet-header prefix (`**So far we've noted:** **…**.` for single-line, `**So far we've noted:**\n\n…\n\n` for multi-line; locale equivalents `**Ab tak note kiya:**` / `**Haje tak note kita:**`), all bold phrase tokens preserved (`**Is there anything else**` / `**Kya aur kuch**` / `**Hor kuj**`, `**nothing else**`, `**booking**`, `**fees**`), and a regression guard that forbids the legacy single-line tail pattern `Reply **nothing else** if … — then we can (help with|move to) booking or fees` in every user-text × snippet combination. No other test needed migration — search confirmed no pre-existing unit/characterization test asserted on the old exact strings (only the source file itself held the literals). `tsc --noEmit` clean (15 s); ESLint clean on both touched files (`reason-first-triage.ts` + `dm-copy.snap.test.ts`). Full unit suite: **80 suites / 953 tests / 57 snapshots** green — net +15 tests, +9 snaps vs. post-Task-09 baseline (938 / 48), zero regressions. **Before / after example (en, blank snippet):** old — `"**Is there anything else** you'd like the doctor to address at this visit? Reply **nothing else** if what you shared is the full picture — then we can help with **booking** or **fees** next."` (one sentence stacking question + escape hatch + next-step preview + em-dash bridge); new — `"**Is there anything else** you'd like the doctor to address at this visit?\n\nIf that's the full picture, reply **nothing else** and we'll move to **booking** or **fees**."` (question on line 1, escape-hatch + next-step on line 3 with a blank line between). Follow-up: staging smoke on English + one Roman locale (Hinglish suggested) to verify the Instagram renderer honors the paragraph break at the client — deferred to PR-5 rollout.
- [x] 2026-04-18 — Plan 04 Task 09 shipped (P3, PR-5): mixed-complaint clarification now echoes the parsed concerns back as a numbered pick-list so the patient can reply with a number instead of re-typing. Three-layer change. **Matcher (`backend/src/services/service-catalog-matcher.ts`):** added `concerns?: string[]` to `ServiceCatalogMatchResult`; extended the LLM system prompt with a dedicated "Concerns list (only when mixed_complaints is true)" rules block + updated the schema line to `{"service_key":…,"mixed_complaints":true|false,"concerns":["<label1>",…]}`; added `normalizeLlmConcerns(raw)` at the parser boundary (array-only, trims entries, truncates > 40 chars with `…`, case-insensitive dedupe first-wins, caps at `SERVICE_MATCH_MAX_CONCERNS = 5`, returns `undefined` when fewer than 2 valid entries remain — `undefined` signals "render the existing open-ended copy"); `concerns` is populated only when `mixedComplaints === true` (hallucinated lists alongside `mixed_complaints:false` are dropped silently). Exported the new caps so every caller sees one contract. **Copy (`backend/src/utils/complaint-clarification.ts`):** extended `resolveComplaintClarificationMessage(userText, parsedConcerns?: readonly string[])` with an optional second arg — when `parsedConcerns.length ∈ [2, 5]` it renders a three-paragraph numbered-list shape (intro + `**N.** {label}` list + grammatical CTA with bolded numeric choices: `Reply **1** or **2**` / `Reply **1**, **2**, or **3**` / up to `Reply **1**, **2**, **3**, **4**, or **5**`); outside that range (0/1/6+, `undefined`) it returns today's locale string unchanged so legacy callers and in-flight LLM responses never regress. 5 locale variants via a `CLARIFICATION_NUMBERED_BY_LOCALE` table keyed by `en | hi | pa | latin-hi | latin-pa`, each owning `intro`, `ctaTemplate` with `{choices}` token, and a `joinChoices` function (Oxford-joiners for English/Hinglish/Roman-Punjabi; Devanagari `"या"` and Gurmukhi `"ਜਾਂ"` conjunctions for script variants). Concern labels themselves render English verbatim per the task's "English concerns in all locales for now" design constraint (localized concerns is a matcher-quality concern, not a copy concern). Also added `resolveClarificationNumericReply(replyText, concerns): string | null` — a pure helper that accepts only 1–2 ASCII digits, rejects free-text / non-integer / out-of-range / negative / decimal / hex inputs, and returns the mapped concern label or `null` (no locale-digit parsing for now; future enhancement). **State + handler (`backend/src/types/conversation.ts` + `backend/src/workers/instagram-dm-webhook-handler.ts`):** added `pendingClarificationConcerns?: string[]` to `ConversationState`, documented as "may contain PHI, same compliance posture as `originalReasonForVisit`, cleared on every exit from `awaiting_complaint_clarification`"; `maybeTriggerComplaintClarification` persists the concerns list on state in the same tick it transitions to `step = 'awaiting_complaint_clarification'` and passes the list to the copy helper so the patient sees the numbered list; the clarification-requested log breadcrumb now includes `clarificationConcernCount` (count only — the labels themselves are never logged at `info` to avoid PHI-adjacency). Added a numeric-reply short-circuit inside the first-attempt branch of the `awaiting_complaint_clarification` dispatch: `const mappedConcern = resolveClarificationNumericReply(rawReply, state.pendingClarificationConcerns); const narrowedReason = mappedConcern ?? rawReply;` — so `"2"` → matcher sees `"Diabetes follow-up"` as the narrowed reason instead of an ambiguous `"2"`. Invalid digits (`"9"` when only 3 concerns) resolve to `null` and fall through to the raw reply, which typically produces a low-confidence match and escalates to staff review on this attempt (intentional: the patient gave us no usable signal). `pendingClarificationConcerns` is cleared on every exit path — consent-resume, staff-review-escalate, cap-reached — so stale concerns from a prior round never leak into a later clarification event with a different complaint set. **Tests:** 7 new matcher tests (prompt schema includes concerns spec; LLM `mixed_complaints:true + concerns` surfaces the array; > 40-char entries truncated with `…`; case-insensitive dedupe first-wins; `mixed_complaints:false + concerns` drops the list; < 2 valid entries → `undefined`; single-fee short-circuit reports `concerns:undefined`). 9 new snapshots in `dm-copy.snap.test.ts` (en/2, en/3, en/5, en/1-fallback, en/6-fallback, hi Devanagari/3, pa Gurmukhi/3, latin-hi/3, latin-pa/3). Two new invariant blocks totaling 15 unit tests: `resolveComplaintClarificationMessage invariants` (7 tests covering paragraph structure, bold-dot numbers, grammatical CTA joins, fallback ranges, script detection for hi/pa, Roman variants, verbatim concern labels across all 5 locales) + `resolveClarificationNumericReply` (8 tests covering valid mapping, whitespace tolerance, out-of-range null, free-text null, undefined/empty concerns null, > 2-digit rejection, negative/decimal/hex rejection). Handler integration coverage is via the pure `resolveClarificationNumericReply` helper — the handler wiring is a single-line `?? rawReply` fallback; end-to-end dispatch tests deferred to staging smoke. Pre-existing tests in `complaint-clarification.test.ts` (single-arg legacy behavior across all 5 locales + `shouldRequestComplaintClarification` predicate) still pass unchanged thanks to the optional second arg. Full suite: **80 suites / 938 tests / 48 snapshots** green, up from 80/923/39 post-Task-08 (+15 tests / +9 snapshots, zero regressions). `tsc --noEmit` clean (14.5 s); ESLint clean on all 6 touched files. **Before / after example (en, 3 concerns):** old — `"You've mentioned a few concerns. Which one would you like to consult about first? We can address the others in a follow-up visit."`; new — `"You've mentioned a few concerns:\n\n**1.** Headache\n**2.** Diabetes follow-up\n**3.** Knee pain\n\nWhich one is the main reason for this visit? Reply **1**, **2**, or **3** — we can handle the rest in a follow-up."`. Out-of-range / single / 6+ concerns gracefully falls back to the legacy one-liner. Follow-up: staging smoke with a deliberately mixed-complaint opening message to verify the numbered list renders correctly on Instagram iOS and that replying `"2"` continues the booking flow for the correct concern.
- [x] 2026-04-18 — Plan 04 Task 08 shipped: `buildStaffReviewResolvedBookingMessage` + `StaffReviewResolvedKind` added to `backend/src/utils/dm-copy.ts`; the pre-existing ARM-05 entry point `formatStaffReviewResolvedContinueBookingDm` in `backend/src/utils/staff-service-review-dm.ts` (lines 52–70) now delegates to the new builder so both call sites — `service-staff-review-service.ts:431+` (staff confirm/reassign flow) and `service-match-learning-autobook.ts:162+` (learning-policy autobook) — pick up the new layout without any caller changes. Old copy: `"{intro} You can **pick a time and complete booking** here — tap to open: {url}\n\nIf something looks wrong, just reply here in this chat."` (URL hugs the end of a sentence — unreliable tap target on Instagram iOS; bold CTA verb buried mid-sentence rather than next to the link). New copy is a three-paragraph shape: (P1) kind-specific intro sentence unchanged — `"**{practice}** has confirmed your visit type: **{label}**."` / `"… has updated your visit type to **{label}**."` / `"… has applied your saved visit-type preference: **{label}**."`; (P2) `"Pick a time and complete your booking here:\n{url}"` with the URL isolated on its own line for a clean tap target; (P3) verbatim `"If something looks wrong, just reply here in this chat."` closer. Fallbacks preserved verbatim — empty/whitespace `practiceName` → `"the clinic"` (matches sibling `formatAwaitingStaffServiceConfirmationDm`), empty/whitespace `visitLabel` → `"your visit"`. `bookingUrl` is throw-guarded on empty/whitespace for symmetry with the Task-06 abandoned-reminder helper: every call site already computes the URL via `buildBookingPageUrl` upstream, so an empty value signals an upstream config bug (missing `BOOKING_PAGE_URL` env / bad conversation or doctor id) that we want surfaced, not a silent no-CTA DM. Sibling helpers in the same file (`formatAwaitingStaffServiceConfirmationDm`, `formatStaffServiceReviewStillPendingDm`) are intentionally left untouched per task scope — they don't have URL-hugging issues today. 5 new snapshots cover all 3 `kind` variants with practice+label present, plus the two fallback paths (practice missing → `"the clinic"`, visit label empty → `"your visit"`). 6 new unit tests in a dedicated `buildStaffReviewResolvedBookingMessage invariants` describe block assert: (1) throws on empty / whitespace `bookingUrl`; (2) URL sits on its own line below the `"Pick a time and complete your booking here:"` label across all three kinds; (3) `practiceName` missing/empty/whitespace all produce identical `"the clinic"` output; (4) `visitLabel` missing/empty/whitespace all produce identical `"your visit"` output; (5) each `kind` produces a distinct intro phrasing (`confirmed` / `updated` / `applied your saved visit-type preference`); (6) closing paragraph is `"If something looks wrong, just reply here in this chat."` and the legacy `"tap to open"` / `"pick a time and complete booking"` substrings are no longer present. Pre-existing tests in `staff-service-review-dm.test.ts` and `staff-service-review-dm-autobook.test.ts` only asserted on substrings (practice name, visit label, URL, `updated`, `confirm`, `saved visit-type preference`) — all still pass against the new three-paragraph layout without modification. Full suite: 80 suites / 908 tests / 39 snapshots green. `tsc --noEmit` clean; ESLint clean on all three touched files (`dm-copy.ts`, `staff-service-review-dm.ts`, `dm-copy.snap.test.ts`). Follow-up: manual staff-review resolution smoke in staging (or hit the service-match-learning autobook path with a repeat patient whose saved preference matches an active service) to visually confirm the URL renders on its own line in Instagram DM.
- [x] 2026-04-18 — Plan 04 Task 07 shipped: `buildCancelChoiceListMessage` + `appointmentConsultationTypeToLabel` + `formatAppointmentChoiceDate` added to `backend/src/utils/dm-copy.ts` and wired into both cancel branches of `backend/src/workers/instagram-dm-webhook-handler.ts` (the `upcoming.length === 1` ad-hoc string at line 2365 and the multi-upcoming pick-list at line 2380). Cancel pick-list is now adaptive: (a) 1 item → `"You have one upcoming appointment: **Tue, Apr 29 · 4:30 PM — Video consult**.\n\nReply **Yes** to cancel it, or tell me what else to do."`; (b) 2 items → header + bolded numbered list (`**1.** {date} — {modality}`) + `"Reply **1** or **2**."`; (c) N≥3 → header + bolded list + `"Reply a number from **1** to **N**."`. Each item renders the choice key in bold (`**1.**`) separated from content by a single space, and appends a patient-facing modality suffix after an em-dash (`— Video consult` / `— In-person` / `— Text consult` / `— Voice consult`) when `a.consultation_type` is a known enum (`'text' | 'voice' | 'video' | 'in_clinic'` per `backend/src/types/database.ts:115`); unknown/null enums omit the suffix rather than leak a raw token into the DM. Date format is en-US middot (`"Tue, Apr 29 · 4:30 PM"`) via the new `formatAppointmentChoiceDate(iso, tz)` helper that builds on `formatDateWithMiddot` — this deliberately diverges from the legacy `formatAppointmentStatusLine` (en-GB, day-before-month, lowercased AM/PM) so the cancel DM matches the Task-05 payment DM's date shape for visual consistency. Fixes three audit findings: (1) awkward `"Reply 1, 2, or 3"` trailer (now always grammatical at all N); (2) unbolded choice keys making numbers hard to scan; (3) no modality hint so patients with mixed types couldn't tell which row was the teleconsult vs the in-clinic visit. State transitions (`awaiting_cancel_confirmation` for single, `awaiting_cancel_choice` for multi, `pendingCancelAppointmentIds` mapping) are untouched — only the prompt copy changes. Reschedule branch at line 2422 intentionally left untouched per task scope (`"Reschedule list (parallel UI) is NOT in this task"`); a follow-up can reuse the same builder renamed to `buildAppointmentChoiceListMessage` when that ships. 4 new snapshots (1/2/3/5-item cases, last one mixes known and unknown modalities to prove the suffix-omission path) + 9 new unit tests (3 for the label helper: canonical map / case+whitespace normalization / undefined fallback for null/empty/`in_person` typo/`zoom`; 6 for the builder: empty-items throws / modality omission on undefined+empty+whitespace / 2-item trailer regex / N≥3 trailer regex / single-item branch has no numbered list or trailer-number / `**N.**` bolding + input-order preservation). No existing tests asserted on the old cancel strings (`webhook-worker-characterization.test.ts:550` only exercises the state transition after the user picks a number, not the prompt text). Full suite: 80 suites / 897 tests / 34 snapshots green. `tsc --noEmit` clean; ESLint clean on all three touched files. Follow-up: manual cancel-intent smoke in staging with 2 upcoming appointments to visually confirm bold numbers + `"Reply 1 or 2"` trailer + modality suffixes render correctly in Instagram DM.
- [x] 2026-04-18 — Plan 04 Task 06 shipped: `buildAbandonedBookingReminderMessage` added to `backend/src/utils/dm-copy.ts` and wired into `backend/src/services/abandoned-booking-reminder.ts:59`, replacing the inline one-liner `"Just checking in — your booking link is still active if you'd like to complete it. Reply anytime if you need help."`. New copy is a three-paragraph nudge: (a) `"Just checking in — your booking link is still active."` status line, (b) `"Pick a time here:\n{bookingUrl}"` with the URL on its own line and blank lines above/below so Instagram auto-linkifies it reliably (intentionally no markdown link — Instagram DMs don't render them), (c) `"Reply here anytime if you need help."` closing. The URL is regenerated per-reminder via `buildBookingPageUrl(row.id, row.doctor_id)` from `slot-selection-service.ts`, so the reminder points at the same conversation-scoped booking token as the original link — patient no longer has to scroll up through hours of DMs to find the tappable link. Builder throws on empty/whitespace `bookingUrl`; `buildBookingPageUrl` itself always returns a non-empty string (falls back to `https://example.com/book` when `BOOKING_PAGE_URL` unset), so the guard surfaces upstream config bugs rather than silently shipping a no-CTA reminder. Cron eligibility (`step === 'awaiting_slot_selection'`, `bookingLinkSentAt` past the 1-hour cutoff, `bookingReminderSent !== true`), token lookup, `bookingReminderSent` metadata write, and error counters are all untouched. 1 new snapshot + 4 new invariant unit tests (empty throws / whitespace throws / URL on own line with label above + blank line below / trims whitespace around URL). No existing test asserts on the old literal string (`abandoned-booking-reminder` has no dedicated test file) so no assertion fix-ups needed; post-swap grep for the old literal returns zero hits in `backend/src` + `backend/tests`. Full suite: 80 suites / 884 tests / 30 snapshots green. `tsc --noEmit` clean; ESLint clean on all three touched files. Follow-up: manual cron trigger in staging to visually confirm the reminder DM contains a tappable URL.
- [x] 2026-04-18 — Plan 04 Task 05 shipped: `buildPaymentConfirmationMessage` + `formatDateWithMiddot` added to `backend/src/utils/dm-copy.ts` and wired into `backend/src/services/notification-service.ts:180`, replacing the inline template literal `"Payment received. Your appointment on {dateStr} is confirmed.{idLine}\n\nWe'll send a reminder before your visit."` and its `idLine` helper. Patient now sees a scan-friendly four-paragraph DM: `✅ **Payment received.**` trust-signal header, `Your appointment is confirmed for **Tue, Apr 29 · 4:30 PM**.` date line (year dropped, date/time comma rewritten as a middle dot purely for readability — no timezone math touched), an optional `🆔 **Patient ID:** CLR-00123` + `_Save this for future bookings._` italic sub-line only when MRN is non-empty after trim, and a closing `We'll send a reminder before your visit. Reply here anytime if you need to reschedule or have questions.` that proactively invites follow-ups (this is the one place in the flow where we do that; payment DM often lands hours after the booking thread ended). The ✅ and 🆔 emojis are deliberately scoped to this single message family — no other DM in Plan 04 uses emoji. `formatDateWithMiddot` is regex-based, matches `Intl.DateTimeFormat('en-US', {weekday, month, day, year, hour, minute})` output, and falls back to the input unchanged if the shape doesn't match — never throws on locale drift. 3 new snapshot cases (with MRN / without MRN / MRN-with-whitespace) + 4 `formatDateWithMiddot` unit tests + 3 `buildPaymentConfirmationMessage` invariants (MRN omission on empty/whitespace, closing paragraph, exactly-one ✅/🆔). Existing `notification-service.test.ts` stringContaining('Payment received') assertion still matches the new copy. Full suite: 80 suites / 879 tests / 29 snapshots green. `tsc --noEmit` clean; ESLint clean on all three touched files. Follow-up: manual DM screenshot in staging for both with-MRN and no-MRN variants once deployed.
- [x] 2026-04-18 — Plan 04 Task 04 shipped: `buildConsentOptionalExtrasMessage` in `backend/src/utils/dm-copy.ts` replaces both consent-step ternaries at lines 1785–1786 and 3070–3071 of `backend/src/workers/instagram-dm-webhook-handler.ts`. Patient now sees three paragraphs instead of one wall of text — greeting line ("Thanks, **{name}**.\nWe'll use **{phone}** to confirm your appointment by call or text."), the optional-notes question ("Any notes for the doctor? _(allergies, current medicines, anything else — optional)_"), and a bolded CTA ("Reply **Yes** when you're ready to pick a time.") separated by blank lines. Someone-else variant is parallel: ("Thanks." / "Do I have your consent to use these details to schedule?" / "Reply **Yes** to continue.") and asks no notes question (parity-preserving — today the clinic only collects notes on self-bookings). Helper throws when `bookingForSomeoneElse && !bookingForName` (unreachable upstream state); the self branch treats empty / `'there'` sentinel `patientName` as "missing" and renders plain `"Thanks."` instead of the old `"Thanks, there."`. Upstream detector `isOptionalExtrasConsentPrompt` (in `booking-consent-context.ts`) gained a new primary clause for the new copy with previous "special notes" + "anything else" rules kept as backward-compat fallbacks; prompt rule at `ai-service.ts:1377` also refreshed. 6 snapshot cases + 4 builder-invariant unit tests + 1 new detector regression test; existing characterization regex `/special notes|allergies|Anything else|extras/i` still matches (new copy contains both "allergies" and "anything else"). Full suite: 80 suites / 869 tests / 26 snapshots green. `tsc --noEmit` clean; ESLint clean on all six touched files. Follow-up: manual DM screenshot in staging for both self + someone-else branches once deployed.
- [x] 2026-04-18 — Plan 04 Task 03 shipped: `buildIntakeRequestMessage` in `backend/src/utils/dm-copy.ts` replaces all 9 inline intake asks in `backend/src/workers/instagram-dm-webhook-handler.ts` (the "Please share: Full name, Age, Mobile, Reason for visit…" string family + the two `Still need: …` variants). Three variants — `initial` / `still-need` / `retry-not-received` — plus `forRelation`, `alreadyHaveReason`, `includeEmail`, and an `intro` escape hatch for call-site-specific framing (e.g. the two-person "I'll help you book for you and your mother…" header, the "Got it, just your mother then" switch, and the "I didn't catch the details for the person you're booking for" retry when the relation word is unknown). Canonical field-label map `INTAKE_FIELD_LABELS` replaces 3 copies of the local `labels` Record scattered across the webhook handler. `ai-service.ts` prompt literals at lines 423 + 2023 refreshed to reference the new bulleted structure so the LLM's self-check stays accurate; the LLM-input context string `Still need: …` at line 651 is intentionally left as-is (internal prompt, not patient-facing). Snapshot suite now has 20 cases (+12 intake) plus 2 new builder-invariant unit tests (empty-`missing` throws; 64-char `forRelation` clamps to 32 + lowercases). Verification: `rg "Please share: Full name" backend/src` → zero matches; `tsc --noEmit` clean; ESLint clean on all four touched files; full unit suite 858/858 (was 844 before Task 03). **Note:** at the two-person site (line 2451) the code flow collects the OTHER person's fields first, so the rendered copy says "your {relation} first, then you" — not "yours first, then theirs" as the task doc literally quoted. Matched code semantics over literal text per the task's parenthetical "preserves the two-step framing". Follow-up: manual DM screenshot in staging for each of the three variants × relation/no-relation permutations.
- [x] 2026-04-19 — **Bug-fix: "AI returned malformed JSON" on Review my catalog (Plan service-catalog-matcher-routing-v2 / Task 12 shipped).** Doctors hit the generic "AI returned malformed JSON. Please try again." error when running **Review my catalog** (and intermittently on `starter` AI autofill). Root cause was **silent token truncation**, not a model bug: `defaultRunAiSuggestLlm` in `backend/src/services/service-catalog-ai-suggest.ts` used `max_completion_tokens: 1500` for **all three modes** (`single_card`, `starter`, `review`), the `review` schema asks the LLM to emit a full `suggestedCard` (matcher_hints + 3 modalities + prices) per `gap` / `service_suggestion` issue and routinely wanted 3–5 starter cards on even a 2-row catalog → output exceeded 1500 tokens → response_format `json_object` cuts mid-string → `JSON.parse` threw → generic "malformed JSON" error reached the doctor. Fix: (1) replaced the global cap with a new exported `AI_SUGGEST_MAX_COMPLETION_TOKENS_BY_MODE: Record<AiSuggestMode, number> = { single_card: 1500, starter: 6000, review: 4000 }` sized per-mode for the schema each emits (rationale + tuning notes documented in JSDoc on the constant). (2) Added `mode: AiSuggestMode` to `AiSuggestRunLlmParams` and threaded it through the three dispatchers (`generateSingleCard` / `generateStarterCatalog` / `runLlmCatalogReview`) — keeps the runner pure, lets future test stubs assert on it. (3) Inspect `completion.choices[0].finish_reason` after the call: when it's `'length'`, log a structured `info` (not `warn` — expected boundary condition, not a defect) with `mode` + `maxCompletionTokens` + `totalTokens`, emit a new audit-log marker `service_catalog_ai_suggest_truncated` (distinct from the legacy `*_openai_error` catastrophic / `*_empty_completion` empty markers — lets a future Datadog query count truncations independently from real bugs), and throw `InternalError("AI response was cut short — the catalog or request is large for this mode. Please try again.")` so the doctor sees a specific, actionable copy instead of "malformed JSON". (4) Defended the SDK-error catch path so this `InternalError` is rethrown as-is rather than remapped to the generic `ServiceUnavailableError("AI suggestion service is unavailable. Please try again.")`. (5) **Optional one-shot retry deferred** — cap bump + truncation marker fully addresses the user-facing bug; retry doubles latency on borderline cases for marginal value. Will revisit only if telemetry shows `finish_reason='stop'` + invalid JSON in the wild (the existing `'malformed JSON'` `warn` log already pins that case). Coverage: 10 new unit tests in a `Task 12 — per-mode token budget + truncation handling` describe block in `backend/tests/unit/services/service-catalog-ai-suggest.test.ts` — pin the per-mode cap map; assert `chat.completions.create` receives `max_completion_tokens: 1500 / 6000 / 4000` for each mode; assert `finish_reason='length'` throws an `InternalError` containing `"cut short"` (not `"malformed JSON"`); assert the audit-log call carries `errorMessage: 'service_catalog_ai_suggest_truncated', tokens: 1500` and never the legacy markers; assert `finish_reason='stop'` + valid JSON success path is unchanged (no truncation marker); assert `finish_reason='stop'` + invalid JSON still surfaces the existing `"malformed JSON"` copy + does **not** log the truncation marker; assert empty completion preserves `*_empty_completion`; assert SDK throw preserves `*_openai_error` + `ServiceUnavailableError`. Tests mock `config/openai` at the module level (`mockChatCompletionsCreate = jest.fn(async (_args: unknown): Promise<unknown> => undefined)`) so the **real** `defaultRunAiSuggestLlm` runs against a programmable fake — that's the code we needed to verify; injecting `runLlm` would have bypassed it. **Backend regression: 1055/1055 tests across 82 suites green** (+10 vs Task 10 baseline of 1045/82), `tsc --noEmit` clean, `eslint src/services/service-catalog-ai-suggest.ts` clean. **No prompt-content change** in this task (prompt cleanup → `examples[]` lives in Task 11; staff feedback learner → `examples[]` lives in Task 13). Files: `backend/src/services/service-catalog-ai-suggest.ts` (~80 lines net add for the per-mode map + JSDoc + truncation branch + InternalError rethrow defense), `backend/tests/unit/services/service-catalog-ai-suggest.test.ts` (+~290 lines for the new describe block + module-level openai mock). Plan ledger updated (Task 12 row marked ✅). Task 11 (AI suggest prompts emit `examples[]`) and Task 13 (staff feedback learner writes `examples[]`) remain pending and **unblocked** by this change. Task: `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-12-ai-suggest-token-budget-and-truncation.md`. Follow-up: (a) **manual repro pending the user** — click `Review my catalog` on the same 2-card catalog that originally surfaced the bug, expect `issues[]` + no error; (b) **audit-log spot check** — temporarily lower `review` cap to 200 in dev, run review, confirm audit-log row has `errorMessage = 'service_catalog_ai_suggest_truncated'` and the doctor sees the new "cut short" copy, restore cap before merging; (c) telemetry (Datadog board / SQL query) on `errorMessage = 'service_catalog_ai_suggest_truncated'` so we can spot per-mode cap pressure within ~1 week of doctors using `Review my catalog` post-deploy.
- [ ] **Plan 02 follow-up — add `appointments.consultation_modality` column + populate it during the booking flow.** Surfaced 2026-04-19 by Plan 04 / Task 18 (`text-session-supabase-adapter.md`) — the new `consultation-pre-ping-job.ts` cron filters appointments by `consultation_modality = 'text'` to provision their `consultation_sessions` rows 5 min before scheduled start. The column does not exist today, so the cron is wired in but no-ops gracefully when Postgres returns `42703 undefined_column`. The cron only becomes useful once Plan 02 adds the column AND the booking flow writes the patient-chosen modality (`text | voice | video`) into it. Until then, text consultations either need (a) doctor manual launch via `POST /api/v1/consultation/start-text`, or (b) the future Plan 03 frontend launcher to call `consultation-session-service.createSession` directly. Tasks: (1) migration to add `consultation_modality consultation_modality_enum NULL` (or `TEXT NULL` if the enum doesn't exist yet) on `appointments`; (2) the booking flow (`appointment-service.ts` create paths + the new `confirm_details → consent → modality_pick` step in plan-02 Task 14 if it lands) writes the chosen modality; (3) backfill: existing video appointments → `'video'`, leave others NULL until next interaction. Once shipped, remove the `42703` fallback breadcrumb in `consultation-pre-ping-job.ts` and tighten the `CandidateAppointmentRow` type.

- [ ] **Bootstrap a live-Supabase test harness with `auth.jwt()` shim so RLS policies can be tested end-to-end in CI.** Surfaced 2026-04-19 by Plan 04 / Task 17 + Task 18 — both shipped *content-sanity* migration tests instead of integration tests because the repo has no Supabase test container with `auth.jwt()` (the function the new RLS policies key on for patient JWTs) shimmed in. Today's `tests/integration/` folder is stand-alone scripts run via `npx ts-node`, not a Jest harness. Without an integration harness we can't directly verify: (a) a patient JWT with `consult_role = 'patient'` + `session_id = X` can SELECT/INSERT on `consultation_messages` for session X but not session Y; (b) the `status = 'live'` guard actually blocks INSERTs after `endSession` flips status to `'ended'`; (c) the storage bucket RLS extends correctly when Plan 06 writes attachments. Manual smoke covers this for now (documented per task) but is not CI-enforced. Estimated effort: ~2 days to wire `supabase-js` against a Postgres + GoTrue test container in `docker-compose.test.yml`, port the migrations into the container start-up, and add a `tests/integration/rls/` jest project that mints JWTs with `jsonwebtoken` and runs the policies through the regular Supabase client. Won't block Plan 04, but is a load-bearing dependency for Plan 06 (attachments) and any future RLS work.

- [ ] **Promote `consultation-message-service.rateLimitInsertCheck` from in-memory to Redis when traffic warrants.** Surfaced 2026-04-19 by Plan 04 / Task 18 — current implementation uses a per-process `Map` with a sliding window (configurable via `CONSULTATION_MESSAGE_RATE_LIMIT_MAX` / `_WINDOW_SECONDS`, defaults 60 messages / 60 seconds). Acceptable for v1 because (a) backend pod count is low and (b) the rate limit's main purpose is anti-abuse / runaway-client defense, not strict per-second SLO enforcement. Two reasons to promote: (1) when scaling to N pods, the effective rate is `60 × N` per minute per (session, sender) which weakens the limit linearly with pod count; (2) when adding a separate worker process (e.g. the WhatsApp / SMS bridge) that also writes consultation_messages, the in-memory counter doesn't see those writes at all. Promotion path: replace the `Map` with a Redis `INCR` + `EXPIRE` (or a lua script for atomic sliding window). Trigger condition: pod count ≥ 4 OR observed message rate per (session, sender) reaches 30/min (50% of the v1 cap) for any session in production telemetry.

- [ ] **Plan 04 / Task 19 follow-up — wire `<TextConsultRoom>` into `<LiveConsultPanel>` once Plan 03 / Task 20 lands.** Patient-side text consult surface shipped 2026-04-19 (`<TextConsultRoom>` + `/c/text/[sessionId]` + scoped Supabase client + token-exchange helper). Doctor-side mount was deferred because `<LiveConsultPanel>` and `<ConsultationLauncher>` don't exist yet — Task 20 owns those. Once Task 20 lands, integration is small: (1) inside `<LiveConsultPanel>`'s text-modality branch, mount `<TextConsultRoom currentUserRole="doctor" sessionId={session.id} currentUserId={doctorAuthUid} accessToken={supabase.session.access_token} sessionStatus={session.status} counterpartyName={patient?.full_name ?? 'Patient'} onRequestTokenRefresh={async () => (await supabase.auth.refreshSession()).data.session!.access_token} />`; (2) inside `<ConsultationLauncher>`, the text-branch primary button (currently shows "Coming soon" toast per Task 20) should call `POST /api/v1/consultation/start-text` with `{ appointmentId }` to provision the session, then mount the panel. Cross-side smoke test (one doctor tab + one patient incognito) is the v1 acceptance gate.

- [ ] **Plan 04 / Task 19 follow-up — decide whether to surface a public-facing `doctors.full_name` for chat headers + the existing patient-fan-out surfaces.** Surfaced 2026-04-19 — Task 19 dropped the spec'd "Dr. {doctorName}" header and uses `doctor_settings.practice_name` only because no public-facing doctor name column exists today. Every other patient-facing fan-out (IG DM, SMS, email, booking confirmation) keys on `practice_name`, so the chat header staying consistent is fine for v1. If user testing surfaces "patients want to see the doctor's name in chat", the fix is a `doctor_settings.public_doctor_name` field (NOT `doctors.full_name` — that's the auth/billing identity, not the patient-facing one) populated by the doctor at onboarding, plumbed through every fan-out + the new `exchangeTextConsultTokenHandler` response, and consumed via the existing `<TextConsultRoom counterpartyName>` prop (no component change needed). Effort: ~half a day. Trigger: post-launch user feedback.

- [ ] **Plan 03 / Task 20 follow-up — wire the patient-side `<TextConsultRoom>` (Task 19) into `<ConsultationLauncher>`'s text branch.** Doctor-side launcher + slot-based `<LiveConsultPanel>` shipped 2026-04-19 with the text and voice primary CTAs short-circuiting to an inline "Coming soon" notice. To enable the doctor end-to-end for text consults: (1) inside `<ConsultationLauncher>`'s text branch (currently the `flashComingSoon('Text consultations…')` line in `handlePrimaryClick`), call `POST /api/v1/consultation/start-text` with `{ appointmentId }` to provision the `consultation_sessions` row + mint a doctor-scoped Supabase token; (2) on success, set `sessionId` + render `<LiveConsultPanel modality="text" sessionId={…} roomSlot={<TextConsultRoom currentUserRole="doctor" … />} />` exactly like the video branch does today with `<VideoRoom>`; (3) re-hydrate the in-memory session on page refresh by checking for an existing live `consultation_sessions` row (mirrors the existing video `useEffect` that fetches the doctor token off the persisted Twilio room SID). Cross-side smoke (one doctor tab + one patient incognito on `/c/text/[sessionId]`) is the acceptance gate. Effort: ~2 hours; depends on no further plan work — Tasks 17 / 18 / 19 already shipped the backend + patient-side glue.

- [ ] **Plan 03 / Task 20 follow-up — drop the `<AppointmentConsultationActions>` wrapper once nothing imports it.** Task 20 kept the file alive (now ~95 lines: launcher mount + the three preserved write surfaces — `<PreviousPrescriptions>`, `<PrescriptionForm>`, `<MarkCompletedForm>`) to avoid churning the page-level import in `frontend/app/dashboard/appointments/[id]/page.tsx`. The compromise was deliberate: the spec called for a one-line pass-through, but those three write surfaces were also living in the file and a literal pass-through would have regressed them. Once the prescription + mark-completed surfaces are extracted into their own page-level mount (or moved into a future `<PostConsultActions>` host), `<AppointmentConsultationActions>` becomes a true one-liner and can be deleted with a single import swap on the appointment detail page.

- [ ] **Frontend test harness — bootstrap jest + RTL + ts-jest before Plan 04 / Task 21 (doctor-side text consult experience) needs unit coverage.** Reiterated 2026-04-19 by Plan 03 / Task 20 (no test harness exists today — verification posture is tsc + next lint + manual smoke). Already on the inbox via Task 19's notes; promoted here so it's not lost. Trigger: any frontend task whose acceptance criteria reference `__tests__/` files (the next is `<TextConsultRoom>` doctor-side integration in the not-yet-written Plan 04 / Task 21).

- [ ] **Plan 07 / Task 31 follow-up — frontend tests for `<TextConsultRoom mode='readonly'>`.** Deferred per task spec — the frontend test harness still doesn't exist. Once Jest + RTL bootstrap lands (see existing inbox item), add the 5 cases the spec listed: (1) renders no composer, (2) renders watermark with formatted date, (3) does NOT call `client.channel(...)` (spy on the Supabase client constructor — readonly path must skip both the INSERT subscription and the presence channel), (4) renders system rows identically to live, (5) `onIncomingMessage` never fires in readonly even if a Realtime event is synthesized. Trigger: same harness gate as the existing inbox item.

- [ ] **Plan 07 / Task 31 follow-up — manual cross-side smoke pass.** End a text/voice/video consult → verify the patient receives the IG-DM (with SMS fallback if IG fails), verify tapping the link from a fresh device opens `<TextConsultRoom mode='readonly'>` with full history (text + attachments + system banners), composer absent, watermark dated. Verify the doctor's `/dashboard/appointments/[id]` page shows the "View conversation" button and clicking lands on the readonly room with the doctor's session. Verify a patient-scoped JWT cannot INSERT new messages (Migration 051's INSERT policy requires `status='live'`). Defer-blocked on Task 30 (notify wiring) + Task 36 (every modality has a chat thread) being deployed to staging.

- [ ] **Plan 07 / Task 31 follow-up — per-route rate-limiter for `/chat-history-token` + `/text-token`.** Both endpoints currently inherit only the global per-IP limiter (if any) — Task 31's spec listed a 429 test as acceptance but the test file documented "rate-limiting is out of scope; no dedicated middleware mounted in v1." If telemetry shows abuse (e.g. an attacker burning through HMAC verifications hoping to find a stale-but-valid one), add a per-route limiter (~30 req/min per IP) using the same pattern as the dashboard-events route limiter. Cheap to add; easy to forget. Captured here so it's not lost.

- [x] 2026-04-19 — **Plan 07 / Task 32 shipped: transcript PDF export.** `pdfkit@0.18.0` + `@types/pdfkit@0.17.6` installed (chosen per Notes #1 decision matrix — pure JS, streaming, no headless browser). Migration `068_consultation_transcripts_bucket.sql` provisions a private Supabase Storage bucket with an RLS `SELECT` policy that only allows session participants (`doctor_id = auth.uid()` OR `patient_id = auth.uid()`); no `INSERT/UPDATE/DELETE` policies because only the service role writes. New `transcript-pdf-composer.ts` (pure rendering, `compress: false` + `bufferPages: true` so footers can be drawn post-body on each buffered page, `mergeByTimestamp` merges `consultation_messages` with `consultation_transcripts` by creation-time with chat-wins-ties stability, `🎙` prefix on voice segments, "Confidential" watermark footer on every page, `HH:MM` in the doctor's timezone with `Asia/Kolkata` fallback) + `transcript-pdf-service.ts` (pipeline: input validation → `runReplayPolicyChecks` (reused from Task 29) → session-ended gate → `isSessionOrCompositionRevoked` → cache lookup (if `session.actual_ended_at < object.last_modified` hit, signed URL minted immediately) → compose+upload on miss → 15-min signed URL with `?download=transcript-{sessionId8}.pdf` → audit row with `artifact_kind='transcript'` → fire-and-forget mutual notification via extended `notifyPatientOfDoctorReplay` / `notifyDoctorOfPatientReplay` with new `actionKind: 'reviewed' | 'downloaded'` parameter). Handles both Whisper segment-timed transcripts and Deepgram flat-string transcripts (`loadVoiceTranscriptSegments`), falls through with a "Transcript not yet available" placeholder when voice transcription is still processing. `recording-access-service.ts` exports (`SessionContext`, `PipelineCheckOutput`, `runReplayPolicyChecks` [renamed from private `runPolicyChecks`], `isSessionOrCompositionRevoked` [wrapper around private `isRevoked`]) extracted to enable reuse without refactoring Task 29's flow — all 20 existing recording-access tests still pass. `dm-copy.ts#buildTranscriptDownloadedNotificationDm` authored with distinct wording vs. the audio-replay DM (patient sees "Your doctor at {practice} downloaded the written transcript of your consult on {date}" + audit reassurance). `notification-service.ts` threads `actionKind` through audit metadata + dashboard event payload so downstream analytics can differentiate "reviewed" (audio) from "downloaded" (transcript). Routes: `POST /api/v1/consultation/:sessionId/transcript-token` (HMAC exchange → 15-min patient-scoped JWT, mirrors Task 29's `replay-token` and Task 31's `chat-history-token`, accepts both `hmacToken` and legacy `token` body keys) + `GET /api/v1/consultation/:sessionId/transcript.pdf` (auth via `resolveReplayCaller`; **returns JSON** `{signedUrl, expiresAt, cacheHit, filename}` **not a 302 redirect** — rationale: browser navigations don't replay the Authorization header, so `location.assign(thisUrl)` would 401; letting the frontend navigate directly to the Supabase signed URL avoids the auth replay issue). Frontend `lib/api.ts` gains `TranscriptExportDenyReason` + `TranscriptDownloadData` types + `requestTranscriptToken` + `downloadTranscript` helpers (two-step: fetch JSON with Bearer → `window.location.assign(signedUrl)`). New `<TranscriptDownloadButton>` component with denial-code-aware empty-state copy (session-not-ended, beyond-self-serve-window [patient-specific "contact support" copy], revoked, not-a-participant) + `sessionLive` prop for call sites that know the consult is still active. Mounted in 4 places: (1) `<ConsultArtifactsPanel>` replaces the Task 32 placeholder card; (2) `/c/history/[sessionId]` patient chat-history page (passes the 90-day chat-history JWT — same `consult_role:'patient'` claim shape accepted by the transcript route); (3) `/dashboard/appointments/[id]/chat-history` doctor page (doctor's Supabase session JWT); (4) `/c/replay/[sessionId]` patient replay page (via `<ConsultArtifactsPanel>`). Coverage: 4 migration tests (idempotency / privacy / SELECT policy / no-write-policies) + 2 dm-copy snapshots + 7 composer tests (merge ordering, letterhead content, footer watermark, speaker labels, pending-transcription banner; custom `extractPdfText` helper handles pdfkit's hex encoding + `normalizeWs` normalizes kerning-fragmented words like "Dr . Shar ma" → "Dr.Sharma") + 10 service tests (cache hit, cache miss, doctor + patient notification routing, denial paths) + 14 route tests (token validation, 200 JSON response, error mapping 403/409/410, uncaught error propagation via `asyncHandler`). Backend verification: `tsc --noEmit` clean; targeted jest (`--testPathPattern "(transcript|notification-service|recording-access|dm-copy|consultation-transcripts-bucket)"`) = **23 suites / 336 tests / 66 snapshots** green, no regressions. Frontend verification: `tsc --noEmit` clean; `next lint` clean on all 5 touched files. **Known follow-ups (recorded in task-32 doc):** (a) Devanagari / non-Latin text renders as tofu with default Helvetica — embed Noto Sans on first user report; (b) no dedicated `doctors.name` column yet — composer uses `practice_name` as doctor display with `"Doctor"` fallback; (c) Deepgram v1 transcripts render as a single un-timestamped block until Deepgram v2 wiring ships; (d) no rate limiter on `GET /transcript.pdf` in v1 (cache fronts the heavy work, participant-only auth gates abuse surface) — revisit if cost pressure emerges.

- [ ] **Plan 07 / Task 31 follow-up — `signed_url_revocation`-style revocation surface for issued patient JWTs.** Today's only recourse for a leaked DM link is "wait for the 90-day TTL" or "rotate the session HMAC signing secret" (nuclear — invalidates every link for that session). If support workflows reveal real revocation requests (vs. just "patient lost the DM, re-mint"), add a `consultation_session_jwt_revocations(session_id, revoked_at, revoked_by)` table + a JTI claim on the patient JWT + a SELECT-policy check in Migration 052 that joins against the revocation table. Not v1 — Plan 02's `signed_url_revocation` pattern is the template when this becomes a real ask.

- [x] 2026-04-19 — **Plan 08 / Task 43 shipped: `recording-track-service.ts` — Twilio Video Recording Rules API wrapper (audio-only ↔ audio+video toggling, Plan 08 keystone).** Four-function service (`startAudioOnlyRecording` / `escalateToFullVideoRecording` / `revertToAudioOnlyRecording` / `getRecordingArtifactsForSession`) sitting on top of an extended `backend/src/services/twilio-recording-rules.ts` (added mode-level `getCurrentRecordingMode` / `setRecordingRulesToAudioOnly` / `setRecordingRulesToAudioAndVideo` + `TwilioRoomNotFoundError` alongside Task 28's kind-level pause/resume primitives, single shared file rather than forking into `adapters/`). Migration `071_recording_audit_action_video_values.sql` additively extends the `recording_audit_action` ENUM with exactly two new values — `video_recording_started` + `video_recording_reverted` — on purpose NOT six status-suffixed variants: Migration 064's ledger doctrine stores status in `metadata.status` JSONB (`attempted`/`completed`/`failed`), so status-suffixed action names would have introduced a parallel grammar. `startAudioOnlyRecording`'s system-baseline path reuses the pre-existing `recording_started` value. Actor resolution (`resolveActor(sessionId, initiatedBy)`) returns `SYSTEM_ACTOR_UUID` (all-zeros UUID, consistent with other system actors) for `system`, `doctor_id` for `doctor*`, `patient_id` for `patient*` with graceful fallback to system-actor if patient row doesn't yet exist (staff-booked consults). Every mutating call writes a double-row ledger (`attempted` → adapter → `completed`|`failed`) keyed by a single `correlation_id`; `tryInsertFailedRow` wraps the failure write so a DB outage during the error path never masks the original Twilio error. `getRecordingArtifactsForSession` delegates to a new `twilio-compositions.listCompositionsForRoom` helper, splits/sorts by `includeVideo`+`startedAt ASC`, and caches per-session in a 60s in-memory `Map` that's explicitly busted by every rule-flip on the same session (plus a `__resetArtifactCacheForTests()` hook for test isolation). Wiring: `consultation-session-service.createSession` invokes `startAudioOnlyRecording` after `persistSessionRow` for video sessions with a `providerSessionId`, wrapped in try/catch so baseline-ledger failures never block session creation. **Deliberate divergences from the original spec (documented in the task's implementation log):** (1) adapter lives under `services/` not `adapters/` (Task 28 already established that convention); (2) ENUM gets 2 values not 6 (status lives in metadata); (3) `revertToAudioOnlyRecording` does NOT delegate to `startAudioOnlyRecording` — both share the `setRecordingRulesToAudioOnly` adapter primitive but write their own distinct ledger rows, because delegation would stack two audit rows per revert and confuse analytics; (4) `recording-pause-service.resumeRecording` is intentionally NOT wired to `startAudioOnlyRecording` (the two operate at different abstraction levels — `resumeRecording` is kind-scoped, `startAudioOnlyRecording` is mode-scoped and would overwrite in-flight video); (5) voice `createSession` wiring skipped because `voice-session-twilio.createSession` already applies audio-only rules inline, and wiring both paths would double-ledger every voice session for zero benefit. Tests: `backend/tests/unit/services/recording-track-service.test.ts` (NEW, 21 cases — state machine, actor resolution per `initiatedBy`, failure path, input validation, cache hit/miss/bust on all three mutating paths); `consultation-session-service-recording-baseline-hook.test.ts` (NEW, 5 cases — wiring contract for video/voice/text/missing-roomSid/hook-throws paths, all green); `twilio-recording-rules.test.ts` (EXTENDED — mode-level helpers, short-circuit paths, 404 → `TwilioRoomNotFoundError`, non-404 → `InternalError`); `recording-audit-action-video-values-migration.test.ts` (NEW — content-sanity with comment-stripped regex to avoid false positives from commented-out reverse-migration guidance). Full backend verification: `tsc --noEmit` clean, **135 suites / 1746 tests / 66 snapshots** green, `ReadLints` clean on all 9 touched files. **Unblocks Task 40 (doctor escalation button), Task 41 (patient consent modal + escalation orchestration), Task 42 (patient revoke), Task 44 (replay video toggle)** — they now call into the service directly with no further plumbing. Files: `backend/src/services/recording-track-service.ts` (NEW), `backend/src/services/twilio-recording-rules.ts` (extended), `backend/src/services/twilio-compositions.ts` (extended with `listCompositionsForRoom` + `RoomCompositionSummary` + test override seam), `backend/src/services/consultation-session-service.ts#createSession` (video-only wiring), `backend/migrations/071_recording_audit_action_video_values.sql` (NEW), 4 test files (new + extended), task doc `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-43-recording-track-service-twilio-rules-wrapper.md` (Completed + implementation log), plan ledger `plan-08-video-recording-escalation.md` (Task 43 row marked ✅). Follow-ups captured below.

- [ ] **Plan 08 / Task 43 follow-up — Twilio sandbox integration harness.** Task 43 deferred the opt-in integration test (`recording-track-service-against-sandbox.test.ts` from the original spec) because the repo has no Twilio sandbox credentials wired into CI. Goal when it lands: create a real Twilio sandbox room, run `startAudioOnlyRecording → escalateToFullVideoRecording → revertToAudioOnlyRecording`, assert `getRecordingArtifactsForSession` returns ≥ 1 audio Composition + ≥ 1 video Composition after teardown. Natural home is Task 41's PR (patient consent modal + full escalation orchestration) — that task already needs end-to-end coverage across the doctor-request → patient-allow → Twilio-flip pipeline, and bundling the sandbox harness there keeps both tasks' CI signal honest. Until then, unit mocks cover the state machine + adapter boundary exhaustively.

- [ ] **Plan 08 / Task 43 follow-up — route `critical` structured-log signal to the on-call channel.** Every `failed` ledger write in `recording-track-service.ts` emits a `logger.error` with action + session id + original Twilio error attached. Plan 08 acceptance line 330 explicitly asked for "failures are detected + alarm fires (don't silently leave the room with mismatched recording rules)". In v1 the ledger row IS the audit signal (SQL: `SELECT COUNT(*) FROM consultation_recording_audit WHERE action IN ('video_recording_started','video_recording_reverted','recording_started') AND metadata->>'status' = 'failed'`), and the `logger.error` is the in-the-moment signal. Real alerting-pipe wiring (PagerDuty / Slack on-call / a per-failure counter metric like `recording_track_service_failures_total{action, reason}`) is a Plan 2.x telemetry concern — carry through once the observability infra lands. Not v1.

- [ ] **Plan 08 / Task 43 follow-up — promote `getRecordingArtifactsForSession` cache from in-memory Map to Redis.** Same reasoning as the consultation-message rate-limit inbox item (see the `Promote \`consultation-message-service.rateLimitInsertCheck\` from in-memory to Redis` entry). Current 60s per-session `Map` is scoped per backend pod; multiple pods will each maintain their own cache. Acceptable for v1 because the replay page re-fetches on navigation and cache-bust hooks fire locally only (any rule-flip happens in the same process that serves the subsequent artifact read for the same HTTP session flow). Trigger condition: backend pod count ≥ 4 OR a cross-pod write path that mutates recording rules (e.g. a webhook worker) lands. Promotion is straightforward — swap the `Map` for a Redis `SETEX {roomSid} 60 {JSON}` keyed by session id.

- [x] 2026-04-19 — **Plan 08 / Task 40 shipped (frontend-only; Task 41 server pending): doctor `<VideoEscalationButton>` + co-located reason-capture modal + 4-stage modal (`idle | requesting | declined | timedout`) + 5-variant button (`idle | loading | requesting | cooldown | locked:max_attempts`).** Three new modules — `frontend/lib/api/recording-escalation.ts` (typed `requestVideoEscalation()` + `getVideoEscalationState()` + `VideoEscalationError` class with code discriminants `RATE_LIMITED | SESSION_ENDED | NOT_A_PARTICIPANT | BAD_INPUT | NETWORK_ERROR | UNKNOWN`); `frontend/hooks/useVideoEscalationState.ts` (state FSM with initial GET fetch, 1Hz wall-clock countdown ticker, defensive local-expiry auto-transitions, Supabase Postgres-changes subscription on `video_escalation_audit` filtered by session id, optimistic markers `markRequesting`/`markCooldown`/`markLocked`, `formatMinuteSecond` helper); `frontend/components/consultation/VideoEscalationButton.tsx` (mounts the button + co-located modal with preset radios defaulting to `other`, 5..200 char free-text validation mirroring Migration 070's CHECK, AA-red error palette, `aria-label` + `aria-live` on the countdown, focus lands on textarea on open). Extended `frontend/components/consultation/VideoRoom.tsx` only — one import + one mount adjacent to `<RecordingControls>` inside the existing `recordingEnabled && recordingSessionId && recordingToken` gate; no prop surface change (button self-resolves the doctor's Supabase UID via `createClient().auth.getSession()`). **Deliberate divergences from the original spec (documented in task-40 implementation log):** (1) no `currentRecordingRule` prop cascade through `<ConsultationLauncher>` → `<LiveConsultPanel>` → `<VideoRoom>` — the hook derives the equivalent state from the audit-row stream (`kind=locked:already_recording_video`), eliminating three files of prop plumbing; (2) a single Supabase Postgres-changes channel replaces the spec's three-channel plan (`escalation:${requestId}` + `consultation-sessions:${sessionId}:recording_rule` + `video_escalation_audit:${sessionId}`) — same data, simpler lifecycle, works without Task 41 broadcasting anything beyond a DB write; (3) `doctorId` resolved internally rather than threaded through props, matching the existing `chatAuth` pattern; (4) `onStateChange` callback dropped in favour of hooking `useVideoEscalationState` directly from host components that need to observe state; (5) `VideoEscalationReasonModal.tsx` not created as a separate file per the spec's "implementer's choice" sanction — single file with four render stages; (6) Esc during the passive waiting view closes the modal (spec said "NOT during requesting"; relaxed because Close CTA is explicitly documented as safe — carried as UX polish follow-up); (7) `aria-live` countdown updates at 1Hz (spec wanted 10s debounce — screen readers typically self-throttle, and the debounce is best validated against real a11y feedback — carried as follow-up). Graceful degradation until Task 41 lands: `getVideoEscalationState` falls back to idle on 404/network error; POST surfaces "Couldn't send the request" inline error. Frontend verification: `npx tsc --noEmit` exit 0; `npx next lint --dir components/consultation --dir hooks --dir lib/api` exit 0 with zero warnings. **Tests deferred — frontend test harness still not bootstrapped** (see existing "Frontend test harness bootstrap" inbox entry; Task 40 piles onto that follow-up). Files: `frontend/lib/api/recording-escalation.ts` (NEW), `frontend/hooks/useVideoEscalationState.ts` (NEW), `frontend/components/consultation/VideoEscalationButton.tsx` (NEW), `frontend/components/consultation/VideoRoom.tsx` (extended — import + single mount), task doc updated with Completed status + implementation log, plan ledger `plan-08-video-recording-escalation.md` Task 40 row marked ✅. Follow-ups captured below.

- [ ] **Plan 08 / Task 40 follow-up — end-to-end smoke once Task 41 ships.** The button's POST currently 404s (Task 41's `/video-escalation/request` endpoint doesn't exist yet). When Task 41 lands: (1) open a video consult, click Start video recording, confirm the modal opens; (2) submit with a ≥5-char reason, confirm the modal transitions to the waiting view with a live countdown that ticks down from 60s; (3) have the patient tab click Decline in the consent modal — confirm the doctor's modal flips to the decline banner, the button shows cooldown with a 5-min countdown, and the hook's Supabase Postgres-changes subscription fires (RLS allowlist verification in dev); (4) wait out the cooldown, confirm the button auto-re-enables to idle with "1 request left" copy; (5) submit again, have the patient Allow — confirm the button hides (Task 42 indicator takes over); (6) after 2 cycles without an Allow, confirm the button permanently disables with the `max_attempts` tooltip.

- [ ] **Plan 08 / Task 40 follow-up — extract `frontend/lib/realtime-consultation-channels.ts` once Task 42 lands.** Today the Supabase Postgres-changes subscription on `video_escalation_audit` lives inline in `useVideoEscalationState.ts`. When Task 42's `<VideoRecordingIndicator>` subscribes to the same audit-row stream, pulling the channel-config + payload-parsing helpers into a shared `frontend/lib/realtime-consultation-channels.ts` module will avoid duplicated subscribe/cleanup boilerplate. Small refactor; trigger on Task 42's implementation PR.

- [ ] **Plan 08 / Task 40 follow-up — dark-mode + mobile visual QA pass.** Four modal stages (`idle | requesting | declined | timedout`) × five button variants (`idle | loading | requesting | cooldown | locked:max_attempts`) × two themes (light/dark) × two viewports (desktop side-panel / mobile tab-switcher) = 40 visual states. Today verified via `tsc` + lint; pixel validation is deferred to the first integration PR that opens the button end-to-end (post Task 41). Flag colour-contrast regressions + overflow of the `Try again in M:SS` label on narrow mobile viewports.

- [ ] **Plan 08 / Task 40 follow-up — decide on aria-live debounce granularity for the countdown.** Spec asked for 10s granularity on the assistive-tech announcement of "N seconds remaining". Shipped version emits the polite region update at 1Hz (browsers + screen readers typically self-throttle these). Validate with real AT user feedback; if the 1Hz cadence feels chatty, add a simple `Math.floor(remaining / 10) * 10` bucket + only re-render the text when the bucket changes.

- [ ] **Plan 09 / Task 51 follow-up — extend `GET /modality-change/state` to include a `pricing` block.** Task 51's `frontend/lib/modality-pricing-display.ts#fetchModalityPricing()` is wired to consume a `pricing: { fees, upgradeDeltaPaiseFromCurrent, downgradeDeltaPaiseFromCurrent }` slice on the state response but the backend doesn't emit it yet (Task 47's state endpoint only returns `currentModality`, `upgradeCount`, `downgradeCount`, `activePendingRequest`). Today the three doctor modals (`<ModalityUpgradeApprovalModal>`, `<DoctorUpgradeInitiationModal>`, `<ModalityDowngradeModal>`) take `deltaPaise` / `refundAmountPaise` as props — the launcher (Task 54) will compute them by calling into Task 49's pricing service directly via a new helper endpoint or prop threading. Cleaner end-state: `fetchModalityPricing` returns the pricing block, launcher memoises it once per session, and every modal reads from that single source of truth. Effort: ~2 hours backend (extend `modalityChangeController.getState` + `modality-change-service.getState`) + ~30 min frontend (launcher swap). Blocks nothing — v1 prop-threaded pricing works.

- [ ] **Plan 09 / Task 51 follow-up — surface `refundStatus` on doctor-initiated downgrade success.** `<ModalityDowngradeModal>` currently always displays "Refund is processing" because Task 47's `applied` result doesn't carry a discriminant for `refund_initiated` vs `refund_queued` vs `refund_pending_retry`. The modal has the copy scaffold wired (`phase.refundPending ? "Refund is pending…"` branch) but always receives `refundPending: false`. Plumbing: add a boolean `refundInitiated: boolean` to `ModalityChangeApplied` when `billingAction === 'auto_refund_downgrade'` (true on sync-success, false when the refund got queued for retry), and tighten the modal copy accordingly. Task 49's billing service already classifies these cases internally — only the result shape needs widening. Effort: ~1 hour.

- [ ] **Plan 09 / Task 51 follow-up — frontend component tests for the three doctor modals + reason capture.** Deferred per existing frontend-test-harness inbox item. When the harness lands, cover: (1) `validateModalityReason()` across all four variants (doctor_upgrade / doctor_downgrade / doctor_decline / patient_downgrade) × (empty / too-short / at-bounds / too-long / preset-only / preset+text); (2) `<ModalityUpgradeApprovalModal>` state machine (idle → paid/free/decline → submitting → success → close; idle → expired on countdown; decline-form validation gating submit); (3) `<DoctorUpgradeInitiationModal>` form → awaiting_consent → applied/declined/timeout transitions; (4) `<ModalityDowngradeModal>` happy-path + rejected paths; (5) `useDoctorPendingUpgradeApproval` Realtime subscription bind / unbind + terminal-UPDATE clearing; (6) the reason-capture component's `need_to_hear_voice` preset visibility guard on `currentModality='voice'`.

- [ ] **Plan 09 / Task 51 follow-up — Realtime subscription deduplication across the doctor room's three modals.** Task 51's `<ModalityUpgradeApprovalModal>` (via `useDoctorPendingUpgradeApproval`) and `<DoctorUpgradeInitiationModal>` each create their own `postgres_changes` subscription on `modality_change_pending_requests` filtered by `session_id`. When both modals mount simultaneously (launcher has downgrade modal open, a patient-upgrade request arrives), the room carries two parallel subscriptions to the same stream with different filters (INSERT vs UPDATE event interest). Supabase doesn't collapse them, so the backend sees 2× channel traffic per doctor. Trigger: instrument channel-count metrics post-launch; if the doctor room routinely holds > 1 modality-change channel, hoist the subscription into `<ModalityChangeLauncher>` (Task 54) or a shared `useModalityChangeRealtime(sessionId)` hook that fans out to multiple consumers. **Task 52 update:** applies equally to the patient-side — `usePatientPendingUpgradeConsent` (Task 52) + the patient-launched `useModalityUpgradeFSM` (Task 50) each open their own channel. Dedup should cover both sides.

- [ ] **Plan 09 / Task 52 follow-up — wait for `consultation_modality_history` INSERT confirmation before firing `onAccepted`.** `<PatientUpgradeConsentModal>` currently fires `onAccepted({ toModality })` on the HTTP 200 from `POST /modality-change/patient-consent` because Task 47's state machine applies synchronously on `decision='allow'`. Task 52 Notes §10 recommended firing only after the Realtime `{ kind: 'applied' }` event so the success-state never shows before the room is actually swapped. Safe in v1 because the HTTP response already carries `{ kind: 'applied' }` (state machine is synchronous on this path). Tighten if a future async path is added (e.g. webhook-driven commit) — subscribe to `consultation_modality_history` INSERT filtered by `session_id`, dispatch `onAccepted` only after the row arrives, with a 15s local safety-net that surfaces an inline error if the INSERT never fires.

- [ ] **Plan 09 / Task 52 follow-up — patient-side frontend component tests.** Deferred with Tasks 50 / 51 per existing frontend-test-harness inbox item. When the harness lands, cover: (1) `<PatientUpgradeConsentModal>` auto-opens on `pending_patient_consent` INSERT event; (2) ESC does NOT close (consent); (3) `[Decline]` posts `decision:'decline'` + fires `onDeclined`; (4) `[Allow]` posts `decision:'allow'` + fires `onAccepted({ toModality })` once it sees `applied`; (5) countdown syncs to `expiresAt`; (6) local expiry auto-transitions to `expired` + fires `onTimeout` exactly once; (7) `<PatientDowngradeModal>` submit with empty reason succeeds (optional reason); (8) `[Cancel]` / ESC / backdrop-click dismiss the downgrade modal; (9) `targetModality === currentModality` renders the inline no-op error without a server call; (10) `usePatientPendingUpgradeConsent` terminal UPDATE clears the pending row.

- [ ] **Plan 09 / Task 52 follow-up — patient-side `role="alertdialog"` visual / a11y QA pass.** Four phases on the consent modal (`idle | submitting_allow/decline | applying | expired | error`) × two themes (light/dark) × two viewports (desktop centered / mobile full-screen) = 32 visual states. Today verified via `tsc` + lint; pixel + screen-reader validation is deferred to the first integration PR that opens the modal end-to-end (post Task 54 mount). Verify: countdown pulse respects `prefers-reduced-motion`; `aria-live="assertive"` actually announces in NVDA / VoiceOver at 1Hz (consider Task 40's debounce follow-up here too — same tension); focus-trap correctness when the only two focusable elements are `[Decline]` / `[Allow]`; high-contrast mode (Windows High Contrast / `forced-colors`) renders both CTAs with preserved borders.

## Inbox

<!-- Add `- [ ] Your item` below -->
Business related
[] start brand building insta post stories 
[] look for comments in docs post see what kind of comments there usually are present  find possible solutions

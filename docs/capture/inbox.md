# Inbox

Add new items **below** this line (or at the top of the list—stay consistent). Uncheck ` [ ] ` until promoted or done.

---
- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 10 (Phase 4, hybrid landing) — plan now Done end-to-end:** Dev-flag-gated "Try as patient" preview shipped as the first slice of Phase 4 (the plan explicitly invited this — "may be **dev-only** first"). Endpoint: `POST /api/v1/catalog/preview-match` registered in `backend/src/routes/api/v1/catalog.ts` **only** when `resolveCatalogPreviewMatchEnabled({ flag, nodeEnv })` returns true (defaults to enabled for `NODE_ENV !== 'production'`; can be force-on via `CATALOG_PREVIEW_MATCH_ENABLED=true` for staging or force-off via `=false`). When disabled, the route is **not mounted at all** so production traffic gets a clean 404 instead of a 403, which matches Express conventions and avoids leaking the route's existence. Auth: `authenticateToken` (doctor scope) regardless of flag — same posture as `ai-suggest`. Body schema (strict): `{ catalog: ServiceCatalogV1, reasonForVisitText: string (1..2000, trimmed), recentUserMessages?: string[] (max 8 × 2000), doctorProfile?: { practiceName?, specialty? } | null }` validated via `serviceCatalogV1BaseSchema` (no catch-all enforcement on purpose — an in-progress draft missing the catch-all is exactly the kind of state a doctor would want to preview, and the matcher already handles a missing catch-all gracefully via first-row fallback). Behavior: **single** call to `matchServiceCatalogOffering` (not two — `result.source` already distinguishes Stage A / Stage B / fallback unambiguously) → translated via the new pure exported helper `summarizePreviewMatchResult(result, label, llmAvailable)` into `{ path, matchedServiceKey, matchedLabel, suggestedModality, confidence, autoFinalize, mixedComplaints, reasonCodes, llmAvailable }` where `path: 'stage_a' | 'stage_b' | 'fallback' | 'single_fee'`. Translation rules: `source==='llm'` → `stage_b`; `source==='fallback'` → `fallback`; `source==='deterministic'` + `SINGLE_FEE_MODE` reason code → `single_fee`; otherwise `stage_a`. `llmAvailable` is set from `Boolean(getOpenAIClient())` so the UI can warn the doctor if a `fallback` simply means "no OPENAI_API_KEY in this env" rather than "no semantic match exists." PHI handling: the matcher already calls `redactPhiForAI` on `reasonForVisitText` before any LLM hop and never logs the raw text — the new route adds **zero new logging surface**. Frontend: new component `frontend/components/practice-setup/CatalogPreviewMatchPanel.tsx` (textarea + "Run preview" button + result card with colored Stage badge, confidence, suggested modality, matched label/key, conditional `!llmAvailable` warning, conditional `mixedComplaints` line) + new helpers `postCatalogPreviewMatch` / `PreviewMatchRequest` / `PreviewMatchResponse` / `PreviewMatchPath` in `frontend/lib/api.ts` (404 from backend → "Preview is not enabled on this backend (set CATALOG_PREVIEW_MATCH_ENABLED=true)" so flag-mismatch is debuggable without DevTools). Mounted in `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx` **only** in `multi_service` mode (single-fee mode has nothing to route) and **only** when `process.env.NEXT_PUBLIC_CATALOG_PREVIEW_MATCH_ENABLED === 'true'` OR `process.env.NODE_ENV === 'development'` — both are inlined at build time so production builds without the flag don't ship the panel at all. The panel runs against the **current draft catalog** (mirrors the existing `runServerReview` pipeline: `draftsToCatalogOrNull` → `safeParseServiceCatalogV1`; if invalid → render disabled), so doctors preview their **on-screen unsaved edits**, not the persisted DB row — which is exactly what makes the preview useful during catalog construction. `previewDoctorProfile` derived from `settings.practice_name` + `settings.specialty` so the LLM gets specialty context in Stage B. **Locked product decisions** (documented in Task 10's Decision log): (1) telemetry — misroute → suggested example phrases — is **explicitly deferred** with un-defer triggers (graduate to telemetry only after the Phase 3 sibling overlap rule has 2+ weeks of shadow data so suggestions aren't seeded by guesses); (2) in-DM-style transcript preview is **explicitly deferred** — DM copy has its own test surface (Plan 04 — Patient DM copy polish) and conflating the two would create a brittle integration test; (3) production-grade RBAC + audit log + rate-limiter hardening is **explicitly out of scope** — the route is dev-flag-gated and admits this. The deliberate decision was: **ship the high-leverage half (preview) in ~2 hours instead of the full 12–24 h slice (preview + telemetry + production hardening) that has no measured signal demanding it yet**. Same pattern as Task 09's hybrid landing. New tests: 16 cases in **new file** `backend/tests/unit/routes/catalog-preview-match.test.ts` (first test under `tests/unit/routes/`) across 3 describe blocks: `previewMatchRequestSchema` (6 — accepts minimal valid payload, accepts optional fields, rejects empty `reasonForVisitText` after trim, rejects missing `catalog`, rejects unknown top-level keys via strict, caps `recentUserMessages` at 8); `summarizePreviewMatchResult — source → path translation` (7 — every `source` × `reasonCodes` combo pinned: `llm→stage_b`, `fallback→fallback`, `deterministic` no-SINGLE_FEE_MODE→`stage_a`, `deterministic`+SINGLE_FEE_MODE→`single_fee`, `llmAvailable=false` propagation on fallback so UI can warn, missing `suggestedModality`→null coercion, `mixedComplaints` passthrough); `resolveCatalogPreviewMatchEnabled — env gating` (3 — `flag=true` always on, `flag=false` always off, `flag=undefined` (auto) on for dev/test off for production). Test pattern note: kept the gating helper **pure** (takes `{ flag, nodeEnv }` rather than reading `env` directly) so unit tests can pin every combination without re-parsing `process.env` — same pattern used for `summarizePreviewMatchResult`. Verification: backend `tsc --noEmit` clean; frontend `tsc --noEmit` clean; `jest tests/unit/routes/catalog-preview-match.test.ts` 16/16; full backend suite **1045/1045 across 82 suites** (+16 vs Task 09's 1029/81), 57/57 snapshots intact; `eslint src/routes/api/v1/catalog.ts src/config/env.ts` clean; `eslint components/practice-setup/CatalogPreviewMatchPanel.tsx app/dashboard/settings/practice-setup/services-catalog/page.tsx lib/api.ts` (frontend) clean. **No matcher logic change** so the Stage A `dm-routing-golden` corpus baseline holds without re-snapshotting. Files touched: `backend/src/config/env.ts` (new `CATALOG_PREVIEW_MATCH_ENABLED` knob with `'auto'` semantics), `backend/src/routes/api/v1/catalog.ts` (new route + 3 exported helpers), `backend/tests/unit/routes/catalog-preview-match.test.ts` (new file, 16 tests), `frontend/lib/api.ts` (new helper + 3 types), `frontend/components/practice-setup/CatalogPreviewMatchPanel.tsx` (new file), `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx` (gated mount + memoized `previewCatalog` / `previewDoctorProfile`), `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-10-preview-widget-stage-a-vs-b.md` (Done — hybrid + Decision log + Shipped section), `docs/Development/Daily-plans/April 2026/19-04-2026/Plans/plan-service-catalog-matcher-routing-v2.md` (Phase 4 acceptance ticked + status footer expanded — **plan now Done end-to-end with hybrid landings on Phases 3 + 4**), `docs/Development/service-catalog-matching-stages.md` (code map gains preview-widget row, Last-updated bumped). Plan status: **all 4 phases done** (0 + 1 + 2 + 3 + 4); deferred items (sibling per-row schema fields, preview telemetry, in-DM transcript preview, production hardening) carry explicit un-defer triggers in their respective task docs.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 09 (Phase 3, hybrid landing):** Sibling boundaries shipped as **prompt-only**; the per-row schema half (`confused_with_service_keys`, `prefer_other_when`) is **explicitly deferred with rationale** in both the task doc's Decision log and the plan's Phase 3 acceptance bullet. Decision rationale (priority order): (1) philosophical fit — adding a doctor-facing field that competes with `examples` for attention contradicts the "doctors fill examples, AI handles disambiguation" rule from `AI_BOT_BUILDING_PHILOSOPHY.md`; (2) the LLM is not blind today — `buildAllowlistPromptLines` already serializes every sibling row's resolved `keywords=` / `include_when=` / `exclude_when=` / `[scope]` for Stage B, so misroutes are a prompt-engineering problem, not an information-availability problem; (3) reversibility — schema changes are forever (migration, Zod, frontend, validation, doctor education), prompt rules tune in one PR; (4) no telemetry yet — building schema for an unmeasured problem is the exact pattern the user has pushed back on. Un-defer triggers documented: (a) shadow telemetry from the Apr 12 learning system shows a recurring sibling-pair misroute the prompt rule cannot fix, or (b) doctors organically ask for explicit "send to the other row" controls. Two changes in `backend/src/services/service-catalog-matcher.ts`: (1) **new rule 6 — Sibling tie-breaker** added to `buildServiceCatalogLlmSystemPrompt` that tells the LLM "prefer the row whose hints contain the more specific phrase the patient text matched verbatim → on equal specificity prefer `[scope: strict]` over `[scope: flexible]` → if still tied pick `'other'` rather than guessing → never split the difference by routing a sibling-overlapping complaint to a third unrelated row." Old rules 6 (specialty defaults) → 7 and 7 (use "other") → 8 with rule 8's "rules 1–6" reference updated to "rules 1–7"; (2) new exported helper `detectSiblingExampleOverlaps(catalog)` + internal renderer `buildSiblingDisambiguationBlock(catalog)` that read through `resolveMatcherRouting` (so v2 `examples` and legacy `keywords` CSV feed the same path), tokenize each phrase (lowercase, ≥ 4 chars, conservative stop-word filter — `today`, `tomorrow`, `patient`, `doctor`, `service`, etc.), exclude the catch-all row (`other`), require a token in 2+ rows, and cap at 5 deterministic entries (token asc, service_keys per token asc). When overlap exists, a `Disambiguation hints (rows whose example phrases share a token — apply the sibling tie-breaker rule, pick by the patient's most specific cue):` block is injected between the JSON schema and the allowlist listing each shared token and the rows that contain it. When no overlap, the block is omitted entirely so clean catalogs stay clean. **Critical edge case pinned in tests**: legacy `include_when`-only rows do NOT synthesize false-positive overlap — the resolver returns `examplePhrases=[]` for those, so the detector ignores them (matches the documented asymmetry: `include_when` is a gate, never a positive signal). New `Phase 3 sibling tie-breaker + disambiguation hints (Routing v2, Plan 19-04, Task 09)` describe block in `service-catalog-matcher.test.ts` adds 10 named cases: shared-token detection with sorted output, catch-all exclusion (even when the catch-all carries matching examples), no-overlap → empty, legacy `include_when`-only → empty, stop-word + short-token exclusion (`appointment` surfaced; `the`/`for`/`today`/`tomorrow` filtered), deterministic 5-entry cap with token-asc ordering, block injection on overlap (anchored on the unique parenthetical block-only header `Disambiguation hints (rows whose…` to disambiguate from the rule-6 mention in prose), block omission on no-overlap (same anchor), rule renumbering (rule 6 = sibling tie-breaker, rule 7 = specialty, rule 8 = use "other" w/ "rules 1–7" reference), and block positioning between Schema and Allowlist. **Verification**: `tsc --noEmit` clean; `jest tests/unit/services/service-catalog-matcher.test.ts` 51/51 pass (41 pre-existing + 10 new); full backend suite **1029/1029 pass across 81 suites**, 57/57 snapshots intact (+10 vs Task 08's 1019); `eslint src/services/service-catalog-matcher.ts` 0 errors (1 pre-existing warning at line 549, unrelated). **No Stage A behavior change** so the `dm-routing-golden` corpus baseline holds without re-snapshotting. Files touched: `backend/src/services/service-catalog-matcher.ts`, `backend/tests/unit/services/service-catalog-matcher.test.ts`, `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-09-sibling-boundary-schema-prompt.md` (marked Done with full Decision log + Shipped section), `docs/Development/Daily-plans/April 2026/19-04-2026/Plans/plan-service-catalog-matcher-routing-v2.md` (Phase 3 acceptance ticked as hybrid + status footer updated), `docs/Development/service-catalog-matching-stages.md` (Stage B summary gains Phase 3 bullet, code map gains sibling-disambiguation row, Last-updated bumped). Plan status: Phase 0 + 1 + 2 + 3 done. Only Phase 4 (preview / telemetry — flagged optional) remains.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 08 (Phase 2):** Strict / flexible × resolved-hints behavior matrix pinned in three places that must move together so any regression points the reviewer at one named test: (1) JSDoc on `runDeterministicServiceCatalogMatchStageA` in `backend/src/utils/service-catalog-deterministic-match.ts` carries the full 9-cell table with cell IDs `A1..C3`; (2) `docs/Development/service-catalog-matching-stages.md` gains a new "Strict / flexible × resolved hints — Stage A behavior matrix (Phase 2 / Task 08)" section before the Stage B summary, with the matrix, a "reading the matrix" key, and the deferred "Prefer assistant matching" flag noted as the future flag-on column; (3) `backend/tests/unit/utils/service-catalog-deterministic-match.test.ts` gets a new `Phase 2 matrix (Routing v2, Plan 19-04, Task 08)` describe block with one named test per cell — every test forces the multi-row Stage A code path on `catalogNcdGpOther()` so cells exercise the same branch the production matcher takes (instead of accidentally hitting the "single non-catch offering" fast-path shortcut). Cells covered: A1/B1 (`examples` + hit → `medium`, `autoFinalize=false`), A2/B2 (`examples` + no overlap → `null` → Stage B), A3 (`examples` + `exclude_when` overlap → `null`, red flag wins), B3 (flexible + label-only → `high`, `autoFinalize=true`), C1 (strict + label-only → `medium` downgrade), C2 (strict + legacy `include_when` only → `null`, no example-phrase corroboration), C3 (flexible + legacy `include_when` only with no examples / no label hit → `null`, `legacy_merge` yields 0 score). Pure documentation + regression tests; **no Stage A behavior change** (Task 04 already lifted the strict-corroboration check to read `ResolvedRoutingHints.examplePhrases`). What this protects against: re-introducing "include_when alone is enough corroboration on strict" trips C2; dropping the strict label-only downgrade trips C1 (matches B3); silently ignoring `exclude_when` when `examples` is set trips A3; letting strict `examples` hit auto-finalize trips A1 (auto-finalize is reserved for the label fast path and even there strict downgrades it). Out of scope (deferred): "Prefer assistant matching" product flag — when/if added, the matrix becomes the flag-off default and a flag-on column gets added that returns `null` for cells A1/B1 (forcing Stage B); doc already calls this out so the next implementer doesn't have to rediscover it. Verification: `npx tsc --noEmit` (backend) clean; `npx jest tests/unit/utils/service-catalog-deterministic-match.test.ts` 30/30 pass (21 pre-existing + 9 new matrix cells); `npx jest` (full backend suite) **1019/1019 / 57 snapshots** across 81 suites (+9 vs Task 07's 1010); `npx eslint src/utils/service-catalog-deterministic-match.ts` clean. Files touched: `backend/src/utils/service-catalog-deterministic-match.ts`, `backend/tests/unit/utils/service-catalog-deterministic-match.test.ts`, `docs/Development/service-catalog-matching-stages.md`, `docs/Development/Daily-plans/April 2026/19-04-2026/Tasks/task-08-strict-flexible-deterministic-matrix.md`, `docs/Development/Daily-plans/April 2026/19-04-2026/Plans/plan-service-catalog-matcher-routing-v2.md`. Plan Phase 1 + Phase 2 acceptance checkboxes both ticked; only Phase 3 (sibling boundaries) and Phase 4 (preview / telemetry) remain — both flagged as optional in the plan.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 07 (Phase 1.5):** Doctor-facing migration UX shipped — un-migrated rows no longer have an "empty" matcher UI. Per-card change in `frontend/components/practice-setup/ServiceOfferingDetailDrawer.tsx`: the Task 06 `<details>` disclosure is replaced by an **always-visible** amber callout (`data-testid="drawer-legacy-hints-migration-callout"`) that explains routing keeps working via the resolver, exposes the editable Keywords / Book-this-when textareas in-place, and adds a one-tap **Convert to example phrases** button (`data-testid="drawer-convert-legacy-hints"`) backed by the new pure helper `convertLegacyHintsToExamples` in `frontend/lib/service-catalog-drafts.ts` (splits `matcherKeywords` on `[,;\n]+`, `matcherIncludeWhen` on `\r?\n`, merges with any existing `matcherExamples`, runs through `normalizeMatcherExamplesDraft` for trim/dedupe/clamp to 24 × 120 chars, zeros both legacy fields). Catalog-level addition in `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`: dismissible amber banner above the multi-service editor (`data-testid="catalog-routing-v2-migration-banner"`) with pluralized count copy ("1 service…" / "N services…") + Dismiss button — dismissal persists per-browser in `localStorage` (`clariva.routing-v2-migration-banner.dismissed`); banner auto-hides when the legacy-only count hits 0, and the `multi_service` JSX gating means single-fee doctors never see it. `hasUnmigratedLegacyHints` is now exported from the drawer module so the page banner and per-card callout share a **single precedence rule** mirroring the backend resolver. **Locked product decision** (documented in Task 07 doc): when a row's `matcherExamples` becomes non-empty, `draftsToCatalogOrNull` writes `matcher_hints.examples` (+ `exclude_when`) and **drops** legacy `keywords` / `include_when` — no audit-only retention; the always-visible callout means the doctor sees legacy values *before* converting so nothing meaningful is silently cleared. localStorage failure modes (private mode / quota) wrapped in try/catch on both read + write paths, fail open (banner shows). Verification: frontend `tsc --noEmit` clean; `next lint --dir lib --dir components/practice-setup --dir app/dashboard/settings/practice-setup` clean; backend untouched (1010/1010 baseline holds). Manual QA checklist in PR (drawer convert flow, mixed/v2 rows skip the callout, banner dismiss persists, per-browser scope, single-fee mode hides banner). Files touched: `frontend/lib/service-catalog-drafts.ts`, `frontend/components/practice-setup/ServiceOfferingDetailDrawer.tsx`, `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`. Plan + task marked done; `service-catalog-matching-stages.md` code map gains Catalog-banner row + Task 07 narrative + Last-updated line refreshed.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 06 (Phase 1.4):** Practice Setup editor now exposes **Example phrases** as the primary doctor-facing matcher input, with `ServiceOfferingDraft.matcherExamples: string[]` round-tripping through `offeringToDraft` / `draftsToCatalogOrNull` / `aiSuggestedCardToDraft` / `applyAiSuggestionToDraft`. Save-side contract is **single-source-of-truth**: when a row has at least one example phrase, `draftsToCatalogOrNull` writes `matcher_hints.examples` (+ `exclude_when`) and **drops** legacy `keywords` / `include_when` — no silent dual-write, fully aligned with the resolver's precedence rules. Un-migrated rows (no examples) keep the pre-v2 byte-for-byte legacy emit so existing catalogs round-trip unchanged. New helpers: `normalizeMatcherExamplesDraft` (mirrors `backend/src/utils/matcher-routing-resolve.ts#normalizeMatcherExamplePhrases`), `exampleTextToList`, `exampleListToText`. UI: new `ExamplePhrasesField` subcomponent in `ServiceOfferingDetailDrawer.tsx` provides a newline-separated textarea + chip preview + remove-chip + count/cap indicator (24-phrase × 120-char limit). **Not this service when…** stays as the secondary input. Legacy `keywords` / `include_when` textareas are tucked behind a `<details>` disclosure shown **only** for un-migrated rows (`hasUnmigratedLegacyHints`); new rows never see them — Task 07 will polish this into a full migration banner. AI suggest plumbing extended end-to-end: `AiSuggestSingleCardPayload.existingHints.examples?: string[]` + `AiSuggestCardV1.matcher_hints.examples?: string[]` in `frontend/lib/api.ts`; drawer sparkle button + page-level `fill_with_ai` action both forward `matcherExamples` to the backend; diff modal renders an Example-phrases row and prefers v2 examples on apply (legacy fields zeroed once doctor accepts examples). Backend route (`backend/src/routes/api/v1/catalog.ts`) `existingHints` schema accepts `examples: string[]` (max 24 × 200 chars, looser than persisted bounds so in-progress drafts don't 400) and counts it toward the single_card "has input" guard. `buildSingleCardPrompt` renders `examples: a | b | c` as a dedicated line and **suppresses** legacy `keywords` / `include_when` lines when `examples` is present so the LLM never sees two competing routing vocabularies for the same card. Local catalog-quality badge (`frontend/lib/catalog-quality-local.ts`) is now resolver-aware: `hasEmptyHints` / token count treat v2 `examples` as routing material, and `include_when` is suppressed when `examples` is non-empty (matches Task 05's resolver-driven `runDeterministicCatalogReview`). 4 new backend Jest tests under `buildSingleCardPrompt — existingHints.examples (Routing v2 Task 06)`: examples renders + suppresses legacy on the same card, legacy fallback when examples absent/empty, route schema accepts `examples`, route schema stays strict (rejects unknown matcher-hint fields). Verification: frontend `tsc --noEmit` clean; `next lint` clean on `lib/`, `components/practice-setup/`, `app/dashboard/settings/practice-setup/`. Backend `tsc --noEmit` + ESLint clean on touched files; full backend suite **1010/1010 / 57 snapshots** across 81 suites (+4 vs Task 05's 1006). Grep across `frontend/`: the only writer of `matcher_hints.keywords` is the legacy fallback branch of `draftsToCatalogOrNull` (un-migrated rows only — required for byte-for-byte legacy round-trip until Task 07 ships). Files touched: `frontend/lib/service-catalog-drafts.ts`, `frontend/lib/api.ts`, `frontend/lib/catalog-quality-local.ts`, `frontend/components/practice-setup/ServiceOfferingDetailDrawer.tsx`, `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`, `backend/src/routes/api/v1/catalog.ts`, `backend/src/services/service-catalog-ai-suggest.ts`, `backend/tests/unit/services/service-catalog-ai-suggest.test.ts`. Plan + task marked done; `service-catalog-matching-stages.md` code map + Routing v2 note + Last-updated line refreshed; v2 (`examples`-only) rows now round-trip end-to-end from the editor through Stage A, the LLM prompt, AI suggest, and review.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 05 (Phase 1.3):** AI suggest, catalog review, and the doctor-context AI prompt now consume `resolveMatcherRouting` exclusively — v2 (`examples`-only) rows route end-to-end from Stage A through review without bypassing the resolver. `service-catalog-ai-suggest.ts#summarizeExistingCatalogForLlm` sources `keywords="..."` from `resolved.examplePhrases.join(', ')` (so v2 rows surface in `single_card`, `starter`, and `review` system prompts); `tokenizeOfferingKeywords` (renamed from `tokenizeKeywords`) tokenizes resolved phrases so a v2 examples-only sibling still trips `keyword_overlap_with_sibling`; deterministic review counters (`runDeterministicCatalogReview` + `hasEmptyMatcherHints`) use `resolved.examplePhrases.length` and `resolved.legacyIncludeWhen` so `strict_thin_keywords` / `strict_empty_hints` honor v2 hints (and `exclude_when`-only cards stay empty — preserves pre-v2 asymmetry). `consultation-fees.ts#formatMatcherHintsForAiContext` signature changed from `(hints)` to `(offering)` and routes through the resolver; LLM-facing vocabulary (`keywords=…; include_when=…; exclude_when=…`) intentionally preserved to avoid prompt drift — v2 rows fill `keywords=` from resolved phrases and omit `include_when=`. AI suggest's LLM-output normalizer (`normalizeAndValidateCard`) now passes through `matcher_hints.examples[]` so future LLM contracts (Task 06) can emit v2 hints without further plumbing. 10 new tests: 7 in `service-catalog-ai-suggest.test.ts` (catalog summary v2 render, v2 examples win over legacy in summary, strict_thin_keywords on v2 row, no false-positive on rich v2 row, no strict_empty_hints on v2-filled row, exclude_when-only stays empty, sibling-overlap on v2 examples-only siblings) + 3 in `consultation-fees.test.ts` (v2 row renders via resolver / no legacy bleed, legacy row backward-compat, v2+legacy mixed row prefers examples). `tsc --noEmit` clean; ESLint clean on all 4 touched source files; full backend suite **1006/1006 / 57 snapshots** across 81 suites (+10 vs Task 04's 996/996). Files: `backend/src/services/service-catalog-ai-suggest.ts`, `backend/src/utils/consultation-fees.ts`, `backend/tests/unit/services/service-catalog-ai-suggest.test.ts`, `backend/tests/unit/utils/consultation-fees.test.ts`. Grep across `backend/src/`: no routing/LLM consumer reads `matcher_hints.keywords` / `include_when` directly outside `matcher-routing-resolve.ts`. Allow-listed remainders are all writer-side (`appendMatcherHints*`, `service-match-hint-sanitize`, schema merge — Task 06 owns the v2 push), payload validation (`routes/api/v1/catalog.ts`), and LLM input/output schema-shaping in AI suggest (`existingHints` payload + LLM-output legacy-field normalization). Plan + task marked done; `service-catalog-matching-stages.md` code map + Routing v2 note updated.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 04 (Phase 1.3):** Stage A + LLM prompt builder now consume `resolveMatcherRouting` exclusively. Stage A (`backend/src/utils/service-catalog-deterministic-match.ts#matcherHintScore`) operates on `resolved.examplePhrases` (`+4` per substring hit), `resolved.legacyIncludeWhen` (loose-overlap gate, legacy rows only), and `resolved.excludeWhen` (`-1` red flag). Stage B snippet (`service-catalog-matcher.ts#matcherHintsSnippetForLlm`) reads via the same resolver but **keeps the LLM-facing vocabulary** (`keywords=…; include_when=…; exclude_when=…`) so the existing matching-policy rules in `buildServiceCatalogLlmSystemPrompt` continue to bind without prompt-text drift — only the data source flips. v2 (`examples`-only) rows feed `keywords=` from `resolved.examplePhrases.join(', ')` and omit `include_when=`; legacy rows produce byte-identical snippets to pre-v2. Resolver field renamed `llmIncludeWhen` → `legacyIncludeWhen` (more honest: both Stage A and Stage B legitimately read `include_when` on legacy rows). SFU-18 strict-mode rule preserved verbatim — strict requires a positive `examplePhrases` hit, an `include_when` overlap alone never auto-routes. `appendMatcherHintsOnDoctorCatalogOffering` (`doctor-settings-service.ts`) keeps appending to legacy fields until Task 06 ships the editor's `examples` UI; extended JSDoc explains the deferral (avoid silent dual-write while editor still surfaces only legacy text areas; reader path is already safe via resolver, so writer deferral doesn't gate anything). 8 new tests across `service-catalog-deterministic-match.test.ts` (5: v2 strict examples-only routes, v2 wins over legacy, v2 ignores legacy `include_when` gate, v2 honors `exclude_when`, blank examples falls through to legacy_merge) + `service-catalog-matcher.test.ts` (3: v2 row serializes into `keywords=` snippet, v2 wins over legacy in snippet, legacy-only keeps `include_when=` snippet). All pre-existing tests pass unmodified — zero behavior drift on legacy rows. `tsc --noEmit` clean; ESLint clean on all 4 touched source files; full backend suite **996/996 / 57 snapshots** across 81 suites (+8 vs Task 03's 988). Files: `backend/src/utils/matcher-routing-resolve.ts`, `backend/src/utils/service-catalog-deterministic-match.ts`, `backend/src/services/service-catalog-matcher.ts`, `backend/src/services/doctor-settings-service.ts`, `backend/tests/unit/utils/matcher-routing-resolve.test.ts`, `backend/tests/unit/utils/service-catalog-deterministic-match.test.ts`, `backend/tests/unit/services/service-catalog-matcher.test.ts`. Remaining direct readers are out of scope for Task 04: `service-catalog-ai-suggest.ts` + `consultation-fees.ts#formatMatcherHintsForAiContext` (AI suggest paths — Task 05), `routes/api/v1/catalog.ts` (request-payload validation, not routing). `service-catalog-matching-stages.md` Stage A + Stage B sections + code map updated.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 03 (Phase 1.2):** New module `backend/src/utils/matcher-routing-resolve.ts` exports `resolveMatcherRouting(offering): ResolvedRoutingHints` plus helpers (`normalizeMatcherExamplePhrases`, `legacyKeywordsToPhraseParts`, types `ResolvedRoutingHints` and `ResolvedRoutingLegacySource`). Contract: `{ examplePhrases, excludeWhen?, legacySource: 'examples' | 'legacy_merge', llmIncludeWhen? }` — `llmIncludeWhen` is set **only** in legacy_merge so Stage A never receives `include_when` prose as keyword tokens (preserves today's asymmetry, but centralized). Legacy merge algorithm frozen in JSDoc + tests: split `keywords` on `[,;\n\r]+`, trim, dedupe case-insensitive (preserve first-seen order), clip per-phrase to `MATCHER_HINT_EXAMPLE_MAX_CHARS`, cap at `MATCHER_HINT_EXAMPLES_MAX_COUNT`. When `examples` normalize to non-empty, legacy strings are ignored entirely (no dual-feed). 18 unit tests in `backend/tests/unit/utils/matcher-routing-resolve.test.ts` across 4 describe blocks (examples path, legacy_merge golden fixtures, edge cases, helpers). No callers wired yet — Tasks 04–05 swap Stage A + LLM prompt + AI suggest off direct `matcher_hints.keywords` / `include_when` reads. `tsc --noEmit` clean; full backend suite **988/988** (81 suites). `service-catalog-matching-stages.md` code map updated.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 02 (Phase 1.1):** Optional `matcher_hints.examples: string[]` on `ServiceMatcherHintsV1` — max 24 items × 120 chars (`MATCHER_HINT_EXAMPLE_MAX_CHARS`, `MATCHER_HINT_EXAMPLES_MAX_COUNT`) in `backend/src/utils/service-catalog-schema.ts` + `frontend/lib/service-catalog-schema.ts`. Legacy `keywords` / `include_when` retained; JSDoc marks routing v2 + `resolveMatcherRouting` (Task 03). `appendMatcherHintFields` preserves `examples` when merging legacy strings. Tests in `backend/tests/unit/utils/service-catalog-schema.test.ts`. `service-catalog-matching-stages.md` code map updated.

- [x] 2026-04-19 — **Plan service catalog matcher routing v2 — Task 01 (Phase 0):** Added `docs/Development/service-catalog-matching-stages.md` (Stage A deterministic → Stage B LLM only when Stage A returns null; single-fee / skipLlm / no-client shortcuts; code map; FAQ). Linked from `docs/Reference/AI_BOT_BUILDING_PHILOSOPHY.md` Related section as the intentional hybrid exception to “LLM first.” `@see` pointers in `backend/src/services/service-catalog-matcher.ts` and `backend/src/utils/service-catalog-deterministic-match.ts`. Practice setup: second paragraph under **Matching hints** in `frontend/components/practice-setup/ServiceOfferingDetailDrawer.tsx` (two-step matching, keywords + “book when” both matter when exact keywords missing). Plan + task marked done.

code related 

[] save and leave  on all pages of settings just like how its set for services catalog
[] description on service catalog 
[] booking flow improvments - consent taking flow issue 
[] doctor availability time on payment slot links in case opd mode is set to queue ,
[] always verify from doctor before booking first mode, some docs may prefer this 
[] only onE APPOINTMETN FEE FLOW ?  in case any doc only wants one appointment fee , others/not listed coloumn should be present only if multiple services are to be selected by doc with different prices ?
[] placeholder appears if want confirmation 
[] notification system for docs ( match reviews and other things )
- [x] Pre-existing test failure on `main`: `backend/tests/unit/workers/webhook-worker-characterization.test.ts` — "confirm_details yes → consent (self booking)" expects `/Anything else|extras/i` but current extras prompt uses "Any special notes for the doctor — like allergies, medications, or preferences". Update either the assertion or the copy to realign. → Fixed 2026-04-16 by widening the regex to `/special notes|allergies|Anything else|extras/i`.
- [ ] Follow-up to task-04 (service scope mode): add a dismissible one-time migration banner in practice setup explaining "New services default to Strict matching; existing services default to Flexible". Needs localStorage or `doctor_settings.ui_flags_json` persistence. Inline helper in the drawer was shipped as interim education.
- [ ] Follow-up to task-06 (AI auto-fill): per-doctor token-budget telemetry for `POST /api/v1/catalog/ai-suggest` (currently only the OpenAI helper logs token counts globally). Open question 1 from plan-02.
- [ ] Follow-up to task-06: add a route-level test harness for `POST /api/v1/catalog/ai-suggest` (express + supertest + auth-token mock). Service layer is fully unit-tested in `tests/unit/services/service-catalog-ai-suggest.test.ts`; the route file itself is currently only covered by tsc + Zod schema validation.
- [ ] Follow-up to task-06: bootstrap Jest in `frontend/` so we can add a unit test for the `aiSuggestionMeta` round-trip in `lib/service-catalog-drafts.ts` (`offeringToDraft` strips it, `draftsToCatalogOrNull` ignores it). Today the frontend has Playwright e2e only.
- [ ] Follow-up to task-06: manual end-to-end click-through of all four AI trigger points (empty-catalog starter, new-card inline banner, sparkle on empty hints, sparkle on filled hints → diff modal) against a real doctor profile to validate copy + UX with live LLM output.
- [ ] Follow-up to task-07 (catalog quality checks): implement `[Fix all with AI]` bulk dispatch — runs each issue's `autoFixAvailable` action sequentially, shows one combined toast at the end, and auto-snapshots `service_offerings_json` before the run so the doctor can revert. Drafts only; never auto-persists. Blocked until snapshot/undo model is agreed on.
- [ ] Follow-up to task-07: wire `enable_modality` and `reprice` one-tap fixes once the per-channel pricing context (per-modality default prices + modality availability from `doctor_settings`) is plumbed through `applyAiSuggestionToDraft`.
- [ ] Follow-up to task-07: frontend component tests for `CatalogCardHealthBadge` (4 states + scope-aware tooltip) and `CatalogReviewPanel` (grouping, fix dispatch, save-anyway). Blocked on the existing "bootstrap Jest in `frontend/`" inbox item above.
- [ ] Follow-up to task-07: cross-workspace schema-parity runner (jest project or node script) that imports `backend/src/types/catalog-quality-issues.ts` + `frontend/lib/catalog-quality-issues.ts` and asserts enum + action equality. Today parity is enforced by `backend/tests/unit/types/catalog-quality-issues.test.ts` plus manual review.
- [ ] Follow-up to task-07: manual end-to-end of catalog review — hand-craft a catalog that triggers one of each issue type, apply each `[Fix with AI]` action, and confirm the resulting draft state.
- [ ] Follow-up to task-07: surface the mixed-complaint signal in `review` mode once Plan 01 Task 05 telemetry (per-service misroute counts) lands.
- [x] 2026-04-18 — Plan 04 (Patient DM copy polish) Task 01 shipped: `backend/src/utils/dm-copy.ts` now owns patient-facing DM strings; golden-snapshot harness at `backend/tests/unit/utils/dm-copy.snap.test.ts`; `nonTextAck` migrated (verbatim) as the seed entry. Tasks 02–11 will migrate their strings into this module. Plan: `docs/Development/Daily-plans/April 2026/18-04-2026/plan-patient-dm-copy-polish.md`.
- [x] 2026-04-18 — Plan 04 Task 02 shipped: `buildConfirmDetailsMessage` now renders a multi-line labeled block ("**Name:** …\n**Age:** …\n…\n\nIs everything correct? Reply **Yes** …") instead of the old comma chain ("Let me confirm: **X**, **Y**, …"). Builder lives in `dm-copy.ts`; `collection-service.ts` re-exports it so existing imports are unchanged. 7 new snapshot cases cover full / long-reason / missing-email / missing-reason+email / gender normalization (MALE → Male, female → Female) / whitespace-only reason. Full suite: 80 suites / 844 tests / 8 snapshots green. Follow-up: manual DM screenshot in staging once deployed.
- [x] 2026-04-18 — **Bug-fix: "Review my catalog" now critiques the on-screen draft, not the stale DB row.** Reported behavior: open `Settings → Practice setup → Services catalog`, hit `Review my catalog`, click `Add suggested card`, then click `Run review again` → the same gap suggestion came back. `Close → Save → Review my catalog` produced a fresh suggestion instead. Root cause: the `add_card` (and every other) one-tap fix only mutated local React state (`services` in `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`); `runServerReview()` then called `POST /api/v1/catalog/ai-suggest` with `{ mode: "review" }` and the backend (`loadAiSuggestContext` in `backend/src/services/service-catalog-ai-suggest.ts`) rehydrated the catalog from `doctor_settings.service_offerings_json` — which still didn't contain the just-added card. So the LLM critiqued the saved snapshot, not what the doctor was looking at. Fix (Option B — "always send the on-screen draft"): (1) extended `AiSuggestRequest.catalog?: ServiceCatalogV1 | null` with three explicit states — `undefined` = use DB (legacy callers / smoke), `null` = "draft is empty" (deterministic `missing_catchall` still fires without a DB read), object = use this exact catalog. Documented the contract in JSDoc on both backend (`AiSuggestRequest`) and frontend (`api.ts AiSuggestRequest`) types. (2) `loadAiSuggestContext` gained a `LoadAiSuggestContextOptions` parameter; when `catalogOverride` is set we use it as-is and skip `safeParseServiceCatalogV1FromDb`. Telemetry: a new `service_catalog_ai_suggest: catalog source resolved` info log records `catalogSource: 'client_draft' | 'db'` + `serviceCount` (PHI-safe) so we can spot if a future caller drops the override. (3) Route schema (`backend/src/routes/api/v1/catalog.ts`) accepts `catalog: serviceCatalogV1BaseSchema.nullable().optional()` — deliberately the *base* schema (not `serviceCatalogV1Schema`) because an in-progress draft missing the catch-all is exactly the case the deterministic review should flag, so we must not reject it at the boundary. (4) `runServerReview()` now runs the same `draftsToCatalogOrNull` + `safeParseServiceCatalogV1` pipeline `performSave` uses — validation symmetry — and refuses the round-trip with a doctor-facing error in `reviewError` if the draft is invalid mid-edit (e.g. catch-all deleted, empty label, bad follow-up window) instead of silently stripping rows and shipping a misleading critique. The deterministic `localIssues` panel still renders, so the doctor isn't left with no feedback. (5) The post-save background review path (`performSave` → `void runServerReview().then(…)`) automatically benefits because it now sends the freshly-saved draft as the override too, eliminating any race between the PATCH commit and the review reading the previous DB snapshot. Coverage: 7 new unit tests in `backend/tests/unit/services/service-catalog-ai-suggest.test.ts` describe-block `catalog override (post-Plan-04 bug-fix)` lock all three states at both the `loadAiSuggestContext` layer and the `generateAiCatalogSuggestion` end-to-end (review mode + single_card mode), plus assert that omitting `catalog` keeps the DB path intact and that `null` skips the LLM call entirely while still firing `missing_catchall`. `tsc --noEmit` clean (backend + frontend); ESLint clean on `service-catalog-ai-suggest.ts`, `routes/api/v1/catalog.ts`, `frontend/lib/api.ts`, and `services-catalog/page.tsx`. Full backend suite: **80 suites / 964 tests / 57 snapshots** green (net +9 tests, +0 snaps vs. post-Plan-04 baseline). Files: `backend/src/services/service-catalog-ai-suggest.ts`, `backend/src/routes/api/v1/catalog.ts`, `backend/tests/unit/services/service-catalog-ai-suggest.test.ts`, `frontend/lib/api.ts`, `frontend/app/dashboard/settings/practice-setup/services-catalog/page.tsx`. Backend retains the DB-load fallback (`catalog === undefined`) on purpose so future server-side workflows (e.g. cron-driven catalog audits, admin tooling, the staff-review surface) can keep calling without an in-flight draft. Follow-up: extend the override to the editor's `single_card` triggers (sparkle on hints, fill_with_ai from the review panel) so AI sibling-summaries also see unsaved cards; for now `handleApplyFix`'s `fill_with_ai` / `switch_to_strict_and_fill` calls still pass no `catalog`, which is acceptable because the bug the user reported was specifically about review re-runs.
- [x] 2026-04-18 — **Plan 04 (Patient DM copy polish) complete — all 11 tasks shipped.** Task 11 (P3, PR-5) closes the plan with a softer **non-text acknowledgement**: when a patient sends an image, voice note, sticker, or reaction, `buildNonTextAckMessage()` in `backend/src/utils/dm-copy.ts` now returns `"I can't read images or voice notes yet — could you type your message instead? I'll take it from there."` instead of the pre-2026-04-18 bot-framed `"I can only process text messages right now. Please type your request and I'll help you."`. Rationale captured in an extended JSDoc above the function (supersedes Task 01's "verbatim seed" placeholder): the old copy framed the problem from the bot's side ("I can only process…") — patients care that their message didn't land and what to do next. New copy names the two inputs patients most plausibly expect a response to (images, voice notes); stickers and reactions are deliberately unnamed because they rarely carry a question. English-only by design, matching the plan's non-goals (if localized variants are needed, add them as a follow-up task). No signature change: the single caller in `backend/src/workers/instagram-dm-webhook-handler.ts:1253` (`const nonTextAck = buildNonTextAckMessage();`) is untouched, and the suppression / audit-log logic around it is untouched too (scope guard was explicit about not re-gating when the ack fires). Coverage: the pre-existing `dm-copy snapshots nonTextAck / default` snapshot was regenerated via `jest -u` (snap count unchanged at 57; this is the single snapshot that was intentionally updated in place, which is why the plan's snapshot-suite baseline stays at 57 rather than 58). Added a new `buildNonTextAckMessage invariants (Task 11)` describe block in `backend/tests/unit/utils/dm-copy.snap.test.ts` with 2 tests guarding the *shape* independently of the *wording*: (a) single-line only (`split('\n').length === 1`), no markdown bolding (`**` banned), no emoji (explicit negative assertion against the Unicode symbol + pictograph ranges `\u{1F300}-\u{1FAFF}` and `\u{2600}-\u{27BF}`), names "images" + "voice notes" + contains "type" so the patient is always told what to do; (b) drops the legacy `process text messages` wording so it can't silently resurface via a future copy-paste. `tsc --noEmit` clean; ESLint clean on both touched files (`dm-copy.ts`, `dm-copy.snap.test.ts`). Full suite: **80 suites / 955 tests / 57 snapshots** green — net +2 tests, +0 snaps vs. the post-Task-10 baseline, zero regressions. **Plan 04 final state:** all 11 tasks `[x]`, the plan's "Acceptance for the plan" checklist is 4-of-5 complete (the last item — staging smoke — is deferred to PR-4 + PR-5 rollout per individual task docs). Source of truth for patient-facing DM copy is now `backend/src/utils/dm-copy.ts` + locale-specific helpers in `complaint-clarification.ts` + `reason-first-triage.ts`; golden-snapshot suite covers every message in `backend/tests/unit/utils/__snapshots__/dm-copy.snap.test.ts.snap`. Before / after on Task 11 example: image DM → old `"I can only process text messages right now. Please type your request and I'll help you."` → new `"I can't read images or voice notes yet — could you type your message instead? I'll take it from there."`. Follow-up: staging smoke — send an image, a voice note, and a sticker to the bot; all three should trigger the same new copy (the suppression logic already dedupes a second ack within the throttle window, unchanged).
- [x] 2026-04-18 — Plan 04 Task 10 shipped (P3, PR-5): reason-first triage **ask-more-after-deflection** copy split from one cramped sentence into two scannable paragraphs across all 3 locale leaves in `backend/src/utils/reason-first-triage.ts` (`clinicalDeflectionAskMoreEnglish` / `clinicalDeflectionAskMoreHi` / `clinicalDeflectionAskMorePa`) × 3 sub-branches (blank snippet / multi-line `\n`-bearing snippet / single-line snippet) = 9 rendered variants. English target shape for the blank-snippet branch: `**Is there anything else** you'd like the doctor to address at this visit?\n\nIf that's the full picture, reply **nothing else** and we'll move to **booking** or **fees**.`; snippet-bearing branches use the shortened `**Is there anything else** you'd like the doctor to address?` question and `If that covers it, reply **nothing else** and we'll move to **booking** or **fees**.` escape-hatch, preserving the `**So far we've noted:**` header (block variant uses `\n\n`-wrapped snippet; single-line uses inline `**{snippet}**.`). Hinglish and Roman-Gurmukhi copy kept byte-for-byte — only `\n\n` was inserted after the question mark (and, where applicable, the `phir …` / `ta …` next-step sentence moved to its own paragraph). **Scope correction vs. the original task doc:** doc projected 15 snapshots (3 sub-branches × 5 locales including Roman Hindi / Roman Punjabi). In practice `detectSafetyMessageLocale` (`backend/src/utils/safety-messages.ts`) collapses Roman-script markers (`mujhe`, `mera`, `menu`, `naal`, …) into the `hi` / `pa` return values, and the existing `reason-first-triage.ts` strings for those locales are already transliterated — so Roman and Devanagari/Gurmukhi inputs route through the same leaf helper. Real target is 3 × 3 = 9 snapshots; the task doc top-note records this. **Tests:** 9 snapshots added to the shared `dm-copy.snap.test.ts` via `formatClinicalReasonAskMoreAfterDeflection(userText, snippet)` as the public entry point (keys `dm-copy snapshots triage / {en,hi,pa} / {blank, single-line, multi-line} snippet`), plus 6 invariant tests under a new `formatClinicalReasonAskMoreAfterDeflection invariants (Task 10)` block — asserting the `?\n\n` break presence, snippet-header prefix (`**So far we've noted:** **…**.` for single-line, `**So far we've noted:**\n\n…\n\n` for multi-line; locale equivalents `**Ab tak note kiya:**` / `**Haje tak note kita:**`), all bold phrase tokens preserved (`**Is there anything else**` / `**Kya aur kuch**` / `**Hor kuj**`, `**nothing else**`, `**booking**`, `**fees**`), and a regression guard that forbids the legacy single-line tail pattern `Reply **nothing else** if … — then we can (help with|move to) booking or fees` in every user-text × snippet combination. No other test needed migration — search confirmed no pre-existing unit/characterization test asserted on the old exact strings (only the source file itself held the literals). `tsc --noEmit` clean (15 s); ESLint clean on both touched files (`reason-first-triage.ts` + `dm-copy.snap.test.ts`). Full unit suite: **80 suites / 953 tests / 57 snapshots** green — net +15 tests, +9 snaps vs. post-Task-09 baseline (938 / 48), zero regressions. **Before / after example (en, blank snippet):** old — `"**Is there anything else** you'd like the doctor to address at this visit? Reply **nothing else** if what you shared is the full picture — then we can help with **booking** or **fees** next."` (one sentence stacking question + escape hatch + next-step preview + em-dash bridge); new — `"**Is there anything else** you'd like the doctor to address at this visit?\n\nIf that's the full picture, reply **nothing else** and we'll move to **booking** or **fees**."` (question on line 1, escape-hatch + next-step on line 3 with a blank line between). Follow-up: staging smoke on English + one Roman locale (Hinglish suggested) to verify the Instagram renderer honors the paragraph break at the client — deferred to PR-5 rollout.
- [x] 2026-04-18 — Plan 04 Task 09 shipped (P3, PR-5): mixed-complaint clarification now echoes the parsed concerns back as a numbered pick-list so the patient can reply with a number instead of re-typing. Three-layer change. **Matcher (`backend/src/services/service-catalog-matcher.ts`):** added `concerns?: string[]` to `ServiceCatalogMatchResult`; extended the LLM system prompt with a dedicated "Concerns list (only when mixed_complaints is true)" rules block + updated the schema line to `{"service_key":…,"mixed_complaints":true|false,"concerns":["<label1>",…]}`; added `normalizeLlmConcerns(raw)` at the parser boundary (array-only, trims entries, truncates > 40 chars with `…`, case-insensitive dedupe first-wins, caps at `SERVICE_MATCH_MAX_CONCERNS = 5`, returns `undefined` when fewer than 2 valid entries remain — `undefined` signals "render the existing open-ended copy"); `concerns` is populated only when `mixedComplaints === true` (hallucinated lists alongside `mixed_complaints:false` are dropped silently). Exported the new caps so every caller sees one contract. **Copy (`backend/src/utils/complaint-clarification.ts`):** extended `resolveComplaintClarificationMessage(userText, parsedConcerns?: readonly string[])` with an optional second arg — when `parsedConcerns.length ∈ [2, 5]` it renders a three-paragraph numbered-list shape (intro + `**N.** {label}` list + grammatical CTA with bolded numeric choices: `Reply **1** or **2**` / `Reply **1**, **2**, or **3**` / up to `Reply **1**, **2**, **3**, **4**, or **5**`); outside that range (0/1/6+, `undefined`) it returns today's locale string unchanged so legacy callers and in-flight LLM responses never regress. 5 locale variants via a `CLARIFICATION_NUMBERED_BY_LOCALE` table keyed by `en | hi | pa | latin-hi | latin-pa`, each owning `intro`, `ctaTemplate` with `{choices}` token, and a `joinChoices` function (Oxford-joiners for English/Hinglish/Roman-Punjabi; Devanagari `"या"` and Gurmukhi `"ਜਾਂ"` conjunctions for script variants). Concern labels themselves render English verbatim per the task's "English concerns in all locales for now" design constraint (localized concerns is a matcher-quality concern, not a copy concern). Also added `resolveClarificationNumericReply(replyText, concerns): string | null` — a pure helper that accepts only 1–2 ASCII digits, rejects free-text / non-integer / out-of-range / negative / decimal / hex inputs, and returns the mapped concern label or `null` (no locale-digit parsing for now; future enhancement). **State + handler (`backend/src/types/conversation.ts` + `backend/src/workers/instagram-dm-webhook-handler.ts`):** added `pendingClarificationConcerns?: string[]` to `ConversationState`, documented as "may contain PHI, same compliance posture as `originalReasonForVisit`, cleared on every exit from `awaiting_complaint_clarification`"; `maybeTriggerComplaintClarification` persists the concerns list on state in the same tick it transitions to `step = 'awaiting_complaint_clarification'` and passes the list to the copy helper so the patient sees the numbered list; the clarification-requested log breadcrumb now includes `clarificationConcernCount` (count only — the labels themselves are never logged at `info` to avoid PHI-adjacency). Added a numeric-reply short-circuit inside the first-attempt branch of the `awaiting_complaint_clarification` dispatch: `const mappedConcern = resolveClarificationNumericReply(rawReply, state.pendingClarificationConcerns); const narrowedReason = mappedConcern ?? rawReply;` — so `"2"` → matcher sees `"Diabetes follow-up"` as the narrowed reason instead of an ambiguous `"2"`. Invalid digits (`"9"` when only 3 concerns) resolve to `null` and fall through to the raw reply, which typically produces a low-confidence match and escalates to staff review on this attempt (intentional: the patient gave us no usable signal). `pendingClarificationConcerns` is cleared on every exit path — consent-resume, staff-review-escalate, cap-reached — so stale concerns from a prior round never leak into a later clarification event with a different complaint set. **Tests:** 7 new matcher tests (prompt schema includes concerns spec; LLM `mixed_complaints:true + concerns` surfaces the array; > 40-char entries truncated with `…`; case-insensitive dedupe first-wins; `mixed_complaints:false + concerns` drops the list; < 2 valid entries → `undefined`; single-fee short-circuit reports `concerns:undefined`). 9 new snapshots in `dm-copy.snap.test.ts` (en/2, en/3, en/5, en/1-fallback, en/6-fallback, hi Devanagari/3, pa Gurmukhi/3, latin-hi/3, latin-pa/3). Two new invariant blocks totaling 15 unit tests: `resolveComplaintClarificationMessage invariants` (7 tests covering paragraph structure, bold-dot numbers, grammatical CTA joins, fallback ranges, script detection for hi/pa, Roman variants, verbatim concern labels across all 5 locales) + `resolveClarificationNumericReply` (8 tests covering valid mapping, whitespace tolerance, out-of-range null, free-text null, undefined/empty concerns null, > 2-digit rejection, negative/decimal/hex rejection). Handler integration coverage is via the pure `resolveClarificationNumericReply` helper — the handler wiring is a single-line `?? rawReply` fallback; end-to-end dispatch tests deferred to staging smoke. Pre-existing tests in `complaint-clarification.test.ts` (single-arg legacy behavior across all 5 locales + `shouldRequestComplaintClarification` predicate) still pass unchanged thanks to the optional second arg. Full suite: **80 suites / 938 tests / 48 snapshots** green, up from 80/923/39 post-Task-08 (+15 tests / +9 snapshots, zero regressions). `tsc --noEmit` clean (14.5 s); ESLint clean on all 6 touched files. **Before / after example (en, 3 concerns):** old — `"You've mentioned a few concerns. Which one would you like to consult about first? We can address the others in a follow-up visit."`; new — `"You've mentioned a few concerns:\n\n**1.** Headache\n**2.** Diabetes follow-up\n**3.** Knee pain\n\nWhich one is the main reason for this visit? Reply **1**, **2**, or **3** — we can handle the rest in a follow-up."`. Out-of-range / single / 6+ concerns gracefully falls back to the legacy one-liner. Follow-up: staging smoke with a deliberately mixed-complaint opening message to verify the numbered list renders correctly on Instagram iOS and that replying `"2"` continues the booking flow for the correct concern.
- [x] 2026-04-18 — Plan 04 Task 08 shipped: `buildStaffReviewResolvedBookingMessage` + `StaffReviewResolvedKind` added to `backend/src/utils/dm-copy.ts`; the pre-existing ARM-05 entry point `formatStaffReviewResolvedContinueBookingDm` in `backend/src/utils/staff-service-review-dm.ts` (lines 52–70) now delegates to the new builder so both call sites — `service-staff-review-service.ts:431+` (staff confirm/reassign flow) and `service-match-learning-autobook.ts:162+` (learning-policy autobook) — pick up the new layout without any caller changes. Old copy: `"{intro} You can **pick a time and complete booking** here — tap to open: {url}\n\nIf something looks wrong, just reply here in this chat."` (URL hugs the end of a sentence — unreliable tap target on Instagram iOS; bold CTA verb buried mid-sentence rather than next to the link). New copy is a three-paragraph shape: (P1) kind-specific intro sentence unchanged — `"**{practice}** has confirmed your visit type: **{label}**."` / `"… has updated your visit type to **{label}**."` / `"… has applied your saved visit-type preference: **{label}**."`; (P2) `"Pick a time and complete your booking here:\n{url}"` with the URL isolated on its own line for a clean tap target; (P3) verbatim `"If something looks wrong, just reply here in this chat."` closer. Fallbacks preserved verbatim — empty/whitespace `practiceName` → `"the clinic"` (matches sibling `formatAwaitingStaffServiceConfirmationDm`), empty/whitespace `visitLabel` → `"your visit"`. `bookingUrl` is throw-guarded on empty/whitespace for symmetry with the Task-06 abandoned-reminder helper: every call site already computes the URL via `buildBookingPageUrl` upstream, so an empty value signals an upstream config bug (missing `BOOKING_PAGE_URL` env / bad conversation or doctor id) that we want surfaced, not a silent no-CTA DM. Sibling helpers in the same file (`formatAwaitingStaffServiceConfirmationDm`, `formatStaffServiceReviewStillPendingDm`) are intentionally left untouched per task scope — they don't have URL-hugging issues today. 5 new snapshots cover all 3 `kind` variants with practice+label present, plus the two fallback paths (practice missing → `"the clinic"`, visit label empty → `"your visit"`). 6 new unit tests in a dedicated `buildStaffReviewResolvedBookingMessage invariants` describe block assert: (1) throws on empty / whitespace `bookingUrl`; (2) URL sits on its own line below the `"Pick a time and complete your booking here:"` label across all three kinds; (3) `practiceName` missing/empty/whitespace all produce identical `"the clinic"` output; (4) `visitLabel` missing/empty/whitespace all produce identical `"your visit"` output; (5) each `kind` produces a distinct intro phrasing (`confirmed` / `updated` / `applied your saved visit-type preference`); (6) closing paragraph is `"If something looks wrong, just reply here in this chat."` and the legacy `"tap to open"` / `"pick a time and complete booking"` substrings are no longer present. Pre-existing tests in `staff-service-review-dm.test.ts` and `staff-service-review-dm-autobook.test.ts` only asserted on substrings (practice name, visit label, URL, `updated`, `confirm`, `saved visit-type preference`) — all still pass against the new three-paragraph layout without modification. Full suite: 80 suites / 908 tests / 39 snapshots green. `tsc --noEmit` clean; ESLint clean on all three touched files (`dm-copy.ts`, `staff-service-review-dm.ts`, `dm-copy.snap.test.ts`). Follow-up: manual staff-review resolution smoke in staging (or hit the service-match-learning autobook path with a repeat patient whose saved preference matches an active service) to visually confirm the URL renders on its own line in Instagram DM.
- [x] 2026-04-18 — Plan 04 Task 07 shipped: `buildCancelChoiceListMessage` + `appointmentConsultationTypeToLabel` + `formatAppointmentChoiceDate` added to `backend/src/utils/dm-copy.ts` and wired into both cancel branches of `backend/src/workers/instagram-dm-webhook-handler.ts` (the `upcoming.length === 1` ad-hoc string at line 2365 and the multi-upcoming pick-list at line 2380). Cancel pick-list is now adaptive: (a) 1 item → `"You have one upcoming appointment: **Tue, Apr 29 · 4:30 PM — Video consult**.\n\nReply **Yes** to cancel it, or tell me what else to do."`; (b) 2 items → header + bolded numbered list (`**1.** {date} — {modality}`) + `"Reply **1** or **2**."`; (c) N≥3 → header + bolded list + `"Reply a number from **1** to **N**."`. Each item renders the choice key in bold (`**1.**`) separated from content by a single space, and appends a patient-facing modality suffix after an em-dash (`— Video consult` / `— In-person` / `— Text consult` / `— Voice consult`) when `a.consultation_type` is a known enum (`'text' | 'voice' | 'video' | 'in_clinic'` per `backend/src/types/database.ts:115`); unknown/null enums omit the suffix rather than leak a raw token into the DM. Date format is en-US middot (`"Tue, Apr 29 · 4:30 PM"`) via the new `formatAppointmentChoiceDate(iso, tz)` helper that builds on `formatDateWithMiddot` — this deliberately diverges from the legacy `formatAppointmentStatusLine` (en-GB, day-before-month, lowercased AM/PM) so the cancel DM matches the Task-05 payment DM's date shape for visual consistency. Fixes three audit findings: (1) awkward `"Reply 1, 2, or 3"` trailer (now always grammatical at all N); (2) unbolded choice keys making numbers hard to scan; (3) no modality hint so patients with mixed types couldn't tell which row was the teleconsult vs the in-clinic visit. State transitions (`awaiting_cancel_confirmation` for single, `awaiting_cancel_choice` for multi, `pendingCancelAppointmentIds` mapping) are untouched — only the prompt copy changes. Reschedule branch at line 2422 intentionally left untouched per task scope (`"Reschedule list (parallel UI) is NOT in this task"`); a follow-up can reuse the same builder renamed to `buildAppointmentChoiceListMessage` when that ships. 4 new snapshots (1/2/3/5-item cases, last one mixes known and unknown modalities to prove the suffix-omission path) + 9 new unit tests (3 for the label helper: canonical map / case+whitespace normalization / undefined fallback for null/empty/`in_person` typo/`zoom`; 6 for the builder: empty-items throws / modality omission on undefined+empty+whitespace / 2-item trailer regex / N≥3 trailer regex / single-item branch has no numbered list or trailer-number / `**N.**` bolding + input-order preservation). No existing tests asserted on the old cancel strings (`webhook-worker-characterization.test.ts:550` only exercises the state transition after the user picks a number, not the prompt text). Full suite: 80 suites / 897 tests / 34 snapshots green. `tsc --noEmit` clean; ESLint clean on all three touched files. Follow-up: manual cancel-intent smoke in staging with 2 upcoming appointments to visually confirm bold numbers + `"Reply 1 or 2"` trailer + modality suffixes render correctly in Instagram DM.
- [x] 2026-04-18 — Plan 04 Task 06 shipped: `buildAbandonedBookingReminderMessage` added to `backend/src/utils/dm-copy.ts` and wired into `backend/src/services/abandoned-booking-reminder.ts:59`, replacing the inline one-liner `"Just checking in — your booking link is still active if you'd like to complete it. Reply anytime if you need help."`. New copy is a three-paragraph nudge: (a) `"Just checking in — your booking link is still active."` status line, (b) `"Pick a time here:\n{bookingUrl}"` with the URL on its own line and blank lines above/below so Instagram auto-linkifies it reliably (intentionally no markdown link — Instagram DMs don't render them), (c) `"Reply here anytime if you need help."` closing. The URL is regenerated per-reminder via `buildBookingPageUrl(row.id, row.doctor_id)` from `slot-selection-service.ts`, so the reminder points at the same conversation-scoped booking token as the original link — patient no longer has to scroll up through hours of DMs to find the tappable link. Builder throws on empty/whitespace `bookingUrl`; `buildBookingPageUrl` itself always returns a non-empty string (falls back to `https://example.com/book` when `BOOKING_PAGE_URL` unset), so the guard surfaces upstream config bugs rather than silently shipping a no-CTA reminder. Cron eligibility (`step === 'awaiting_slot_selection'`, `bookingLinkSentAt` past the 1-hour cutoff, `bookingReminderSent !== true`), token lookup, `bookingReminderSent` metadata write, and error counters are all untouched. 1 new snapshot + 4 new invariant unit tests (empty throws / whitespace throws / URL on own line with label above + blank line below / trims whitespace around URL). No existing test asserts on the old literal string (`abandoned-booking-reminder` has no dedicated test file) so no assertion fix-ups needed; post-swap grep for the old literal returns zero hits in `backend/src` + `backend/tests`. Full suite: 80 suites / 884 tests / 30 snapshots green. `tsc --noEmit` clean; ESLint clean on all three touched files. Follow-up: manual cron trigger in staging to visually confirm the reminder DM contains a tappable URL.
- [x] 2026-04-18 — Plan 04 Task 05 shipped: `buildPaymentConfirmationMessage` + `formatDateWithMiddot` added to `backend/src/utils/dm-copy.ts` and wired into `backend/src/services/notification-service.ts:180`, replacing the inline template literal `"Payment received. Your appointment on {dateStr} is confirmed.{idLine}\n\nWe'll send a reminder before your visit."` and its `idLine` helper. Patient now sees a scan-friendly four-paragraph DM: `✅ **Payment received.**` trust-signal header, `Your appointment is confirmed for **Tue, Apr 29 · 4:30 PM**.` date line (year dropped, date/time comma rewritten as a middle dot purely for readability — no timezone math touched), an optional `🆔 **Patient ID:** CLR-00123` + `_Save this for future bookings._` italic sub-line only when MRN is non-empty after trim, and a closing `We'll send a reminder before your visit. Reply here anytime if you need to reschedule or have questions.` that proactively invites follow-ups (this is the one place in the flow where we do that; payment DM often lands hours after the booking thread ended). The ✅ and 🆔 emojis are deliberately scoped to this single message family — no other DM in Plan 04 uses emoji. `formatDateWithMiddot` is regex-based, matches `Intl.DateTimeFormat('en-US', {weekday, month, day, year, hour, minute})` output, and falls back to the input unchanged if the shape doesn't match — never throws on locale drift. 3 new snapshot cases (with MRN / without MRN / MRN-with-whitespace) + 4 `formatDateWithMiddot` unit tests + 3 `buildPaymentConfirmationMessage` invariants (MRN omission on empty/whitespace, closing paragraph, exactly-one ✅/🆔). Existing `notification-service.test.ts` stringContaining('Payment received') assertion still matches the new copy. Full suite: 80 suites / 879 tests / 29 snapshots green. `tsc --noEmit` clean; ESLint clean on all three touched files. Follow-up: manual DM screenshot in staging for both with-MRN and no-MRN variants once deployed.
- [x] 2026-04-18 — Plan 04 Task 04 shipped: `buildConsentOptionalExtrasMessage` in `backend/src/utils/dm-copy.ts` replaces both consent-step ternaries at lines 1785–1786 and 3070–3071 of `backend/src/workers/instagram-dm-webhook-handler.ts`. Patient now sees three paragraphs instead of one wall of text — greeting line ("Thanks, **{name}**.\nWe'll use **{phone}** to confirm your appointment by call or text."), the optional-notes question ("Any notes for the doctor? _(allergies, current medicines, anything else — optional)_"), and a bolded CTA ("Reply **Yes** when you're ready to pick a time.") separated by blank lines. Someone-else variant is parallel: ("Thanks." / "Do I have your consent to use these details to schedule?" / "Reply **Yes** to continue.") and asks no notes question (parity-preserving — today the clinic only collects notes on self-bookings). Helper throws when `bookingForSomeoneElse && !bookingForName` (unreachable upstream state); the self branch treats empty / `'there'` sentinel `patientName` as "missing" and renders plain `"Thanks."` instead of the old `"Thanks, there."`. Upstream detector `isOptionalExtrasConsentPrompt` (in `booking-consent-context.ts`) gained a new primary clause for the new copy with previous "special notes" + "anything else" rules kept as backward-compat fallbacks; prompt rule at `ai-service.ts:1377` also refreshed. 6 snapshot cases + 4 builder-invariant unit tests + 1 new detector regression test; existing characterization regex `/special notes|allergies|Anything else|extras/i` still matches (new copy contains both "allergies" and "anything else"). Full suite: 80 suites / 869 tests / 26 snapshots green. `tsc --noEmit` clean; ESLint clean on all six touched files. Follow-up: manual DM screenshot in staging for both self + someone-else branches once deployed.
- [x] 2026-04-18 — Plan 04 Task 03 shipped: `buildIntakeRequestMessage` in `backend/src/utils/dm-copy.ts` replaces all 9 inline intake asks in `backend/src/workers/instagram-dm-webhook-handler.ts` (the "Please share: Full name, Age, Mobile, Reason for visit…" string family + the two `Still need: …` variants). Three variants — `initial` / `still-need` / `retry-not-received` — plus `forRelation`, `alreadyHaveReason`, `includeEmail`, and an `intro` escape hatch for call-site-specific framing (e.g. the two-person "I'll help you book for you and your mother…" header, the "Got it, just your mother then" switch, and the "I didn't catch the details for the person you're booking for" retry when the relation word is unknown). Canonical field-label map `INTAKE_FIELD_LABELS` replaces 3 copies of the local `labels` Record scattered across the webhook handler. `ai-service.ts` prompt literals at lines 423 + 2023 refreshed to reference the new bulleted structure so the LLM's self-check stays accurate; the LLM-input context string `Still need: …` at line 651 is intentionally left as-is (internal prompt, not patient-facing). Snapshot suite now has 20 cases (+12 intake) plus 2 new builder-invariant unit tests (empty-`missing` throws; 64-char `forRelation` clamps to 32 + lowercases). Verification: `rg "Please share: Full name" backend/src` → zero matches; `tsc --noEmit` clean; ESLint clean on all four touched files; full unit suite 858/858 (was 844 before Task 03). **Note:** at the two-person site (line 2451) the code flow collects the OTHER person's fields first, so the rendered copy says "your {relation} first, then you" — not "yours first, then theirs" as the task doc literally quoted. Matched code semantics over literal text per the task's parenthetical "preserves the two-step framing". Follow-up: manual DM screenshot in staging for each of the three variants × relation/no-relation permutations.
## Inbox

<!-- Add `- [ ] Your item` below -->
Business related
[] start brand building insta post stories 
[] look for comments in docs post see what kind of comments there usually are present  find possible solutions
